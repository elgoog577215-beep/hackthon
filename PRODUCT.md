# 灵知产品入口

本文件是界面工具的产品上下文桥接，不是新的产品真源。正式产品边界、术语与状态分别以 [`docs/产品蓝图.md`](./docs/产品蓝图.md)、[`docs/产品状态.md`](./docs/产品状态.md) 和 [`docs/系统架构.md`](./docs/系统架构.md) 为准。

## 当前界面方向

- 教师端首页默认进入“我的课程”，同一顶栏保留“我的日历”。课程库使用一张平铺数据表，只显示课程身份、“备课中 / 备课完成”、下次上课与地点、学年学期和最后编辑；保留搜索、状态/学期筛选、表头排序、分页、直接删除与多选批量删除，不再提供卡片模式、视图切换或仅含删除项的三点菜单。
- 新建课程在一个表单中收集中英文课程名、课程代码、层次、课程类别、学分、周/总学时、先修课、学年学期、星期、节次和地点。提交后先创建空课程真源与文件空间，再按“大纲 → 教案 → 讲义 → PPT”四阶段备课；题库、日历、评分细则和自查清单属于其他课程文件。
- 点击课程默认进入备课工作台；课程文件一级目录固定为“教学大纲 / 分讲教案 / 讲义 / PPT / 其他课程文件 / 课程资料 / 回收站”。题库、评分细则和自查清单归入其他课程文件；原件只保存一份，每个生成目标可选一个主来源和多份参考资料。
- PPT 按来源分开处理：AI 生成课件绑定当前、结构可用且来源未过期的教案与讲义修订；进入 PPT 不要求确认讲义，但 PPT 内部仍保留一次“页面内容稿确认”再编译成品。老师上传的 PPTX 保留不可变原件，可建立页级文字索引和一致性审阅报告，手工或 AI 修改都先形成可恢复修订。
- 正式课程不是又一套备课文件。它在预览或上课时按当前修订组装 `CourseDocument + CourseBlock[]` 与正式练习，将分开储存的正文块和题目块合并展示。
- 大纲使用三个真实步骤：课程表单提交后先生成可编辑的轻量讲次方案，只含讲次顺序、标题和内容简介并停下；老师明确点击继续后，系统先根据最新方案生成一次课程级正式合同，再把全部讲次详情一次入队，统一并发生成目标、范围、学时和教学安排并真实流式显示，最后由本地规则按方案顺序校验汇编为完整大纲。单讲失败只重试该讲，完整大纲完成后停留在大纲页。
- 进入教案先显示当前完整大纲投影的全课生成预览，不重复课程表单、强制步骤条或必填全课配置；资料和逐讲补充要求均可选。教案和讲义的默认界面只保留“生成全部”，把符合条件的讲次一次入队并发运行；存在可恢复失败时，同一按钮原位变为“重新生成”，只恢复失败或缺失单元，不覆盖可用讲次。
- 课程工作台以中间正式内容为持续主画布：大纲、教案和讲义生成任务先执行确定性结构校验，发现问题就在当前任务中定点修复；通过后显示“已生成”并成为可编辑、可继续的当前修订，修复耗尽则只把该讲标为失败并保留检查点，不保存不合格结果。三类资产不设人工确认按钮；完整大纲形成后仍生成质量审阅报告，由低强调按钮按需打开，只提供改进建议，不改变完成状态或阻断下一步。监听器不得自动关闭编辑、继续生成、切换阶段或重生成下游。上游变化只把受影响下游标为需更新，不覆盖最后可用版本。教师主动发起的 AI 修改仍遵守“预览差异 → 教师选择应用或放弃”，不得静默覆盖当前修订；PPT 原件审阅与页面内容稿确认保持独立。
- 左侧只用一级导航表示四个生产阶段：大纲不展开讲次目录，教案、讲义和 PPT 固定显示双行真实状态的讲次目录。右栏不常驻 AI 助手或重复讲次列表，生成前显示资料准备，生成中显示当前讲任务详情，生成后显示版本与依据。每份资料可作用于全部讲次、一个连续范围或若干指定讲次；任务启动时冻结本轮资料范围与补充要求快照。
- 视觉继续使用现有蓝紫品牌色、紧凑控件和清晰边界，不用重复 Tab 复述已经由日历、文件夹和文件类型表达的层级。

## 数据边界

- 正式课程正文只有一个 `CourseDocument` 真源。
- 正文块、教学目标、知识点和题目使用稳定 ID、修订号和依赖索引；教案、讲义、PPT 与练习只保存可追溯引用与表达修订。
- 课程级教学语义由共享 `CourseTeachingPlanV3` 承载；分讲教案、讲义的当前修订、AI 候选、并发保护和来源状态由 `TeacherLessonAuthoringRepository` 持有。旧整课教案工作台及其历史修订链不再是兼容真源。
- 课程生产状态由服务端 `course_production_state_v1` 从任务、当前修订、PPT 检查点和来源可用性即时编译，不另存一份状态。课程库、工作台、教学日历和课程文件以它为唯一状态权威；投影用 `allowed_actions` 说明允许什么，用 `action_targets[action]` 绑定该动作能够操作的精确 task ID，页面不得把阶段任务 ID 做无关系并集，也不得让后端重新猜“最新任务”。迁移期由统一前端适配器优先读取新投影，只在新投影完全缺失时读旧字段；存在但不合法的新投影一律只读失败，不恢复旧授权。有最后可用修订时主状态仍为“可使用”，最近重生成失败作为辅助问题呈现。旧字段与底层单讲能力至少保留一个完整生产观察周期；新旧差异和外部单讲调用归零前不删除兼容路径。
- AI 生成 PPT 使用 `TeachingRepresentation / SlideDeckSpec` 注册表与现有 V6 工作台；上传 PPTX 的审阅修订属于 `TeacherLessonAuthoringRepository` 的文字投影，不冒充完整视觉编辑器。
- 上传 PPTX 既可作为结构化教案的可追溯生成证据，也可在 PPT 阶段直接生成审阅修订；两条路径均不得静默覆盖当前教案、讲义或原始 PPTX。
- 教学日历是课程级排课领域对象，不复制课程正文。
- 文件空间按 `course_id + owner_id` 绑定课程。老师上传的原件只保存一份，上传不等于正式；系统生成的结果在非空、结构可用且来源未过期时直接成为当前工作修订。空结果、结构错误、来源过期和任务失败保持阻断；仍被当前或历史文件引用的原件不能直接删除。
