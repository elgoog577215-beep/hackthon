## 1. 架构扫描与规格

- [x] 1.1 读取北极星文档、真实链路文档、事件账本、学习者状态、学习者上下文、助手上下文、课程服务、导师服务和相关 routers/tests。
- [x] 1.2 判断哪些链路已经进入新架构，哪些仍是旧电路或重复上下文拼装。
- [x] 1.3 创建并校验 OpenSpec change。

## 2. 教学决策层

- [x] 2.1 新增 `TeachingDecision` 结构和规则聚合模块。
- [x] 2.2 从 `LearnerState` 生成学习阶段、复习、推进、简化、补例子、追问、练习和风险相关决策。
- [x] 2.3 提供可供 prompt 使用的简洁决策摘要。

## 3. AI Learning Context 编排层

- [x] 3.1 新增统一上下文对象，组合课程、节点、`LearnerContext`、`LearnerState` 和 `TeachingDecision`。
- [x] 3.2 提供结构化输出和 prompt 摘要输出。
- [x] 3.3 将 `assistant_context` 改为消费统一上下文，而不是继续膨胀上下文判断。

## 4. 接入 AI 助手、课件生成和导师建议

- [x] 4.1 AI 助手 prompt 包含状态摘要和教学决策摘要，旧问答接口兼容。
- [x] 4.2 `CourseService` 的正文生成、block 重写、节点重写、扩展和摘要轻量接入统一上下文。
- [x] 4.3 导师建议优先读取新状态/决策，同时保留旧导师记忆作为兼容补充。

## 5. 测试、文档与收束

- [x] 5.1 增加教学决策和统一上下文测试。
- [x] 5.2 更新助手、课程服务或导师建议测试，验证旧接口兼容。
- [x] 5.3 更新架构文档，说明新主链路、职责边界和旧链路收敛状态。
- [x] 5.4 运行相关 pytest、编译检查、OpenSpec 校验和 `git diff --check`。
