# 已完成课程修改先审阅与应用，未完成内容定向补查

教师已明确选择：已完成、经过校验且不依赖未完成部分的修改可以先审阅、先应用；共同结构依赖继续等待。教师同时要求“重新检查未完成内容”必须真实复用已经完成的检查。

沿用 CourseEvolutionPlan、原候选生成、采用/撤销与 TaskManager；不另建正文、任务或应用接口。应用正式课程仍需教师明确选择和确认。本变更不触发任何现有生产课程的自动应用。
