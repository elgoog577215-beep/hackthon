## 1. 建立状态聚合模型

- [x] 1.1 新增 `LearnerState` 数据结构和聚合模块。
- [x] 1.2 从 `LearningEvent` 计算近期活动、节点统计、答题结果摘要、薄弱点候选和风险提示。
- [x] 1.3 支持按课程和节点过滤状态。

## 2. 接入 AI 助手上下文

- [x] 2.1 为状态模型提供可供 prompt 使用的简洁摘要。
- [x] 2.2 在 `assistant_context` 中追加学习者状态摘要，不替代原有 `LearnerContext`。

## 3. 验证与收束

- [x] 3.1 增加状态聚合测试，验证事实证据与系统推断。
- [x] 3.2 增加助手 prompt 测试，验证状态摘要进入上下文且旧接口兼容。
- [x] 3.3 运行相关 pytest、编译检查、OpenSpec 校验和 `git diff --check`。
