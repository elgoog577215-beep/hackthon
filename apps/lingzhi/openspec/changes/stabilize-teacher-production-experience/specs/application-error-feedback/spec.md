## ADDED Requirements

### Requirement: 生产失败必须携带稳定定位

教案、讲义、大纲和 PPT 的生产失败 MUST 携带阶段、讲次、任务和可选教学块身份，恢复动作 MUST 与受影响对象就近呈现。失败信息 MUST 按职责分层：顶部只显示课程级失败数量，正文对象旁显示局部失败状态和恢复动作，右栏显示完整失败原因与恢复说明；同一条具体错误 MUST NOT 在三处重复。

#### Scenario: 用户从异常入口导航
- **WHEN** 用户点击“处理问题”进入失败对象
- **THEN** 页面 MUST 只完成定位、突出和错误展开
- **AND** MUST NOT 自动重试或修改任务

#### Scenario: 深链打开失败对象
- **WHEN** 用户从课程级异常入口进入一个生成失败的讲次
- **THEN** 顶部 MUST 只显示课程级失败数量，不复述具体错误
- **AND** 正文 MUST 显示该讲次的局部失败状态及原位恢复动作
- **AND** 右栏 MUST 完整显示具体失败原因与恢复说明

### Requirement: 资产主状态必须使用统一四态

资产的主显示状态 MUST 只使用“未生成”“生成中”“可使用”“生成失败”。暂停、过期、待核对和最近失败 MUST 作为辅助说明；课程级状态 MUST 继续只使用“备课中”和“备课完成”。

#### Scenario: 暂停中的首次生成
- **WHEN** 资产没有可用版本且生成任务已暂停
- **THEN** 资产主状态 MUST 保持“生成中”
- **AND** 辅助状态 MUST 显示“已暂停”及继续操作

### Requirement: 失败分类必须决定恢复方式

错误 MUST 区分服务暂时不可用、结构校验失败、缺少教师输入和来源版本冲突，并保留安全的具体详情。暂停不得冒充失败，非阻断审阅建议不得冒充任务硬失败。

#### Scenario: 来源冲突
- **WHEN** 失败来自上游修订变化
- **THEN** 系统 MUST 指向按最新来源生成或重新分析，不得只提示刷新重试

### Requirement: 讲义语言建议与生成阻断必须分离

讲义质量合同 MUST 使用 `teacher_script_quality_v10`，教案质量合同 MUST 使用 `teacher_lesson_plan_quality_v2`。篇幅与课时比例、措辞、教学动作关键词、辅助字段和局部复述 MUST 作为建议，MUST NOT 单独阻断交付。必要正文缺失、结构与来源错位、占位内容、成段复读及损坏的代码或公式结构 MUST 继续阻断。建议优化失败 MUST 保留可用结果并开放下一步。

#### Scenario: 已知旧质量报告可以重新计算
- **WHEN** 读取当前生成管线的 v8 或 v9 讲义报告
- **THEN** 系统 MUST 按新分级确定性重算 v10 报告，MUST NOT 为规则变化要求再次调用模型
- **AND** 未知合同版本 MUST 保持来源或质量待核对，不能假定兼容
