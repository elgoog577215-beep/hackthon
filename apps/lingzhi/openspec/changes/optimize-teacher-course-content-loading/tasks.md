## 1. 固定性能合同与证据

- [ ] 1.1 记录大纲正文可读、当前讲教案可读、当前讲讲义可读和编辑可用时间，保存请求图、样本量、P50/P95、失败数及运行版本
- [ ] 1.2 为授权、锁等待、磁盘读取、解析、源选择、审阅、投影和序列化增加低基数分段耗时，不记录正文、材料和身份敏感值
- [ ] 1.3 建立小课程、六讲课程、大课程、历史版本多课程及运行/暂停/失败任务的隔离性能样本

## 2. 大纲直接加载

- [x] 2.1 用测试证明大纲入口不等待整课正式讲义预览，大纲父子组件共享一个在途请求且迟到响应不串课
- [x] 2.2 改造蓝图读取，只读取原始课程和当前可用大纲工作区，不触发教师讲义全文投影
- [x] 2.3 在原 `courseWorkspace` Store 实现按课程的请求去重、成功快照和修订身份，`CourseOutlineReview` 复用已加载结果
- [x] 2.4 调整 `CourseWorkspaceView` 调度：大纲为首个正文，完整正式课程与分讲数据转为非阻断读取
- [ ] 2.5 验证已提交、未发布、失败新尝试保留 last-good、直接链接、切课与权限失败场景

## 3. 大纲投影与审阅复用

- [ ] 3.1 定义大纲响应 schema、来源修订、草稿修订和审阅规则版本
- [ ] 3.2 以完整内容身份复用大纲构建和确定性审阅，合并同一资源并发计算
- [ ] 3.3 验证保存、AI 候选采用/拒绝、撤销、恢复、规则升级与多进程写入都会正确失效
- [ ] 3.4 增加私有条件读取并验证 304 不被解释为空正文或错误

## 4. 教案与讲义按讲加载

- [ ] 4.1 固定摘要和指定讲次响应合同，证明字段未加载不等于内容缺失或获得生成权限
- [ ] 4.2 实现服务端摘要与 `lesson_unit_id + content` 读取，兼容无参数完整模式
- [ ] 4.3 在原 Store 合并摘要和讲次正文，按身份/课程/讲次/种类/修订去重并拒绝迟到结果
- [ ] 4.4 当前讲加载完成后最多预取一个相邻讲次；弱网、切讲、编辑中和任务活动时停止预取
- [ ] 4.5 验证活动流式正文、暂停恢复、失败部分结果、完成到正式修订交接及 last-good

## 5. 文件、列表和后台同步

- [ ] 5.1 课程文件以当前目录清单解除初始化，日历、题库、试卷和配套文档按视图加载且局部失败不阻断文件浏览
- [ ] 5.2 复用包身份减少列包后重复取包，保留历史绑定和创建兼容边界
- [ ] 5.3 任务事件只对账受影响课程，分讲响应自带生产状态时不再读取整课详情
- [ ] 5.4 验证断线重连、其他标签页修改、隐藏/恢复、空闲降频和活动任务短轮询

## 6. 缓存、渲染和交付

- [ ] 6.1 为教师仓储及派生投影增加总内存、单项大小和 LRU 限制，验证原子替换、删除再建、跨进程写入和规则升级
- [ ] 6.2 用浏览器 profile 决定重组件延迟加载和长正文分段渲染，保留搜索、锚点、选区 AI、公式和打印
- [ ] 6.3 运行后端、兼容、Ruff、前端、构建和 OpenSpec strict validation；依赖审计只报告不自动修复
- [ ] 6.4 按大纲、按讲、文件与缓存三批发布，执行任务保护、生产健康和代表课程只读冒烟，并记录准确 SHA 与回退证据
- [ ] 6.5 达到正文可读 P50/P95 预算；未达到时报告剩余函数级瓶颈并继续优化，不用外壳时间宣称完成
