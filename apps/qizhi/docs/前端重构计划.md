# 前端重构计划（任务②，建议暑假执行）

> 会上定调：本周以修 bug/优化为主，**大重构留到暑假**，且任何改动**必须保证不产生 bug**。
> 本文给出可分批、低风险的重构清单，便于后续逐项推进，而不是一次性大改。

## 本次已顺手完成（零行为变更，已构建验证）

- 抽出可复用 `components/CollapsibleSection.vue`，大纲/教案详情页复用，减少重复结构。
- 删除 `PlanDetailView.vue` 中因资源前置而孤立的 scoped CSS（`.count-pill`、`.section-actions`）。

## 一、超大组件拆分（优先级最高）

这些「上帝组件」是维护成本的主要来源，建议每个拆成「视图壳 + 子组件 + composable」：

| 文件 | 行数 | 拆分建议 |
|---|---|---|
| `views/ResourceAnalysisReportView.vue` | ~4500 | 按报告维度拆子组件（各 DimensionSection），数据处理抽 `composables/useAnalysisReport.ts` |
| `views/ResourceView.vue` | ~3600 | 编辑器、AI 侧栏、自动保存逻辑分别抽离 |
| `components/outline-form/OutlineFormPanel.vue` | ~2980 | 表单分步骤子组件 |
| `views/VideoAnalysisReportNewView.vue` | ~2980 | 图表（ECharts）配置抽 `lib/`，与 `ResourceAnalysisReportView` 合并公共逻辑 |
| `views/HomeView.vue` | ~2130 | 区块化 |

> 单个组件控制在 ~500 行内为宜；script 里 ref/computed/函数超过 ~30 个就该拆。

## 二、重复代码合并

- `OutlineDetailView` 与 `PlanDetailView` 的 `.version-list / .version-row / .version-badge / .row-btn`
  等样式与「版本行」结构高度重复 → 抽 `components/VersionRow.vue` + 共享样式。
- `ResourceAnalysisReportView` 与 `VideoAnalysisReportNewView` 报告渲染逻辑疑似重叠，确认后合并。

## 三、清理项

- **console 调试日志**：现有 34 处 `console.log`（其中 12 处跨行）。
  建议加 ESLint 规则 `no-console: ["warn", { allow: ["error", "warn"] }]`，
  保留错误日志、清掉调试日志；逐文件处理，避免机械批量误删跨行语句。
- **疑似历史版本命名**：`VideoAnalysisReportNewView` 的 `New` 后缀（当前仍在路由中、是主报告页，
  **不可直接删**）；重构合并后再统一命名。
- **死视图审计**：用「路由引用 vs views 目录」对比，确认 `AboutView` 等脚手架残留是否可删。

## 四、执行纪律（保证不出 bug）

1. 每拆一个组件单独提交、单独自测，**不跨页面一次性大改**。
2. 拆分只搬运、不改逻辑；先 `npm run type-check` + `npm run build` 通过，再人工点测关键链路：
   新建大纲 → 教案 → PPT → 编辑 → 返回；上传视频 → 分析 → 查看报告。
3. 在 dev 分支预览确认 UI 无误后，再由负责人 merge 到 main。
