# 灵知项目规则

```text
项目 AGENTS.md
├── 一、文件体系
│   ├── 项目文档
│   └── 操作手册与任务合同
├── 二、更新机制
│   ├── 项目变化
│   ├── 事实记录
│   └── 规则沉淀
└── 三、项目规则
    ├── 项目边界
    ├── 执行规则
    ├── 验收规则
    ├── 前端与国际化规则
    ├── 工程护栏
    └── 未分类规则（始终放在最后）
```

## 一、文件体系

### 1.1 项目文档

1. `README.md`：项目简介、环境、启动方式和基本协作入口。

2. `docs/产品蓝图.md`：产品定义、核心闭环、模块设计和长期边界。

3. `docs/产品状态.md`：当前阶段、模块进度、主要缺口和下一步。

4. `docs/系统架构.md`：代码结构、数据真源、运行链和技术边界。当前文件名仍为“系统架构”，重命名时必须同步修复全仓引用。

5. `AGENTS.md`：本项目长期协作规则与经过验证的项目经验。

`docs/归档/` 保存已经退役的决策、旧规则、演示材料和历史验收证据，只用于追溯，不是当前施工入口。

### 1.2 操作手册与任务合同

1. `openspec/specs/`：已经稳定的功能合同、状态、字段和验收场景。

2. `openspec/changes/`：正在实施的复杂变更、设计、任务和验收合同。

3. `runner/README.md`：独立运行器的使用入口。

稳定、可重复的安装、发布、打包或排障方法优先写入 README、脚本或 CI。当前没有独立的 `docs/操作手册/`，不为统一格式创建空目录。

## 二、更新机制

不同内容写入对应位置：

```text
产品设计和长期边界 → docs/产品蓝图.md
模块进度和当前缺口 → docs/产品状态.md
代码、数据和运行架构 → docs/系统架构.md
稳定功能合同 → openspec/specs/
复杂功能开发 → openspec/changes/
执行结果和用户反馈 → docs/事实.md
长期协作规则 → AGENTS.md
```

1. 更新项目：项目发生稳定变化后，写回对应项目文档或 OpenSpec，不建立“最新版”“终稿 2”等平行真源。

2. 记录事实：重要执行结果、用户明确反馈、真实模型或真实页面结果和高代价失败写入 `docs/事实.md`。每条事实记录时间、背景、动作、结果、反馈和证据，不复制产品文档已经承担的静态定义。

3. 提炼规则：用户明确提出长期要求，或者多个事实证明同一种方法稳定有效时，将能够改变后续行动的结论写入本文件。历史对话只提供候选证据，优先采用用户明确纠正、验收反馈和反复提出的要求；AI 当时的方案、自我总结和一次性偏好不能直接升级为规则。写入前还要与当前代码和正式文档核对。

4. 归类规则：新规则写入最合适的章节；暂时无法归类时放入“未分类规则”。同类规则积累成稳定主题后，再整理为新的分类。

5. 保持轻量：新规则优先合并或替换旧规则。可以由测试、CI、领域约束或正式 spec 保证的内容，不在本文件重复维护。修改文件体系、更新机制、核心产品边界或通用规则前，需要用户确认。

6. 写清规则：一条规则只讲一件事，先说在什么情况下做什么，再说明边界或验收方式。能用常用词说清楚时，不堆缩写、术语和多层名词；确实需要的代码标识保持原名。

## 三、项目规则

### 3.1 项目边界

1. 课程生产、教师备课、学生学习、学习事实和个性化成长属于同一产品闭环；局部功能必须放回完整闭环判断。

2. `CourseDocument + ordered CourseBlock[]` 是正式课程正文真源；生成工作区、Markdown、旧节点和兼容投影不能成为第二真源。

3. `LearningEvent` 与正式领域仓库保存学习事实；`LearnerModel` 是可重算解释，不拥有事实。

4. AI 对正式课程、学习事实和长期模型的修改必须经过白名单领域命令、用户确认、幂等回执和可恢复路径。

5. 使用 `task/job ID` 控制任务；`course_id` 只用于归属和分组。

6. 不建立平行 Store、平行课程正文、平行任务系统或只服务 UI 形状的接口。

### 3.2 执行规则

1. 文案、文档、局部样式和单点错误采用快速执行；核心课程链路、跨模块改造、数据模型、权限、API 或复杂 AI 流程使用 OpenSpec。

2. 已有 change 覆盖当前任务时继续使用，不建立平行方案。新能力先确认正式真源、状态所有者、权限、审核、失败、退出和恢复方式，再设计页面与 API。

3. 开始分析、排查或改造前，先确认当前仓库、分支或工作树、实际运行服务、页面路由和数据来源是否属于同一版本。旧截图、旧对话、服务器现场和其他工作树只能提供线索，不能覆盖当前事实。

4. 判断一项能力是否完整时，按用户实际路径检查入口、输入与资料来源、处理过程、结果展示、编辑与确认、失败与恢复，以及它和正式数据的关系。只有后端函数、只有页面外壳或只有一次成功演示，都不能算能力已经完成。

5. 修复问题时先找原因，再检查同一触发机制在本次范围内是否还有同类问题。不能只隐藏眼前现象，也不能把检查范围扩展到无关模块。

6. 教师备课入口和学生预习入口必须保持直接可用。教师 Agent 修改正式内容前，应提供文件范围、候选修改、影响预览、确认、回执和恢复路径。

7. 真实模型调用、浏览器页面、真机和生产环境是不同证据；离线测试通过不能代替真实链路结果。

8. Git 只暂存本任务文件，不带入本地数据、缓存、视频或生成产物；提交前检查范围与 `git diff --cached --check`。

9. 用户明确要求执行的任务在变更完成且验收通过后，默认提交本任务文件并推送当前分支，不再逐次询问。遇到远端冲突、权限失败、受保护分支或发布风险时停止并汇报，不强制推送。

### 3.3 验收规则

采用能够证明本次目标完成的最小充分验收：

1. Markdown、规则和小范围文案只检查内容差异、结构、链接和 `git diff --check`，不启动完整项目或截图。

2. 后端运行直接相关的测试；完整回归时分别执行 `pytest backend/tests` 与 `pytest tests`，不能混合收集。

3. OpenSpec 变更验证对应 change；只有多个 change 或正式 spec 受影响时才运行全量验证。完成的 change 验证通过后再归档并复验。

4. 用户可见变化检查中文、英文和受影响端的真实页面。桌面与移动端分别验收，不能用测试通过代替页面结果。

5. 课程与学习链路区分真实模型、浏览器视觉、真机和生产环境证据；未覆盖的边界必须明确汇报。

6. 汇报时分清已通过、未运行、已有失败、外部系统证据和仍需用户确认的部分。

7. 截图只能说明页面出现了什么，不能单独证明问题为什么发生。先用本轮截图定位现象，再检查页面结构、代码和真实数据来确认原因。只验证本次改动涉及的页面和状态；截图放在临时目录，除非用户明确要求，否则不提交 Git。

### 3.4 前端与国际化规则

1. 当前运行前端只启用中文和英文；用户可见文案必须同时维护中文和英文。

2. 组件通过当前 i18n 入口读取文案，不在组件、静态数组、fallback、SEO 或无障碍属性中写死中文。

3. 当前词典位于 `frontend/public/locales/zh/translation.json` 与 `frontend/public/locales/en/translation.json`。修改后必须解析 JSON，并检查异常字符、英文模式中文残留、原始 key、遮挡和布局破坏。

4. 先明确页面服务的对象、主要任务和操作顺序，再决定布局。内容和操作应放在真正负责该任务的页面中；复用组件不能模糊不同页面的用途，文案只说明事实和可选操作，不替用户做决定。

5. 当前课程界面是视觉与交互基线；保留已经成立的蓝紫、渐变和状态卡表达，只修复真实冲突、重复和失效规则。用户明确喜欢或要求保留的区域，不得顺手改动。

6. 核心流程需要连续操作时，使用能够承载完整任务的工作区；侧栏、抽屉和弹窗只承载补充信息、就近操作或短流程。内容很少或为空时，界面应随状态收拢，不保留巨大的多栏空壳。

7. 操作型页面打开后，用户应该很快知道自己正在处理什么、事情进展到哪里、接下来可以做什么。主要操作靠近它所影响的对象，次要信息降低强调。

8. 全局顶栏已经说明当前位置时，页面正文不再重复大标题和说明。集合页把搜索、状态筛选和视图切换放在内容上方，优先排成一行，不再用大栏目或重复统计占用首屏。

9. 优先用排版、留白和字号建立层级，不为每组内容增加卡片、边框或徽标。容器大小应与内容和任务相称；说明文字可以降低强调，但仍要清楚可读。

10. 界面必须显示对用户有用的真实状态：生成到哪里、哪些内容发生变化、影响范围是什么、失败原因是什么、下一步怎样继续。技术细节可以按需展开，不抢占主视线；看起来可以点击的按钮必须可用，暂不可用时要禁用并说明原因。

11. 桌面端与移动端作为两条独立的分析、修改和截图验收路径；移动端重点检查卡片嵌套、边框、间距和桌面层级机械堆叠。

### 3.5 工程护栏

1. 课程版本变化必须经过带来源修订、影响计划和幂等键的正式命令；普通滚动、可见块和本地恢复只能更新同版本位置。

2. 一级章节只负责导航和运行时刷新；只有具备正文与正式目标的二级节点可以产生学习事件。缺少正式学科包时仍可使用课程局部知识，但不得伪造学科 ID。

3. 模型无结果、结构非法、违反硬约束和质量不足必须明确区分；兜底只能做不改变业务语义的展示降级，不能伪装课程生成成功。

4. 生成预览只读取任务、检查点和安全草稿投影，不写学习进度、笔记、AI 修改或正式课程；发布后切换正式 `CourseDocument`。

5. 发布质量必须分别通过正文、难度、资料、知识、题目和一致性等必要子门，不能用总分平均掉题干答案矛盾或不可判定答案。

6. 会改变通过状态或问题代码的质量规则必须更新合同版本。旧报告只有在合同版本和规则摘要都相同时才能复用；确定性规则变化应直接重算，不能要求用户重新调用模型。

7. 同一能力的自动、手动、恢复和兼容入口必须调用同一领域函数与质量门；验收时覆盖这些真实入口，不能只测试其中一条路径。

8. 后台轮询、健康检查和自动恢复使用局部或静默错误状态；同一资源只有一个同步所有者，同一异常只处理一次。

9. 浏览器缓存只负责断网和刷新连续性，联网后按任务 ID 与服务端对账；断网、超时和未知错误保留最后成功现场，不能伪装后台仍在运行。

10. Markdown 与 LaTeX 修复必须尊重语法边界并建立真实失败样本；连续正则补丁产生回归时转向边界隔离或正式解析。

11. 浮层闪烁、裁切和遮挡同时检查选择器、`pointer-events`、祖先 `overflow`、堆叠上下文和视口边界，不只增加 `z-index`。

12. `index.html` 与 SPA 回退页重新验证或禁止缓存；带内容哈希的静态资源可以长期 `immutable`，发布时同时检查首页、子路由和哈希资源响应头。

### 3.6 未分类规则

本节保存暂时无法归类的稳定规则，始终放在第三章最后。当前暂无。
