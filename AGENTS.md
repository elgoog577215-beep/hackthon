# 灵知项目经验与规则

开始任务时，先读取 Codex 主目录中的通用 `AGENTS.md`（默认 `~/.codex/AGENTS.md`），再用本文件进入灵知项目的真实现场。通用文件提供共同的分析坐标、思考链路和工作原则；本文件只保存灵知的项目真源、学习与课程边界，以及经开发和教学现场验证的方法，两者需要一起使用。

```text
项目 AGENTS.md
├── 一、项目真源
│   ├── 项目文档
│   └── 操作手册与任务合同
├── 二、项目怎样保持更新
│   ├── 项目变化
│   ├── 事实记录
│   └── 规则沉淀
└── 三、项目判断与实践原则
    ├── 产品与数据边界
    ├── 项目设计元框架
    ├── 分级执行与验收
    ├── 前端开发范围
    ├── UI 设计经验
    ├── 工程护栏
    └── 未分类经验（始终放在最后）
```

## 一、项目真源

### 1.1 项目文档

1. `README.md`：项目简介、环境、启动方式和基本协作入口。

2. `docs/产品蓝图.md`：产品定义、核心闭环、模块设计和长期边界。

3. `docs/产品状态.md`：当前阶段、模块进度、主要缺口和下一步。

4. `docs/系统架构.md`：代码结构、数据真源、运行链和技术边界。当前文件名仍为“系统架构”，重命名时必须同步修复全仓引用。

5. `AGENTS.md`：本项目长期协作规则与经过验证的项目经验。

`docs/归档/` 保存已经退役的决策、旧规则、演示材料和历史验收证据，只用于追溯，不是当前施工入口。

### 1.2 操作手册与任务合同

1. `openspec/specs/`：已经稳定的功能合同、状态、字段和验收场景。

2. `openspec/changes/`：正在实施的复杂变更、设计、任务和验收合同。

3. `runner/README.md`：独立运行器的使用入口。

稳定、可重复的安装、发布、打包或排障方法优先写入 README、脚本或 CI。当前没有独立的 `docs/操作手册/`，不为统一格式创建空目录。

## 二、项目怎样保持更新

项目文档回答“灵知现在是什么样”，事实记录回答“这次真实发生了什么”，本文件回答“以后遇到同类情况怎样判断和行动”。三者前后相连，但不能互相代替：

```text
产品设计和长期边界 → docs/产品蓝图.md
模块进度和当前缺口 → docs/产品状态.md
代码、数据和运行架构 → docs/系统架构.md
稳定功能合同 → openspec/specs/
需要预先固定设计与完成标准的复杂变更 → openspec/changes/
执行结果和用户反馈 → docs/事实.md
长期经验与协作规则 → AGENTS.md
```

1. 项目发生稳定变化后，写回承担这项事实的项目文档或 OpenSpec，不建立“最新版”“终稿 2”等平行真源。

2. 记录事实：重要执行结果、用户明确反馈、真实模型或真实页面结果和高代价失败写入 `docs/事实.md`。每条事实记录时间、背景、动作、结果、反馈和证据，不复制产品文档已经承担的静态定义。

3. 一次结果先帮助修正当前行动；当用户明确提出长期要求，或多个事实说明同一种方法稳定有效时，再把会改变后续行动的部分写入本文件。历史对话只提供候选线索，写入前仍要与当前代码、正式文档和真实反馈核对。

4. 新内容先进入最合适的章节，暂时无法归类时放入最后的“未分类经验”。同类经验形成稳定主题后再整理分类；新原则优先修正、合并或替换旧原则。

能够由测试、CI、领域约束或正式 spec 稳定保证的内容，交给机器和项目真源，不在这里重复维护。一条经验只讲一个会改变行动的判断，用普通中文说清适用场景、做法和必要边界。

修改项目真源的职责、核心产品边界或通用规则前，需要用户确认；普通事实回写、经验归类与文字减重可以在任务结束时直接完成，并说明改了什么。

## 三、项目判断与实践原则

本章既有课程、学习数据和发布流程不能跨越的硬边界，也有帮助 AI 判断产品、教学和工程现场的方法。涉及正式正文、数据真源、权限、安全和不可逆结果的“必须”与“不得”需要严格执行；其余经验应先理解它要保护的教学目标和产品闭环，再结合本次任务裁剪使用，而不是逐条机械执行。现实证据与既有经验冲突时，先核验现场，再修正经验。

### 3.1 产品与数据边界

1. 课程生产、教师备课、学生学习、学习记录和个性化成长属于同一个产品闭环。修改局部功能时，要检查它会不会破坏整个闭环。

2. 教师备课和学生预习都是正式入口，必须能够直接使用，不能因为新增功能而被隐藏或绕远。

3. 一门课程的正式正文只有一份，由 `CourseDocument + ordered CourseBlock[]` 保存。生成过程、Markdown、旧数据和兼容页面都不能成为第二份正式正文。

4. 教师课程链中的讲义是教师可以直接用于授课的完整内容：既保存定义、边界、推理、例题、辨析、实践任务与总结，也要把它们组织成自然、连贯的教师讲述，不能退化成教材正文或课堂提纲。教案负责教学目标、教学类型、教学块、师生活动和时间安排，讲义依据当前、结构可用且来源未过期的教案写出实际怎样讲；旧正文能力只能选择性吸收，不能直接改名迁入。

5. 页面内容稿是当前、结构可用且来源未过期的讲义到最终 PPT 之间唯一的逐页内容合同，保存页数、页型、可见文案、版式、构图和讲义来源。页面内容稿保留自身独立的确认门；最终 PPT 只通过模板与渲染器编译已确认页面内容稿，不得在渲染阶段重新写内容或引入知识；内部继续使用 `ppt_manuscript_v1` 兼容标识。

6. 学习过程中真实发生的事情由 `LearningEvent` 和正式领域仓库保存。`LearnerModel` 只是根据这些事实生成的解释，可以重新计算，不能反过来充当原始事实。

7. 后台任务使用 `task/job ID` 控制暂停、恢复、删除和状态同步。`course_id` 只说明任务属于哪门课程，不能用来代替任务 ID。

8. 不为同一件事建立第二套 Store、第二份课程正文、第二套任务系统，或者只为方便某个页面而设计一套平行接口。

9. AI 修改正式课程、学习记录或长期学习模型前，必须使用系统允许的正式命令，并提供修改范围、候选结果、影响说明、用户确认、执行回执和恢复方法。

10. 教学编排的正式产品语言分为课程级与讲次级：`学习目的 × 一级学科类型 × 课程教学类型 → 整课大纲、讲次课型分布与必备教学环节`，再由 `本讲目标 × 本讲课型 × 学科规则 × 课程约束 → 教学块`。学习目的只分系统学习、项目实战、期末冲刺；学科类型只保留八个完整一级合同；课程教学类型只分理论课、实验课、实践课、研讨课、项目课、综合课；本讲课型只分七种课堂主弧。旧学科细分、教学原型、偏好、编排节奏和模块 ID 只作为内部条件、专业动作或兼容信息，不得重新成为页面分类、平行字段或 Prompt 自有规则；完整定义以 `docs/产品蓝图.md` 为准。

11. 新建教师课程的正式层级只有“课程 → 第 N 讲”。讲内主题、小节和教学块只负责组织一讲内的连续教案与讲义，不得产生独立正式资产、确认状态或生成任务；历史章/节只兼容读取，不反向进入新课程写入。

### 3.2 四层分析在灵知中的落点

通用 `AGENTS.md` 已说明领域定义、产品机制、交互设计和技术架构的共同关系；灵知只补充四层在本项目中的实际对象：

| 层级 | 灵知中的主要对象 | 正式真源 |
| --- | --- | --- |
| 领域定义 | 学习目的、学科类型、课程与讲次课型、教学块、大纲、教案、讲义、PPT 和学习证据 | `docs/产品蓝图.md` 的教学体系设计 |
| 产品机制 | 这些对象怎样形成整课结构、讲次安排、教学产物，并根据学习证据继续调整 | `docs/产品蓝图.md` 的产品逻辑设计 |
| 交互设计 | 教师和学习者怎样生成、审阅、确认、学习、修改与恢复 | `docs/产品蓝图.md` 的交互与界面设计 |
| 技术架构 | 标准库、课程真源、生成与质量工作流、任务恢复和 Agent 编排 | `docs/系统架构.md` |

四层不是四套平行设计，而是同一套教学逻辑从概念到运行的连续展开。改变上层定义时，应自然检查下游是否仍然成立；只改页面、Prompt 或数据中的一处，却没有承接同一产品判断，通常意味着改动还没有完成。具体怎样呈现和验收，由本次任务的复杂度决定，不为形式完整强制制作四层表。

### 3.3 分级执行与验收

#### 3.3.1 动手前确认现场与边界

1. 开始分析、排查或改造前，先确认仓库、分支或工作树、运行服务、页面路由和数据来源是不是同一个版本。

2. 多个 AI 对话并行工作时，准备操作具体课程前，先确认是否已有其他对话正在使用、修改或观察这门课程。发现已有对话占用后，当前对话不得再控制该课程，不得启动生成、修改课程状态、暂停、取消、恢复或重试相关任务，也不得通过重启服务、替换进程或改写数据绕开；只允许进行不会影响现场的只读检查，并向用户说明冲突。只有用户明确授权接管或并行操作时才能继续。

3. 当前代码、正式文档、真实数据和运行结果的优先级最高。旧截图、旧对话、服务器旧现场和其他工作树只能提供线索，不能直接当作当前事实。

4. 判断一项功能是否完整时，要沿着真实用户路径检查：

   ```text
   从哪里进入
   → 使用哪些资料和输入
   → 系统怎样处理
   → 用户能否看见过程
   → 结果能否查看和修改
   → 怎样确认或发布
   → 失败后怎样重试和恢复
   → 最终写入哪份正式数据
   ```

   只有后端函数、只有页面外壳或只有一次成功演示，都不能算功能已经完成。

5. 修复问题时先找到原因，再检查同一种触发机制在本次范围内是否还有类似问题。不能只隐藏眼前现象，也不能借机修改无关模块。

6. 设计新能力前，先确认谁保存正式数据、谁管理状态、谁有权限修改、怎样审核、失败后怎样退出和恢复，然后再设计页面与 API。

#### 3.3.2 选择执行方式并完成交付

默认采用快速执行：目标、边界和实现方向已经清楚，任务能够连续完成时，简要规划后直接修改，不创建 OpenSpec。任务涉及课程主链、数据模型、权限、API、AI 或多个文件时，应检查相应风险，但不能仅凭这些标签改走 OpenSpec。

执行前仍需固定教学或产品机制、数据与权限关系、跨模块共同要求和长期完成标准，或者任务需要跨多轮、多人和多个 Agent 延续时，采用 OpenSpec 执行。已有 change 真正覆盖本次范围时继续使用，否则才新建。

```text
快速执行：简要规划 → 修改 → 针对性验收 → 汇报
OpenSpec：固定设计与任务 → 修改 → 针对性验收 → 验证 change → 完成后归档
```

执行中继续遵守以下项目边界：

1. Git 只暂存本次任务修改的文件，不带入本地数据、缓存、视频、录制文件或其他生成产物。提交前检查暂存范围，并运行 `git diff --cached --check`。

2. 用户明确要求执行的任务，在修改完成并通过必要验收后，默认提交并推送当前分支。遇到远端冲突、权限失败、受保护分支或发布风险时停止，不强行推送。

3. 灵知生产环境独立运行在拓途浙享服务器，对外入口固定为 `https://tuotuzju.com/lingzhi/`。代码、Python 环境、持久数据和 systemd 服务位于独立的 `/opt/lingzhi` 边界内；不得复用或改写拓途主站的业务数据、API 和运行目录。服务器地址、账号、密码和密钥只保存在本机私有配置或 GitHub Actions secrets，不进入 Git。

4. 推送 `main` 后由 `.github/workflows/deploy-lingzhi.yml` 构建、校验并发布到拓途服务器。发布必须使用独立的 `lingzhi` 系统用户和 `lingzhi.service`，通过 Caddy 把 `/lingzhi/*` 转发到回环地址 `127.0.0.1:7862`；发布失败时保留上一版本和持久数据，不得影响拓途主站、`/api` 与其他独立应用。

5. 灵知的课程生成、AI 老师、内容修改、评估和 PPT 文本规划永久使用私有服务器登记 `a800-qwen` 上的 `qwen3.8-27b`。不得把魔搭、DeepSeek 或其他外部模型配置成主路由或故障兜底；模型不可用时进入现有失败恢复。地址与凭据只保存在私有登记、本地环境或 GitHub Actions secrets。发布验收必须同时证明真实模型请求和生产进程内代表性文本角色使用了指定模型；赛事作品中的魔搭展示链接和独立图像生成不属于这条文本模型规则。

#### 3.3.3 根据实际影响验收

采用能够证明本次目标完成的最小充分验收，只选择本次真正涉及的项目：

1. Markdown、规则和小范围文案，只检查内容、结构、链接和 `git diff --check`，不启动完整项目，也不为了形式完整而截图。

2. 后端只运行与改动直接相关的测试。需要完整回归时，分别运行 `pytest backend/tests` 和 `pytest tests`，不要混在一起收集。

3. 使用 OpenSpec 时先验证对应 change；只有多个 change 或正式 spec 受影响时才运行全量验证。change 完成后先验证，再归档并复验。

4. 用户可见的页面变化，默认验收中文桌面端。修改文案时同步检查本次涉及的英文内容。只有明确提出移动端需求后，才验收移动端。

5. 单元测试、真实模型调用、浏览器页面和生产环境是不同证据。测试通过不能代替真实模型结果，浏览器截图也不能代替生产环境结果。

6. 截图只能证明页面出现了什么，不能单独证明问题为什么发生。先用截图找到现象，再检查页面结构、代码和真实数据。只检查本次改动涉及的页面和状态；截图放在临时目录，除非用户明确要求，否则不提交 Git。

7. 最终汇报要说清楚哪些已经通过、哪些没有运行、哪些是原有失败、哪些依赖外部系统、哪些仍需要用户确认。

8. 说明同一对象的多项能力、问题或状态时，先讲它们之间的主线、因果或用户操作顺序，再按任务或阶段分组补充关键细节。不平铺大量同级要点，不把功能清单当成结论；超过五个并列项时，优先收敛为小流程、对照表或层级关系。默认顺序为“结论 → 关系与原因 → 当前证据 → 下一步”，只保留帮助用户理解和判断的内容。

### 3.4 前端开发范围

1. 灵知当前只建设桌面端。没有明确的移动端需求时，不分析、不设计、不适配、不测试，也不截图验收移动端。

2. 现有中文和英文继续保留。修改一段用户可见文案时，同步维护对应英文，但不把全站国际化改造当作默认任务，也不扩展其他语言。

3. 新增文案继续通过现有 i18n 入口读取，不直接写死在组件、默认值或无障碍标签里。修改词典后解析 JSON，并检查本次改动有没有原始 key、异常字符或明显溢出。

### 3.5 UI 设计经验

1. 统一设计语言首先统一语义。同一种对象、动作和状态应保持一致的名称、外观、位置和行为；不同页面可以按照真实任务采用不同布局，不强行套用同一种页面外壳。

2. 先确定页面服务谁、主要处理什么对象、用户要完成什么任务以及操作顺序，再决定内容和操作放在哪里。一个页面只突出一个主任务，主要操作靠近它所影响的对象。

3. 界面应通过导航、位置、层级、对象名称、真实状态和操作关系，让用户自然知道自己在哪里、正在处理什么、能够做什么。全局顶栏已经说明位置时，正文不重复大标题和介绍；产品界面不得出现 AI 思考过程、实现说明、需求原话和开发约束。

4. 操作型页面遵循“高密度、强分类、少打扰、正文优先”。搜索、筛选、整体状态和视图切换优先组成紧凑工具栏，主体内容尽早进入首屏；整体状态放在顶部，局部异常放在对应对象旁边，只展示能够帮助用户立即判断的数字。

5. 信息靠排版、留白、字号和位置建立层级；容器只用于成组信息、独立对象、独立状态或可操作区域。需要比较的内容优先使用列表、表格和状态行，不把普通标题、单行文字、字段和数字分别装进卡片；内容很少或为空时，页面随状态收拢。

6. 与具体对象有关的操作优先放在对象旁边并保持原有上下文。连续、复杂的主任务使用完整工作区；侧栏、抽屉和弹窗只承载补充信息、就近操作或短流程，关闭后应回到原来的对象和位置。

7. 所有执行操作的元素都必须同时具备正确的控件语义和清楚的视觉边界。独立按钮不得表现成普通文字；导航和帮助使用链接，纯状态不绑定点击操作。既不能留下“看起来能点但不能用”的元素，也不能让“实际能点”的元素看起来不可操作。

8. 按钮按照作用统一分级：主操作使用品牌色实心按钮，次操作使用边框或浅底按钮，危险操作使用危险色按钮，图标操作使用固定点击区域；菜单项、标签页和分段控件可以由父容器提供边界，但必须有清楚的悬停、选中和焦点状态。一个操作区域原则上只突出一个主按钮。

9. 可交互控件必须具备默认、悬停、按下、键盘焦点、加载、禁用和失败状态。暂时不可用时明确禁用并就近说明原因；动画只用于反馈状态变化，并尊重 `prefers-reduced-motion`。

10. 页面显示对用户有用的真实状态，包括处理进度、修改内容、影响范围、失败原因和恢复方法。长时间 AI 任务不能因为关闭弹窗、切换局部视图或刷新页面而丢失，技术细节可以按需展开，但不抢占主要视线。

11. 新增或精修常见控件前，先检索并复用现有共享组件和设计变量。同类控件的尺寸、间距、圆角、颜色、状态、键盘行为和动效由共享实现统一管理；业务页面只传入内容和状态，不复制模板或另写一套私有 CSS。

12. 当前课程界面是视觉和交互基线。已经成熟的蓝紫色、渐变、状态表达、密度和节奏，以及用户明确喜欢或要求保留的区域，不得顺手重做；新增设计应扩展现有体系，不引入第二套设计系统。

13. 在已有成熟界面上增加功能时，先复用现有组件、变量和交互状态，不用所谓“科技感”或“AI 感”替代真实的信息层级。未经用户确认或没有明确语义时，不得新增霓虹光效、装饰性渐变、玻璃拟态、大面积高纯度底色、粗色条、多层描边、过重阴影和过大圆角；选中与焦点状态只保留必要的辨识和可访问性反馈。

14. 设计页面时先把导航、内容和操作三个区域安排清楚：导航只负责定位和切换，内容只承载当前任务，操作固定在它所影响的对象附近。保存状态、处理进度和错误反馈嵌入这三个区域，不为少量状态或按钮另加横幅、卡片或漂浮操作条。用户能够通过位置、内容和控件直接看懂区域用途时，不再显示“教学结构”“生成依据”等解释性标题；只有消除歧义、说明风险、解释禁用原因或提供失败恢复时才增加文字。

15. 教师课程的 AI 审阅入口必须保留，不得因全课联动或修改流程复杂而隐藏。界面统一为“提出要求 → 查看影响 → 审阅候选 → 应用或撤销”，底层继续使用正式候选、回执和恢复能力。

16. 教师端核心工作区不得用成排的灰色小字承载统计、重复标签或解释。不能帮助教师立即判断或操作的信息直接删除；必须保留的正文、状态和常用操作使用正常阅读层级，字号原则上不低于 15px。讲次标题必须具有清楚的标题尺度和稳定位置；紧随其后的状态与操作保持轻量，按钮精致但不装饰过度；正文优先照顾年长教师阅读，使用充足字号、行高、对比度和自然换行，不用截断、省略或密集多栏换取表面整齐。页面以极简、干净、现代为基线，不用小字、徽标或装饰图标把已经删除的信息重新塞回来。

17. 课程工作区顶栏的“备课工作台 / 课程文件”是紧凑型同级切换控件的视觉标准。设计同类分段控件时，外层只负责分组并提供安定边界，选中项使用尺度更小的白底和轻阴影形成前后层级；控件宽度随内容收拢，内外圆角随高度递减，不把整段做成长胶囊，也不用大圆角和大块留白制造视觉重量。复用的是层级、密度和比例，不是把一组固定像素套到不同角色、尺寸的控件上；普通按钮、标签页和导航仍按各自语义处理。

### 3.6 工程护栏

#### 3.6.1 课程和学习数据

1. 正式修改课程版本时，要记录修改来源、影响范围和防止重复执行的幂等键。普通滚动、阅读位置和本地恢复只能更新当前位置，不能产生新的课程版本。

2. 一级章节只负责导航和刷新。只有具备正式正文和学习目标的二级内容才能产生学习事件。缺少正式学科包时可以使用当前课程知识，但不能伪造学科 ID。

3. 生成预览只能读取任务、检查点和安全草稿，不能写入学习进度、笔记、AI 修改或正式课程。发布成功后才能切换到正式 `CourseDocument`。

#### 3.6.2 生成和质量

1. 模型没有返回结果、返回结构错误、违反硬性要求和内容质量不足，是四类不同问题，必须分别处理。

2. 降级方案只能解决展示问题，不能把不合格或不完整的课程伪装成生成成功。

3. 发布质量要分别检查正文、难度、资料、知识、题目和一致性。不能用一个平均总分掩盖答案矛盾、答案不可判断等严重问题。

4. 质量规则如果会改变通过状态或问题代码，就必须更新合同版本。旧报告只有在合同版本和规则摘要相同时才能复用；能够直接重新计算的质量变化，不能要求用户再次调用模型。

5. 同一能力的自动、手动、恢复和兼容入口，必须调用同一个底层函数和质量检查。验收时不能只测试其中一条路径。

#### 3.6.3 任务和恢复

1. 后台轮询、健康检查和自动恢复出现问题时，使用局部或静默错误状态，不要让后台异常反复打断用户。

2. 同一资源只能有一个同步负责人，同一个异常只能处理一次，避免多个组件同时恢复、重复提示或互相覆盖状态。

3. 浏览器缓存只保存最后一次成功现场，用于断网和刷新恢复。重新联网后，要按照任务 ID 与服务端对账，不能假装后台任务仍在运行。

4. 用户已经启动且仍在运行的生成任务属于用户现场。除非用户明确授权，任何 AI 对话都不得暂停、取消、重启、替换、标记失败、改写任务状态或数据，也不得另起同目标任务影响它；诊断默认只读，并保留原任务继续运行。

#### 3.6.4 专项排障

1. 修复 Markdown 和 LaTeX 时，要尊重各自的语法边界，并保留真实失败样本。连续增加正则仍然产生回归时，应改用正式解析或先隔离语法区域。

2. 浮层出现闪烁、裁切或遮挡时，同时检查选择器、`pointer-events`、祖先 `overflow`、堆叠上下文和视口边界，不要只增加 `z-index`。

3. `index.html` 和 SPA 回退页应禁止缓存或在发布后重新验证。带内容哈希的静态资源可以长期缓存；发布时同时检查首页、子路由和静态资源的响应头。

### 3.7 未分类经验

暂时无法确定位置，但已经证明长期有效的经验放在这里。形成稳定主题后，再迁入对应分类。当前暂无。
