# 灵知 AI 协作规则

> 本文件是灵知项目的 AI 协作操作手册，也为人类协作者提供可读的规则总览。
>
> 核心循环：**回复 -> 用户确认 -> 执行与验收 -> 迭代回写 -> 下一轮回复**。
>
> 当前事实优先级：代码、运行结果、数据库 schema、正式文档和 OpenSpec 高于记忆、对话和历史材料。

## 目录

- [0、总规则](#0总规则)
- [一、文档系统](#一文档系统)
- [二、回复：五步回复法](#二回复五步回复法)
- [三、执行与验收](#三执行与验收)

## 0、总规则

### 0.1 阶段总览

| 阶段 | 进入条件                                                           | 主要动作                                         | 结束条件                           |
| ---- | ------------------------------------------------------------------ | ------------------------------------------------ | ---------------------------------- |
| 回复 | 用户提出问题、想法或方案，尚未确认执行                             | 定位项目、解读意图、提升认知、判断问题、提出方案 | 方案清楚，等待用户确认             |
| 执行 | 用户明确说“执行、开始、直接做、继续执行、修复、推送”等，且对象清楚 | 读取现状、按方案修改、按风险验收、处理落差       | 目标达到并完成必要验收，或明确阻塞 |
| 迭代 | 执行结果产生稳定结论，或用户提出新的长期要求                       | 更新正确文档、沉淀经验、必要时升级 `AGENTS.md`   | 真源已回写，下一轮可从稳定状态继续 |

```text
回复 -> 用户确认 -> 执行与验收 -> 迭代回写 -> 下一轮回复
```

用户说“好、继续、下一步”时，只延续上一轮已经明确的阶段，不静默扩大到发布、删除、覆盖或其他高风险动作。

### 0.2 事实、范围与风险

| 规则 | 要求                                                                                                                   |
| ---- | ---------------------------------------------------------------------------------------------------------------------- |
| 语言 | 默认中文；路径、命令、包名、模型 ID、接口字段和代码标识保留英文                                                        |
| 事实 | 代码、运行结果、数据库 schema、正式文档和 OpenSpec 高于记忆、对话和历史材料                                            |
| 资料 | 外部事实必须实际核验，并区分事实、推断和假设；未查资料时不伪造来源                                                     |
| 范围 | 读取当前对象后再判断；理解意图不能扩大用户授权，不创建平行业务真源                                                     |
| 改动 | 聚焦本任务，不回滚或覆盖用户及其他任务的改动                                                                           |
| 风险 | 低风险细节可直接推进；涉及生产、权限、用户资产、数据库、密钥、账号或不可逆结果时，先说明影响、恢复方式，必要时停下确认 |

### 0.3 输出与隐私

| 领域 | 规则                                                                                        |
| ---- | ------------------------------------------------------------------------------------------- |
| 结构 | 复杂回复使用最高级标题和中文编号；简单问题可以压缩，不机械凑齐固定格式                      |
| 表达 | 根据关系选择表格、流程图、编号、对照结构或段落，不机械平铺                                  |
| 推理 | 不暴露完整思维过程，只呈现影响结论的判断、依据、不确定性、方向和结果                        |
| i18n | 用户可见前端文案通过现有 i18n 同时维护中文和英文                                            |
| 隐私 | 数据库、WAL、备份、上传目录、密钥、用户数据、商业文书、会话记录和本地 memory 不进入公开 Git |

## 一、文档系统

文档系统只有三类内容：**项目全景、任务执行、事实与经验**。迭代不是第四类文档，而是读取、验证和更新这三类内容的运行方式。

### 1.1 项目全景

项目全景回答“项目是什么、现在在哪里、技术如何运行”。相关文件职责如下：

| 文件               | 职责                                                 |
| ------------------ | ---------------------------------------------------- |
| `README.md`        | 人类入口：项目简介、安装、启动、测试和基本协作入口   |
| `docs/产品蓝图.md` | 产品设计：服务谁、核心闭环、长期模块和产品边界       |
| `docs/产品状态.md` | 当前状态：阶段、模块位置、成熟度、缺口、风险和下一步 |
| `docs/系统架构.md` | 技术设计：代码结构、数据真源、运行链、部署和技术边界 |

这些文件在分类上属于项目全景，但不合并成一个大文件；每个文件只维护自己的职责和更新节奏。

本项目的产品、工程和学习领域边界见“灵知项目专属规则”；不把其他项目的身份、权限或索引规则复制进来。

### 1.2 任务执行

任务执行文档回答“当前具体做什么、按什么合同做、如何确认完成”：

| 载体                               | 作用                                   |
| ---------------------------------- | -------------------------------------- |
| `openspec/changes/<change>/`       | 正在实施的变更、设计、任务和验收合同   |
| `openspec/specs/`、正式 spec       | 稳定功能合同、字段、状态和接口         |
| `docs/指南/`                       | 可重复执行的发布、采集、打包和验收流程 |
| Git diff、测试、运行结果、用户验收 | 任务完成证据，不复制进度日志           |

具体字段、状态机、接口和验收场景以当前 OpenSpec 或正式 spec 为准；历史方案不能覆盖当前合同。

### 1.3 事实与经验

事实与经验直接放在 `docs/` 下：

```text
docs/事实.md
  成功事实
  失败事实

docs/经验.md
  有效经验
  警戒经验
```

这两个文件只保存在本地忽略目录，不进入公开仓库。

| 对象               | 记录内容                                              | 升级边界                                 |
| ------------------ | ----------------------------------------------------- | ---------------------------------------- |
| `docs/事实.md`     | 时间、原始输入、AI 实际动作、用户反馈、运行结果和证据 | 只记录发生过什么，不直接写长期规则       |
| `docs/经验.md`     | 从多个事实归纳的有效经验和警戒经验                    | 需要相似场景验证，不能凭一次结果升级     |
| `docs/开发禁区.md` | 已验证、跨任务有效的警戒经验正式版                    | 不是第三种经验层，也不是独立产品类别     |
| `docs/归档/`       | 已退役或不再作为当前施工入口的历史文件                | 可定向提取，但不整批搬迁、不覆盖当前真源 |

### 1.4 文档迭代与规则升级

重要执行结束后按以下路径回写：

```text
执行完成
-> 记录事实
-> 归纳候选经验
-> 多个相似场景验证
-> 回写项目全景、任务合同或稳定警戒规则
-> 下一轮回复从新真源继续
```

经验经过验证后按用途回写：

```text
稳定产品结论 -> docs/产品蓝图.md
当前阶段变化 -> docs/产品状态.md
稳定技术约束 -> docs/系统架构.md
高代价错误警戒 -> docs/开发禁区.md
稳定 AI 行为要求 -> AGENTS.md
专项操作经验 -> docs/指南/
具体功能合同 -> OpenSpec / 正式 spec
```

当用户提出新的稳定要求，或反思发现可复用的行为缺口时，升级 `AGENTS.md`：

1. 备份当前文件，备份不进入仓库。
2. 阅读当前规则、事实、经验、相关历史版本和真实案例。
3. 说明原规则要改变什么行为、当前哪里没有承载住。
4. 按“原则 -> 触发 -> 必须动作 -> 完成证据”重组规则。
5. 删除重复、冲突、过时项目事实和不必要的维护负担。
6. 用户确认后执行，或在用户已明确授权持续维护时直接执行。
7. 用复杂回复、局部方案、直接执行、持续项目和高风险动作回放新规则。
8. 检查 Markdown、引用、隐私边界、`git diff --check` 和实际行为。

## 二、回复：五步回复法

回复发生在执行之前。复杂分析、产品判断、架构判断、规则设计、方案比较和持续项目协作，必须让用户看见以下五个步骤；简单问题可以压缩，但不能省略影响结论的必要判断。

### 2.1 进入与结束

用户提出问题、想法、方向、方案或规则，但尚未确认执行时，进入回复阶段。回复阶段不修改代码和正式文档，不把讨论结果说成已执行。

回复结束条件：

```text
意图清楚
-> 问题明确
-> 方案明确
-> 等待用户确认
```

### 2.2 五步内容

| 步骤         | 必须回答                                                 | 输出重点                                                     |
| ------------ | -------------------------------------------------------- | ------------------------------------------------------------ |
| 一、项目全景 | 项目总结构、当前模块/小节、本轮对象、影响范围和下一位置  | 产品蓝图讲“是什么”；产品状态讲“到哪里”；系统架构讲“怎么运行” |
| 二、意图     | 用户表面说了什么，真正想推进、理解、选择、验证或解决什么 | 目标和本轮应得到的结论                                       |
| 三、认知     | 当前判断所需的框架、边界、变量、误区和可迁移标准         | 提升判断力，不做无关百科介绍                                 |
| 四、问题     | 当前项目真实缺口、证据、影响对象和未确认假设             | 把认知落回当前对象                                           |
| 五、方案     | 修改什么、为何取舍、预期结果和确认方式                   | 形成可执行、可验收的下一步                                   |

### 2.3 资料按需嵌入

资料不是固定的第六步。只有需要查最新新闻、数据、线上规则、竞品、论文、版本、平台信息或其他外部事实时才启动资料层，并嵌入相关步骤。

查过资料时说明来源、查到的关键事实、事实与推断的区别，以及资料怎样改变本轮判断。没有查外部资料时，不单独列资料。

### 2.4 回复前检查

| 检查项   | 完成标准                                     |
| -------- | -------------------------------------------- |
| 项目全景 | 已定位总结构和当前模块/小节                  |
| 意图     | 用户能看出真实目标，而不只是最后一句话       |
| 认知     | 提供了服务本轮的判断增量                     |
| 问题     | 描述当前真实状态，而不是重复表面说法         |
| 方案     | 说明修改动作、预期结果和确认方式             |
| 资料     | 如使用外部资料，来源、事实、推断和假设已分清 |

## 三、执行与验收

### 3.1 进入执行与执行前检查

| 触发场景                                                                 | 必须动作                                                                                    |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| 用户明确说“执行、开始、直接做、继续执行、修复、推送”等，且对象和范围清楚 | 进入执行阶段                                                                                |
| 目标、范围、对象、资料、路径或最低验收等级仍缺失                         | 先补齐缺口，不重复已确认方案                                                                |
| 涉及新能力                                                               | 先确认实体、正式真源、状态所有者、权限、审核、退出和恢复，再决定页面、组件和 API            |
| 涉及 UI 或多端                                                           | 先确认真实页面、稳定导航、用户任务和运行状态；Web、PWA、微信、Android、iOS 复用同一产品能力 |

### 3.2 执行过程

```text
读取现状
-> 按方案修改
-> 检查改动范围
-> 按风险验收
-> 对比目标
-> 处理落差
```

执行时必须使用当前真源、保持改动聚焦、不覆盖用户已有改动、不创建平行真源。执行结果改变原判断时，回到项目全景、当前问题或方案重新定位。

### 3.3 验收分级

验收以用户目标、风险、影响范围和返工成本为尺度。GPT-5.6 不得把所有任务自动升级为完整工程验收。

| 等级               | 适用范围                                                                          | 最低动作                                                                                    |
| ------------------ | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 一级：轻量验收     | 极小、低风险、结果直接可见且容易撤回的规则措辞、文案、单点整理                    | 检查文件状态、差异内容和基本语法，不额外启动全量测试、浏览器或多端检查                      |
| 二级：常规验收     | 大多数普通功能、局部 UI、单模块修复和文档变更                                     | 运行与改动直接相关的 lint、测试、构建、smoke、解析或页面检查                                |
| 三级：完整工程验收 | 多模块、高风险、核心流程、数据库迁移、架构/性能、跨端、复杂 AI 流程或正式合同变化 | 按需要运行完整测试、浏览器流程、多端验证、迁移回放、性能检查、OpenSpec 验证或真实环境 smoke |

可在确有稳定需求后增加专项验收模式，例如 UI 视觉、移动端、AI provider、数据迁移或生产环境验收；不因一次任务临时扩张全局规则。

汇报时区分：已通过、未运行、已有失败、外部系统证据和仍需用户验收的部分。不能运行的检查要说明原因，不能把推断说成完成。

### 3.4 收束与 Git

| 收束项     | 要求                                                                |
| ---------- | ------------------------------------------------------------------- |
| 目标未达到 | 下一轮必须产生新的判断、修改或验证，不机械重复                      |
| 目标达到   | 完成对应等级的必要验收，说明改动、原因、证据、剩余边界和下一步      |
| 明确阻塞   | 说明权限、安全、外部依赖或信息阻塞，不把推断说成完成                |
| Git 范围   | 只暂存本任务文件，不使用 `git add -A`，不强推，不带入来源不明的提交 |
| Git 完成   | 提交前运行 `git diff --cached --check`，汇报提交和远端结果          |

## 四、灵知项目专属规则

### 4.1 项目真源与领域边界

| 对象     | 规则                                                                                                                  |
| -------- | --------------------------------------------------------------------------------------------------------------------- |
| 产品全景 | `docs/产品蓝图.md` 维护产品定义、核心闭环和长期边界；`docs/产品状态.md` 维护当前阶段、缺口和主攻位置。                |
| 技术全景 | 当前技术架构文件仍为 `docs/系统架构.md`；任何重命名必须同步修复全仓引用。                                             |
| 课程正文 | `CourseDocument + ordered CourseBlock[]` 是正式课程正文真源；生成工作区、Markdown、旧节点和兼容投影不能成为第二真源。 |
| 学习事实 | `LearningEvent` 与正式领域仓库保存事实；`LearnerModel` 是可重算解释，不拥有事实。                                     |
| AI 修改  | AI 对正式课程、学习事实和长期模型的修改必须经过白名单领域命令、用户确认、幂等回执和可恢复路径。                       |
| 任务控制 | 使用 `task/job ID` 控制任务；`course_id` 只用于归属和分组。                                                           |

### 4.2 前端与国际化

- 当前运行前端只启用中文和英文；用户可见文案必须同时维护中文和英文。
- 组件通过当前 i18n 入口读取文案，不在组件、静态数组、fallback、SEO 或无障碍属性中写死中文。
- 当前词典位于 `frontend/public/locales/zh/translation.json` 与 `frontend/public/locales/en/translation.json`。
- 修改 locale 后必须执行 `JSON.parse`，并检查中文问号乱码、英文模式中文残留、原始 key、遮挡和布局破坏。
- 当前课程界面是视觉与交互基线；保留已经成立的蓝紫、渐变和状态卡表达，只修复真实冲突、重复和失效规则。
- 不建立平行 Store、平行课程正文、平行任务系统或 UI 形状专用接口。

### 4.3 灵知专项验收

| 场景           | 必须补充的检查                                                                                      |
| -------------- | --------------------------------------------------------------------------------------------------- |
| 后端           | 相关测试按目录分别运行；完整回归时分别执行 `pytest backend/tests` 与 `pytest tests`，不能混合收集。 |
| OpenSpec       | 运行 `openspec validate --all`；完成的 change 验证通过后再归档并复验。                              |
| 前端           | 用户可见变化检查中文、英文、桌面和移动端真实页面；不能用测试通过替代真实页面验收。                  |
| 课程与学习链路 | 区分真实模型调用、浏览器视觉、真机和生产环境证据；未覆盖的边界必须明确汇报。                        |
