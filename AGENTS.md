# 灵知 AI 协作规则

> 第一读者：AI Agent。本文规定 AI 怎样理解、修改、验证和提交灵知项目。
>
> 人类开发者从 [README.md](./README.md) 进入项目；产品与工程事实由 `docs/` 中的中文文档承载。

## 0. 总则

### 0.1 协作目标

用户输入通常只是局部切口。处理任务时遵循：

```text
理解真实目标
-> 读取当前项目事实
-> 定位问题在产品和代码中的位置
-> 形成有边界的方案
-> 执行并验证真实结果
-> 更新受影响的当前真源
```

不要只完成字面任务，也不要把局部请求擅自扩大成未授权的产品、数据或发布变更。

### 0.2 语言

- 默认使用中文回复、写项目文档、任务、方案、风险和验收说明。
- 文件路径、命令、包名、模型 ID、接口字段、代码标识和行业惯用术语可以保留英文。
- 用户可见文案必须同时维护中文和英文，不在组件中只写死中文。
- 当前前端词典位于 `frontend/public/locales/zh/translation.json` 与 `frontend/public/locales/en/translation.json`。

### 0.3 事实优先级

任何记忆、旧文档或对话都不能覆盖当前代码、运行结果和正式规格。发生冲突时先核验，再更新对应真源。

## 1. 开始任务时读什么

### 1.1 默认入口

先确认真实仓库、分支和工作区，再按任务需要读取：

1. [README.md](./README.md)：项目启动、测试和人类协作入口。
2. [产品蓝图](./docs/产品蓝图.md)：产品定义、核心闭环、长期建设顺序和不可违反边界。
3. [产品状态](./docs/产品状态.md)：当前成熟度、活动 OpenSpec、缺口和下一步。
4. [系统架构](./docs/系统架构.md)：当前代码结构、领域真源、主链和接入边界。
5. [开发禁区](./docs/开发禁区.md)：已确认不能采用的产品和工程做法。
6. 当前任务对应的 `openspec/changes/<change>/` 或局部设计文件。

不要每轮机械读取全部文档。简单任务读取直接相关文件；跨模块、高影响或产品任务再补齐蓝图、状态和架构。

### 1.2 历史材料

- [项目决策记录](./docs/归档/项目决策记录.md)、`docs/研究/`、`docs/验收/` 和 `docs/归档/` 都是按需读取的历史或证据材料。
- 只有需要回答“过去为什么这样选择”或复现特定历史现场时才读取。
- 历史文件中的“当前”“下一步”“唯一依据”和旧路径不具备规范效力。
- 当前结论必须回到蓝图、状态、架构、开发禁区、正式 OpenSpec、代码和测试重新确认。

## 2. 消息回复阶段

### 2.1 五空间

每次回复前内部扫描：

- 意图空间：用户真正想推进、理解、验证或解决什么。
- 认知空间：当前判断需要哪些概念边界、关键变量和判断框架。
- 问题空间：这些维度在当前产品、文档、代码、数据或流程中对应什么真实缺口。
- 资料空间：是否需要核验最新外部事实、版本、竞品、论文、政策或平台规则。
- 方案空间：目标、路径、优先级、影响范围、验证方式和风险边界是什么。

复杂分析、产品判断、架构判断和规则设计，应显式呈现真正影响结论的关键空间。标题可使用“空间名：本轮关键判断”。不机械凑齐五个空间，也不以避免机械化为由隐藏关键判断。

简单事实、状态说明、命令结果和低风险执行直接回答结论、依据与边界。

### 2.2 项目全景

持续项目和多处影响任务需要确认：

- 整体目标和当前讨论位置。
- 当前对象依赖什么，会影响哪些对象。
- 哪些已经稳定、正在实施、等待验证或已经归档。
- 哪份当前文档或规格承载正式状态。

只展示与本轮有关的全景，不重复整份项目播报。稳定结论更新原有真源；未确认想法继续留在讨论或局部设计中。

### 2.3 意图和执行授权

- 低风险、可逆且可从上下文可靠推定的细节，说明关键假设后继续，不反复追问。
- 不同选择会显著改变产品方向、外部权限、用户资产或不可逆结果时，必须停下确认。
- 用户说“执行、开始、直接做、全自动、不要问、启动、拉取、推送、合并、扫描并修复”等，进入动作执行阶段。
- “好、继续、下一步”只延续已经明确的上下文，不能静默扩大到发布、删除、覆盖或其他高风险动作。

## 3. 动作执行阶段

### 3.1 执行前

- 读取相关代码、文档、正式规格和当前状态，不凭记忆下结论。
- 检查 `git status --short --branch`，区分本任务改动、用户改动、运行数据和生成产物。
- 涉及远端时先 `git fetch` 并确认分支关系；拉取使用可验证的安全方式，不覆盖脏工作区。
- 涉及数据库、部署、密钥、上传文件、批量删除、迁移或用户资产时，先确认范围、可逆性和恢复方法。
- 依赖最新外部信息时实际核验可靠来源，并区分事实、推断和假设。

### 3.2 选择执行载体

- 直接执行：明确、低风险、容易验证的小改动。
- 简短计划：普通功能、局部 UI、小型重构或明确单模块改动。
- 紧凑规格：多步骤、多处影响但不改变长期产品、数据或接口合同。
- OpenSpec：尚未被有效规格承载的高影响功能、核心流程、数据库迁移、架构或性能改造、高风险重构，以及正式输入输出契约变化。

不为只读分析、已有 OpenSpec 内继续实施、已确认设计下的局部修复或机械调整重复创建 OpenSpec。现有规格过期或与当前产品判断冲突时，先修订或替换它。

### 3.3 修改原则

- 保持改动聚焦，不顺手重构无关模块。
- 不回滚、覆盖或提交用户和其他任务的现有改动。
- 结构化数据使用正式解析器、Schema 和领域服务，不用脆弱字符串拼接代替。
- 新能力先确认实体、真源、状态所有者、触发、控制、退出和恢复，再决定页面、组件和 API。
- 同一业务能力的自动、手动、恢复和兼容入口共用领域函数、质量门和状态机。
- 如果实际问题超出原判断，先重新定位影响范围，不因已经开工而默认扩大任务。

### 3.4 前端和用户可见变化

- 修改标题、按钮、导航、空状态、错误、表单、SEO、无障碍、提示、筛选和默认数据文案时同步中英文。
- 组件通过当前 i18n 入口读取文案；完成后检查英文模式无中文残留、原始 key、遮挡和布局破坏。
- 从用户当前任务、触发、轻量出现方式、控制和返回路径出发，不先为功能创造新页面或平行入口。
- 当前课程界面是视觉与交互基线。保留已经成立的蓝紫、渐变和状态卡表达，只修复真实冲突、重复和失效规则。
- 不建立平行 Store、平行课程正文、平行任务系统或 UI 形状专用接口。

## 4. 验证

验证规模与风险匹配，但不能用描述代替真实检查：

- Bug：先复现，再修复，再复测并增加回归保护。
- 后端：运行相关测试；需要完整回归时分别执行 `pytest backend/tests` 与 `pytest tests`，当前两套目录不能混合收集。
- 前端：运行相关测试、类型检查或构建；用户可见变化还要验证中英文、桌面和移动端真实页面。
- OpenSpec：运行 `openspec validate --all`；完成的变更执行 `openspec archive <change>` 后再次验证。
- 网站启动：真实启动并验证 URL、健康端点和目标页面可访问。
- 数据和文档：确认文件可解析、链接有效、Markdown 可渲染、产物可打开。
- 部署和 Git：核对远端、健康状态、回滚边界和最终分支差异。

测试通过只证明对应检查通过，不自动等于真实模型、浏览器视觉、长期效果或生产环境已经验证。汇报必须说明未覆盖边界。

## 5. 灵知当前真源边界

- 产品定义、核心闭环、领域对象和产品禁区只由 `docs/产品蓝图.md` 维护。
- 当前成熟度、活动 OpenSpec、缺口、风险和主攻只由 `docs/产品状态.md` 维护。
- 当前代码模块、数据流、运行链、兼容和降级只由 `docs/系统架构.md` 维护。
- 跨任务仍有效的禁止做法只由 `docs/开发禁区.md` 维护，并尽量由测试、Schema、lint 或领域门禁执行。
- 具体功能的字段、状态机、接口、任务和验收场景进入活动 OpenSpec；完成后归档为正式规格。
- 研究、验收、演示和旧决策只提供证据或历史，不得覆盖当前真源。

课程与学习领域继续遵守：

- `CourseDocument + ordered CourseBlock[]` 是正式课程正文真源。
- 生成工作区、Markdown、旧节点和兼容投影不能成为第二课程真源。
- `LearningEvent` 与正式领域仓库保存事实；`LearnerModel` 是可重算解释，不拥有事实。
- AI 对正式课程、学习事实和长期模型的修改必须经过白名单领域命令、用户确认、幂等回执和可恢复路径。
- 任务控制使用 `task/job ID`，`course_id` 只用于归属和分组。

## 6. 文档维护

### 6.1 全局与局部

全局文档只保留当前有效真相。一次具体开发使用 OpenSpec 或必要的中文 `spike`；完成后：

```text
稳定产品结论 -> 产品蓝图
当前进度变化 -> 产品状态
稳定技术约束 -> 系统架构或正式 spec
不能重复的错误 -> 开发禁区 + 自动化测试
过程与时间线 -> Git / PR / 归档 OpenSpec
```

不要新建第二蓝图、第二状态板、综合交接长文、正向经验日志或按时间追加的错误日志。

### 6.2 更新规则

- 新结论推翻旧结论时修改当前真源原位置，不在文末追加冲突修正。
- 文档只承担一个主要问题；字段、进度、历史和任务不混写。
- 历史项目决策只在需要追溯时追加，并明确 `有效 / 已替代 / 历史`；不得写当前进度和下一步。
- 文档移动或归档后修复全仓引用，并验证 Markdown、OpenSpec 和 `git diff --check`。
- 修改本文件前先确认现有版本已进入 Git；若工作区中存在未提交版本，创建时间戳备份后再重组。

## 7. Git 收束

- 达到目标并完成必要验证后，默认提交并推送；用户明确要求本地保留、只提交或不推送时除外。
- 提交前再次检查 `git status --short --branch`，显式暂存本任务文件，不使用 `git add -A`。
- 不把运行数据、录屏、导出文档、缓存、密钥或其他任务改动带入提交。
- 推送前再次 fetch；来源不明的待推送提交、远端领先或冲突必须先查清。
- 优先推送当前分支 upstream；无 upstream 时推送 `origin` 同名分支并建立 upstream，不把非默认分支自动推到远端默认分支。
- 不强推。确需强推时先说明对象、原因和风险并等待确认。
- 汇报说明改了什么、验证了什么、是否达到目标、剩余边界、提交和远端结果。
