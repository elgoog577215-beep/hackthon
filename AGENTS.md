# 灵知项目经验与规则

开始任何任务前，先读取并遵守 Codex 主目录中的通用 `AGENTS.md`（默认 `~/.codex/AGENTS.md`）；本文件在其基础上补充项目规则，除明确说明的项目例外外，不替代或削弱通用规则。

```text
项目 AGENTS.md
├── 一、文件体系
│   ├── 项目文档
│   └── 操作手册与任务合同
├── 二、更新机制
│   ├── 项目变化
│   ├── 事实记录
│   └── 规则沉淀
└── 三、项目经验与规则
    ├── 产品与数据边界
    ├── 动手前的判断
    ├── 开发与交付
    ├── 验收与汇报
    ├── 前端开发范围
    ├── UI 设计经验
    ├── 工程护栏
    └── 未分类经验（始终放在最后）
```

## 一、文件体系

### 1.1 项目文档

1. `README.md`：项目简介、环境、启动方式和基本协作入口。

2. `docs/产品蓝图.md`：产品定义、核心闭环、模块设计和长期边界。

3. `docs/产品状态.md`：当前阶段、模块进度、主要缺口和下一步。

4. `docs/系统架构.md`：代码结构、数据真源、运行链和技术边界。当前文件名仍为“系统架构”，重命名时必须同步修复全仓引用。

5. `AGENTS.md`：本项目长期协作规则与经过验证的项目经验。

`docs/归档/` 保存已经退役的决策、旧规则、演示材料和历史验收证据，只用于追溯，不是当前施工入口。

### 1.2 操作手册与任务合同

1. `openspec/specs/`：已经稳定的功能合同、状态、字段和验收场景。

2. `openspec/changes/`：正在实施的复杂变更、设计、任务和验收合同。

3. `runner/README.md`：独立运行器的使用入口。

稳定、可重复的安装、发布、打包或排障方法优先写入 README、脚本或 CI。当前没有独立的 `docs/操作手册/`，不为统一格式创建空目录。

## 二、更新机制

不同内容写入对应位置：

```text
产品设计和长期边界 → docs/产品蓝图.md
模块进度和当前缺口 → docs/产品状态.md
代码、数据和运行架构 → docs/系统架构.md
稳定功能合同 → openspec/specs/
复杂功能开发 → openspec/changes/
执行结果和用户反馈 → docs/事实.md
长期经验与协作规则 → AGENTS.md
```

1. 更新项目：项目发生稳定变化后，写回对应项目文档或 OpenSpec，不建立“最新版”“终稿 2”等平行真源。

2. 记录事实：重要执行结果、用户明确反馈、真实模型或真实页面结果和高代价失败写入 `docs/事实.md`。每条事实记录时间、背景、动作、结果、反馈和证据，不复制产品文档已经承担的静态定义。

3. 提炼经验：用户明确提出长期要求，或者多个事实证明同一种方法稳定有效时，将能够改变后续行动的结论写入本文件。历史对话只提供候选证据，优先采用用户明确纠正、验收反馈和反复提出的要求；AI 当时的方案、自我总结和一次性偏好不能直接升级为长期经验。写入前还要与当前代码和正式文档核对。

4. 归类经验：新经验写入最合适的章节；暂时无法归类时放入“未分类经验”。同类经验积累成稳定主题后，再整理为新的分类。

5. 保持轻量：新经验优先合并或替换旧经验。可以由测试、CI、领域约束或正式 spec 保证的内容，不在本文件重复维护。修改文件体系、更新机制、核心产品边界或通用规则前，需要用户确认。

6. 写清经验：一条经验只讲一件事，先说在什么情况下做什么，再说明边界或验收方式。能用常用词说清楚时，不堆缩写、术语和多层名词；确实需要的代码标识保持原名。

## 三、项目经验与规则

### 3.1 产品与数据边界

1. 课程生产、教师备课、学生学习、学习记录和个性化成长属于同一个产品闭环。修改局部功能时，要检查它会不会破坏整个闭环。

2. 教师备课和学生预习都是正式入口，必须能够直接使用，不能因为新增功能而被隐藏或绕远。

3. 一门课程的正式正文只有一份，由 `CourseDocument + ordered CourseBlock[]` 保存。生成过程、Markdown、旧数据和兼容页面都不能成为第二份正式正文。

4. 学习过程中真实发生的事情由 `LearningEvent` 和正式领域仓库保存。`LearnerModel` 只是根据这些事实生成的解释，可以重新计算，不能反过来充当原始事实。

5. 后台任务使用 `task/job ID` 控制暂停、恢复、删除和状态同步。`course_id` 只说明任务属于哪门课程，不能用来代替任务 ID。

6. 不为同一件事建立第二套 Store、第二份课程正文、第二套任务系统，或者只为方便某个页面而设计一套平行接口。

7. AI 修改正式课程、学习记录或长期学习模型前，必须使用系统允许的正式命令，并提供修改范围、候选结果、影响说明、用户确认、执行回执和恢复方法。

### 3.2 动手前的判断

1. 开始分析、排查或改造前，先确认仓库、分支或工作树、运行服务、页面路由和数据来源是不是同一个版本。

2. 当前代码、正式文档、真实数据和运行结果的优先级最高。旧截图、旧对话、服务器旧现场和其他工作树只能提供线索，不能直接当作当前事实。

3. 判断一项功能是否完整时，要沿着真实用户路径检查：

   ```text
   从哪里进入
   → 使用哪些资料和输入
   → 系统怎样处理
   → 用户能否看见过程
   → 结果能否查看和修改
   → 怎样确认或发布
   → 失败后怎样重试和恢复
   → 最终写入哪份正式数据
   ```

   只有后端函数、只有页面外壳或只有一次成功演示，都不能算功能已经完成。

4. 修复问题时先找到原因，再检查同一种触发机制在本次范围内是否还有类似问题。不能只隐藏眼前现象，也不能借机修改无关模块。

5. 设计新能力前，先确认谁保存正式数据、谁管理状态、谁有权限修改、怎样审核、失败后怎样退出和恢复，然后再设计页面与 API。

### 3.3 开发与交付

1. 文案、文档、局部样式和单点错误可以直接修改。核心课程链路、跨模块改造、数据模型、权限、API 和复杂 AI 流程使用 OpenSpec。

2. 已有 OpenSpec change 能覆盖当前任务时，继续使用原来的 change，不再建立平行方案。

3. Git 只暂存本次任务修改的文件，不带入本地数据、缓存、视频、录制文件或其他生成产物。提交前检查暂存范围，并运行 `git diff --cached --check`。

4. 用户明确要求执行的任务，在修改完成并通过必要验收后，默认提交并推送当前分支。遇到远端冲突、权限失败、受保护分支或发布风险时停止，不强行推送。

### 3.4 验收与汇报

采用能够证明本次目标完成的最小充分验收。

1. Markdown、规则和小范围文案，只检查内容、结构、链接和 `git diff --check`，不启动完整项目，也不为了形式完整而截图。

2. 后端只运行与改动直接相关的测试。需要完整回归时，分别运行 `pytest backend/tests` 和 `pytest tests`，不要混在一起收集。

3. OpenSpec 变更先验证对应 change。只有影响多个 change 或正式 spec 时，才运行全量验证。change 完成后先验证，再归档并复验。

4. 用户可见的页面变化，默认验收中文桌面端。修改文案时同步检查本次涉及的英文内容。只有明确提出移动端需求后，才验收移动端。

5. 单元测试、真实模型调用、浏览器页面和生产环境是不同证据。测试通过不能代替真实模型结果，浏览器截图也不能代替生产环境结果。

6. 截图只能证明页面出现了什么，不能单独证明问题为什么发生。先用截图找到现象，再检查页面结构、代码和真实数据。只检查本次改动涉及的页面和状态；截图放在临时目录，除非用户明确要求，否则不提交 Git。

7. 最终汇报要说清楚哪些已经通过、哪些没有运行、哪些是原有失败、哪些依赖外部系统、哪些仍需要用户确认。

### 3.5 前端开发范围

1. 灵知当前只建设桌面端。没有明确的移动端需求时，不分析、不设计、不适配、不测试，也不截图验收移动端。

2. 现有中文和英文继续保留。修改一段用户可见文案时，同步维护对应英文，但不把全站国际化改造当作默认任务，也不扩展其他语言。

3. 新增文案继续通过现有 i18n 入口读取，不直接写死在组件、默认值或无障碍标签里。修改词典后解析 JSON，并检查本次改动有没有原始 key、异常字符或明显溢出。

### 3.6 UI 设计经验

1. 先确定页面服务谁、处理什么任务、用户按照什么顺序思考和操作，再决定页面布局。页面结构要符合用户对对象、关系和步骤的自然认知，不能先画一套通用界面，再把功能硬塞进去或平铺成一组同级状态。

2. 内容和操作要放在真正负责该任务的页面中。产品界面只保留帮助用户完成当前任务的事实、状态、输入提示和可选操作；不得把 AI 思考过程、实现说明、需求原话、开发约束或对用户纠正的复述写进界面。只用于约束生成或实现的规则，应由代码、合同、测试或项目规则承担。

3. 当前课程界面是视觉和交互基线。已经成熟的颜色、渐变、状态表达，以及用户明确喜欢或要求保留的区域，不得顺手重做。

4. 核心流程需要连续操作时，使用完整工作区。主对象、当前任务和相关操作按照真实关系放入稳定区域；侧栏、抽屉和弹窗只放补充信息、就近操作或较短的流程。

5. 内容很少或为空时，页面应当主动收拢，不保留巨大的多栏空壳，也不使用很大的容器承载少量内容。

6. 操作型页面打开后，用户应该通过真实对象、客观数据、状态层级和操作位置直接理解界面，不依赖额外说明，并且很快知道：

   - 自己正在处理什么；
   - 事情进行到哪里；
   - 接下来可以做什么。

   主要操作要靠近它所影响的内容。

7. 全局顶栏已经说明当前位置时，页面正文不再重复大标题和介绍。集合页优先把搜索、状态筛选和视图切换放在内容上方的一行工具栏中。

8. 优先使用排版、留白和字号建立层级，不为每组内容都增加卡片、边框和徽标。没有明确判断或操作价值的统计数据不展示；必要说明应短、具体、就近，不能代替界面结构本身。

9. 页面要显示对用户有用的真实状态，包括生成进度、修改内容、影响范围、失败原因和恢复方法。技术错误可以展开查看，但不应抢占主要视线。

10. 看起来能够点击的按钮必须真的可用。暂时不能使用时，应当禁用并说明原因，不能留下只有外观、没有功能的控件。

### 3.7 工程护栏

#### 3.7.1 课程和学习数据

1. 正式修改课程版本时，要记录修改来源、影响范围和防止重复执行的幂等键。普通滚动、阅读位置和本地恢复只能更新当前位置，不能产生新的课程版本。

2. 一级章节只负责导航和刷新。只有具备正式正文和学习目标的二级内容才能产生学习事件。缺少正式学科包时可以使用当前课程知识，但不能伪造学科 ID。

3. 生成预览只能读取任务、检查点和安全草稿，不能写入学习进度、笔记、AI 修改或正式课程。发布成功后才能切换到正式 `CourseDocument`。

#### 3.7.2 生成和质量

1. 模型没有返回结果、返回结构错误、违反硬性要求和内容质量不足，是四类不同问题，必须分别处理。

2. 降级方案只能解决展示问题，不能把不合格或不完整的课程伪装成生成成功。

3. 发布质量要分别检查正文、难度、资料、知识、题目和一致性。不能用一个平均总分掩盖答案矛盾、答案不可判断等严重问题。

4. 质量规则如果会改变通过状态或问题代码，就必须更新合同版本。旧报告只有在合同版本和规则摘要相同时才能复用；能够直接重新计算的质量变化，不能要求用户再次调用模型。

5. 同一能力的自动、手动、恢复和兼容入口，必须调用同一个底层函数和质量检查。验收时不能只测试其中一条路径。

#### 3.7.3 任务和恢复

1. 后台轮询、健康检查和自动恢复出现问题时，使用局部或静默错误状态，不要让后台异常反复打断用户。

2. 同一资源只能有一个同步负责人，同一个异常只能处理一次，避免多个组件同时恢复、重复提示或互相覆盖状态。

3. 浏览器缓存只保存最后一次成功现场，用于断网和刷新恢复。重新联网后，要按照任务 ID 与服务端对账，不能假装后台任务仍在运行。

#### 3.7.4 专项排障

1. 修复 Markdown 和 LaTeX 时，要尊重各自的语法边界，并保留真实失败样本。连续增加正则仍然产生回归时，应改用正式解析或先隔离语法区域。

2. 浮层出现闪烁、裁切或遮挡时，同时检查选择器、`pointer-events`、祖先 `overflow`、堆叠上下文和视口边界，不要只增加 `z-index`。

3. `index.html` 和 SPA 回退页应禁止缓存或在发布后重新验证。带内容哈希的静态资源可以长期缓存；发布时同时检查首页、子路由和静态资源的响应头。

### 3.8 未分类经验

暂时无法确定位置，但已经证明长期有效的经验放在这里。形成稳定主题后，再迁入对应分类。当前暂无。
