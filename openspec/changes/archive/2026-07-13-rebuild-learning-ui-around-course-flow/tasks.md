# 任务

## 0. 执行准备

- [x] 0.1 对照 `9ae4cc7` 审计 3 月学习页、覆盖层、选区和 AI 交互
- [x] 0.2 审计当前路由、学习模式栏、`CourseWorkspace`、固定笔记栏、`SmartBar` 和 AI 重复入口
- [x] 0.3 核对学习记录、正式练习、诊断、运行时和 AI 老师的现有前后端契约
- [x] 0.4 运行前端生产构建与后端核心领域测试，记录改造前基线
- [x] 0.5 明确保留、换壳、拆分、退役和新增组件清单

## 1. P1-A 骨架样板

- [x] 1.1 建立 `/courses` 课程库路由和 `/course/:courseId/learn/:nodeId?` 学习路由，并保留旧 URL 重定向
- [x] 1.2 建立 `LearningShell`、`CourseNavigator`、`LearningMain`、`AITeacherRail` 和 `ToolOverlayHost`
- [x] 1.3 建立单一视觉 token 源并迁移典型学习页，消除样板中的重复颜色、圆角和阴影定义
- [x] 1.4 建立 `LearningFlowItem` 只读适配器，支持结构化块和旧 Markdown 降级
- [x] 1.5 用真实课程完成 390、789、1024、1440 像素骨架截图与交互样板
- [x] 1.6 完成门禁 A 人工验收后再进入业务链路

## 2. P1-B 正文任务链

- [x] 2.1 从现有学习资产生成稳定 `TaskLauncherBlock`，不得复制正式题目
- [x] 2.2 将 `PracticeWorkspace` 嵌入统一 `TaskOverlay`，按 `task_ref` 精确恢复 Attempt/诊断任务
- [x] 2.3 实现来源块、滚动位置、草稿和 AI 任务上下文的打开/关闭恢复
- [x] 2.4 覆盖开始、继续、提交、评分、提示、AI 支持、重试、诊断、补救和复验状态
- [x] 2.5 增加任务入口、覆盖层恢复、刷新续答和冲突的前端测试与浏览器验收

## 3. P1-B 行内记录与锚定问答

- [x] 3.1 扩展前后端锚点为 `text_position + text_quote`，增加重复文本与内容更新测试
- [x] 3.2 从 `ContentArea` 拆出 `InlineAnnotationLayer`、`InlineLearningRecordBlock` 和 `InlineRecordPopover`
- [x] 3.3 将手写笔记改为原文附近行内编辑、自动保存、失败保留草稿和立即撤销
- [x] 3.4 增加锚定 AI 问答摘要的幂等 upsert 命令、记录子类型和会话消息引用
- [x] 3.5 首个完整回答创建摘要、连续追问更新同一块；普通聊天和中间输出不得自动沉淀
- [x] 3.6 把 `NotesPanel` 改为按需打开的学习记录工具，覆盖搜索、筛选、定位和迁移状态
- [x] 3.7 完成门禁 B 真实完整学习链验收

## 4. P1-C 退役重复布局

- [x] 4.1 删除顶部阅读/总览/练习/掌握/蓝图/版本模式栏
- [x] 4.2 迁出 `CourseWorkspace` 可复用逻辑并退役其学生页面外壳
- [x] 4.3 删除固定笔记栏、像素位置同步、正文占位和 `notesCollapsed` 协调链
- [x] 4.4 删除旧六按钮 `SmartBar`、学习页顶部 `LearningContinuityBar`、AI 浮球和旧导师建议残余；将唯一下一步与正式工具收进桌面学习工具坞
- [x] 4.5 从学习目录移除重新生成、蓝图、版本和质量修复等生产操作
- [x] 4.6 建立旧入口不可到达测试和新入口能力到达性测试
- [x] 4.7 完成门禁 C 人工验收

## 5. P1-D 运行时适配与剩余工具

- [x] 5.1 定义并实现 `LearningRuntime.adaptive_blocks`，包含锚点、依据、状态、到期和反馈
- [x] 5.2 渲染解释、反例、过渡和非正式理解检查，限制同锚点最多一个活动块
- [x] 5.3 将统计、知识图谱和其他大工具迁移到统一覆盖层
- [x] 5.4 完成课程库创建、导入、生成状态、失败重试和返回学习的完整流程
- [x] 5.5 在保持 3 月视觉身份不变的前提下，清理嵌套 glass 模糊、重复光晕、跨容器 fixed 和重复 token 技术债
- [x] 5.6 融合 3 月版顶栏与课程目录的视觉性格和新版高密度布局，移除正文重复横线并强化收束型模块标签

## 6. 验证与收束

- [x] 6.1 运行前端单元测试、生产构建和后端相关领域测试
- [x] 6.2 验证中文与英文标题、按钮、空状态、错误、无障碍文案和布局
- [x] 6.3 以同视口参考/实现对比完成 390、789、1024、1440 像素视觉验收
- [x] 6.4 真实验证阅读、记录、AI、任务、诊断、恢复、断网草稿和刷新续答
- [x] 6.5 检查运行时数据、无关改动和旧兼容适配器边界
- [x] 6.6 更新产品蓝图、归档变更并执行 `openspec validate --all`

## 7. 收束证据

- 前端：29 个测试文件、191 项测试通过；新增课程生成、任务中心、任务覆盖层、行内记录、自适应块和旧路由不可达测试。
- 后端：`backend/tests` 237 项、根 `tests` 106 项分别通过；两个测试目录因同名 `conftest.py` 需要分开运行。
- 构建：`vue-tsc -b && vite build` 通过；仅保留既有 Browserslist 数据过期和大 chunk 警告。
- 视觉：真实课程在 390、789、1024、1440 像素完成截图和交互检查，并与 3 月参考截图同批对照。
- 数据：运行时验收数据与用户工作区改动保持隔离，提交不包含 `backend/data` 生成物。
