## 1. 规格与基线

- [x] 1.1 审计课程创建、资料、prompt、调度、正文、质量、存储和前端状态的真实生产链路
- [x] 1.2 重写课程生成内核 V2 的数据契约、职责边界、迁移策略和验收标准
- [x] 1.3 为保留能力和已确认缺陷建立回归测试

## 2. 教学内核

- [x] 2.1 实现八种教学模式、通用模块、学科模块包和旧值读取适配
- [x] 2.2 实现自动识别、用户锁定、主辅模式和辅助强度
- [x] 2.3 将教学画像和模块计划写入 brief、CourseBlueprint 与节点
- [x] 2.4 用唯一课程 prompt 编排器替换 V5 和固定正文板块

## 3. 后端主链路

- [x] 3.1 建立唯一 GenerationJob 并让创建接口异步返回 job_id
- [x] 3.2 让 CourseService 从持久化课程上下文生成，不依赖进程内 plan/settings 真源
- [x] 3.3 实现蓝图、节点和全课质量闸门及一次定向修复
- [x] 3.4 在正文完成后生成最终 GenerationQualityReport
- [x] 3.5 删除第二阶段 auto_generate 和旧课程生成旁路

## 4. 调度与恢复

- [x] 4.1 支持多课程并发和全局节点并发限制
- [x] 4.2 实现真实暂停、草稿检查点、继续和取消
- [x] 4.3 实现任务原子持久化与服务重启自动恢复
- [x] 4.4 推送 brief、资料、画像、蓝图、正文、检查和保存的阶段进度

## 5. 前端

- [x] 5.1 让 generation store 只使用后端 job_id，并以后端状态覆盖本地缓存
- [x] 5.2 创建课程后直接订阅同一任务，不再调用 auto_generate
- [x] 5.3 增加课程结构模式选择和教学画像结果展示
- [x] 5.4 展示多课程、阶段、暂停、恢复和失败状态
- [x] 5.5 为本次新增或修改文案同步中英文资源并通过 t(...) 读取

## 6. 清理与验证

- [x] 6.1 删除旧学科配置、V5 prompt、旧质量模式、旧修复器和死生成分支
- [x] 6.2 验证旧课程读取、无资料、有资料、低置信度和模型结构化输出失败降级
- [x] 6.3 验证同主题不同目标、混合课程、模块去重和用户锁定
- [x] 6.4 验证多课程并发、暂停、重启续跑、节点重试和最终质量报告
- [x] 6.5 运行 OpenSpec、后端测试、前端测试、构建和旧电路全仓扫描
- [x] 6.6 生成编程实践、数学推导和商务语言三门课程做真实 smoke
