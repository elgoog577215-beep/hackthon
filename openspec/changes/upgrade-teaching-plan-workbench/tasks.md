# 实施任务

## 0. 范围冻结与基线

- [x] 0.1 锁定 `CourseTeachingPlanV3`、projection、course revision vector、course evolution、teaching representation 的当前接口和测试基线。
- [ ] 0.2 记录现有课程中 V2、V3、缺失教案和失败生成状态的样本，建立只读兼容验收夹具。
- [x] 0.3 确认本变更不改变首次生成阶段、模型调用次数、目录确认门和发布门；把新增编辑能力标记为教师工作台能力。
- [x] 0.4 定义字段白名单、只读字段、结构操作白名单、操作 ID 规则、修订状态枚举和中英文术语表。

## 1. 教案草稿与修订底座

- [x] 1.1 定义 `TeachingPlanDraft`、`TeachingPlanRevision`、`TeachingPlanChangeSet`、`TeachingPlanOperation`、`TeachingPlanImpactReport` schema。
- [ ] 1.2 实现课程级草稿仓库，支持原子写入、当前用户单草稿、跨刷新读取、草稿过期和删除。
- [x] 1.3 实现正式教案修订仓库，保存完整快照、父修订、修订编号、来源向量、质量报告和操作回执。
- [x] 1.4 扩展课程修订向量、课程 envelope 和下游来源读取，明确 `course_teaching_plan` 当前正式修订与历史修订的边界。
- [x] 1.5 接入文件仓库锁、原子替换、命令幂等和基础修订冲突校验。
- [x] 1.6 为草稿/修订/并发/幂等/失败不污染正式课程补充后端单测。

## 2. 结构化编辑命令与质量门

- [x] 2.1 实现总体字段 patch：定位、对象、起点、目标、策略、评价和课堂条件。
- [x] 2.2 实现小节字段 patch：学习目标、教学职责、教学指导、课堂执行和描述性知识字段。
- [ ] 2.3 实现教学模块操作：新增、删除、替换、排序；校验模块类型、职责、知识引用和必需模块合同。
- [ ] 2.4 实现知识语义操作：描述、能力、易错、掌握标准和边界；禁止直接修改稳定知识 ID 和来源绑定。
- [ ] 2.5 对目录结构、章节增删、章节排序等操作返回 `redirect_to_outline_edit`，不在教案命令内绕过目录真源。
- [x] 2.6 复用现有 normalize/validate/compile 函数，新增教案编辑后的结构、知识、模块、目标和课程块一致性校验。
- [ ] 2.7 为每类操作补充“通过、警告、阻断、过期、锁定冲突”测试和错误码。

## 3. 确定性影响分析与下游状态

- [x] 3.1 实现字段语义到影响对象的静态映射和依赖闭包，不让模型决定失效范围。
- [x] 3.2 输出 `changed / needs_regeneration / stale / unchanged / blocked` 五组结果及理由、来源路径和修订前置。
- [ ] 3.3 接入当前课程知识库与 `KnowledgeBinding` 的引用反查，支持知识变化的局部影响预览。
- [ ] 3.4 接入 CourseDocument、课程块、正式练习、SlideDeckSpec、讲义和 TeachingRepresentation 来源修订检查。
- [ ] 3.5 为描述性变更、目标变更、模块变更、知识变更、关系变更、章节变更建立影响矩阵快照测试。
- [ ] 3.6 定义下游候选/待重建/锁定冲突状态，保证旧正式正文、练习和 PPT 在新产物失败时继续可读。

## 4. 教案工作台接口与 AI 提案

- [x] 4.1 新增 workbench 聚合读取接口，返回正式教案、当前草稿、修订向量、可编辑边界、质量状态和下游摘要。
- [x] 4.2 新增草稿创建、patch、校验和删除接口；所有写请求携带基础教案修订、字段值摘要和幂等键。
- [x] 4.3 新增变更集生成、差异读取、应用、拒绝、历史列表、历史差异和恢复新修订接口。
- [x] 4.4 新增 AI 字段改写、小节重新生成和全课设计调整提案接口；统一返回结构化 patch、理由、影响和质量状态。
- [ ] 4.5 将 AI 提案接入现有白名单动作/课程生长确认协议，不允许模型直接调用教案写命令。
- [ ] 4.6 为接口补充 200/400/409/422/500 的结构化错误与真实 API smoke，覆盖重复请求和基础版本冲突。

## 5. 前端教案工作台

- [ ] 5.1 将 `GenerationLessonPlan.vue` 拆分为总体编辑器、分小节编辑器、字段编辑单元、差异审阅和影响面板，保留现有阅读投影。
- [ ] 5.2 增加查看/编辑模式、字段可编辑标识、只读原因、输入校验、撤销当前字段和恢复正式值。
- [ ] 5.3 实现草稿状态条、防抖自动保存、保存失败保留、本地离开保护、刷新恢复和冲突提示。
- [x] 5.4 实现 AI 字段改写、小节重新生成、全课设计调整入口、候选生成状态和候选放弃。
- [ ] 5.5 实现差异审阅：新增、删除、替换、结构变化、AI/手工来源和字段路径定位。
- [ ] 5.6 实现影响审阅：受影响、待重建、保持不变、阻断项和重建策略选择。
- [ ] 5.7 实现应用回执、失败恢复、修订编号刷新、历史列表、版本比较、恢复新修订和撤销入口。
- [x] 5.8 保持知识标签跳转、目录导航、知识库入口和生成现场跟随行为不回归。
- [x] 5.9 补齐 zh/en locale、无障碍标签、键盘操作、响应式布局和 loading/empty/error 状态。

## 6. 下游重建与课程表达同步

- [x] 6.1 将教案正式修订写入 CourseDocument/课程修订事件的来源向量和操作日志。
- [ ] 6.2 按影响报告创建正文块、练习、知识绑定、PPT 和讲义的定向重建候选；未受影响对象复用原修订。
- [ ] 6.3 复用现有质量门和 representation reconciliation，保证生成失败不覆盖最后可用产物。
- [ ] 6.4 返回逐对象同步回执，区分 `content_changed`、`source_verified`、`stale`、`blocked` 和 `unchanged`。
- [ ] 6.5 验证教案变更与既有正文块级课程生长、PPT 目标修改、知识库候选之间的冲突和合并边界。
- [ ] 6.6 增加重建恢复、取消、重复应用、并发候选和过期来源的测试。

## 7. 历史兼容与数据迁移

- [x] 7.1 为缺失教案的历史课程保留只读降级，不伪造结构化编辑字段。
- [x] 7.2 为 V2/V3 历史教案实现读取归一化和首次编辑基线修订创建，保证旧课程首次打开不静默改写。
- [ ] 7.3 为缺失知识绑定的课程限制知识结构编辑，只开放描述性字段并显示补全要求。
- [x] 7.4 验证取消/删除草稿不影响正式课程、历史修订、学习事实和已发布表达。
- [x] 7.5 记录迁移版本、来源校验和失败恢复，不删除历史课程文件。

## 8. 验证、发布与收束

- [ ] 8.1 运行后端教案、命令、影响、修订、知识绑定、表达同步和并发相关测试。
- [x] 8.2 运行前端组件/Store 测试和生产构建。
- [ ] 8.3 使用 Playwright 验证桌面 1440/1024、平板 789、移动 390 的完整编辑闭环和无重叠布局。
- [ ] 8.4 验证英文模式无中文残留、原始 key、按钮截断、空状态缺失和无障碍名称缺失。
- [ ] 8.5 启动隔离端口的灵知前后端，执行真实草稿、校验、应用、冲突、历史和下游状态 API smoke。
- [ ] 8.6 运行 `openspec validate --all`、`git diff --check`、相关文档链接检查和工作区边界检查。
- [ ] 8.7 接入课程级 feature flag 和观测指标，按测试课程、新建 V3、完整绑定 V3、V2 兼容四档灰度；验证关闭开关不会删除草稿、修订或旧产物。
- [ ] 8.8 仅在产品语义变化时更新产品蓝图，同步产品状态、项目决策和发布说明，完成代码评审后再归档本变更。
