# 任务：同源教学表达编译与派生同步

> 完成度门禁：`P0 / R1 / R2 / R3` 只表达实现依赖，不表达产品优先级或验收完成度。结构化同源必须独立通过“基础课程真源 → 多种表达 → 影响预览 → 维护者确认 → 精准重建 → 失败降级”纵向验收；个体化生长的完成度不作为本能力的替代验收条件。

## 1. P0 共享内核

- [x] 1.1 定义并版本化课程修订向量与持久修订事件
- [x] 1.2 定义 `SourceBinding / RepresentationPlan / TeachingRepresentation / RepresentationSet`
- [x] 1.3 实现按课程隔离、原子保存的教学表达注册表
- [x] 1.4 实现 `AssetDerivationGraph`、来源边和确定性陈旧传播
- [x] 1.5 将课程发布与正式课程命令接入可重放修订事件
- [x] 1.6 覆盖局部变化、课程级变化、删除、幂等重放和跨课程隔离测试
- [x] 1.7 将 P0 注册表和状态读取接入正式后端 API

## 2. R1 最小表达闭环

- [x] 2.1 实现大纲确定性投影，不保存第二章节树
- [x] 2.2 实现 `LessonPlanSpec` 生成、来源绑定与质量检查
- [x] 2.3 实现 `HandoutSpec` 生成、来源绑定与质量检查
- [x] 2.4 实现正式题目引用和 `PracticeSheetSpec` 投影
- [x] 2.5 实现 `SlideDeckSpec` 生成、预览和 `.pptx` 导出
- [x] 2.6 建立 `RepresentationPlan` 的模态理由、成本、无障碍和降级链

## 3. R2 精准重建与质量

- [ ] 3.1 将修订事件接入异步陈旧传播消费者和对账任务
- [ ] 3.2 实现单页、单段和单题组的增量重建计划
- [ ] 3.3 复用统一任务运行时实现暂停、取消、重试、断点和重启恢复
- [ ] 3.4 实现表达类型质量门和跨产物一致性检查
- [ ] 3.5 构建失败时保留最后可用版本并启用降级表达
- [x] 3.6 在课程工作区展示最新、过期、构建中、失败和旧修订状态

## 4. R3 反向语义编辑

- [x] 4.1 实现 `SlideDeckSpec` 结构化编辑差异
- [x] 4.2 实现表现、等义、语义和模糊四类编辑判定
- [x] 4.3 将语义修改转换为独立 `CourseAuthoringChange`，并禁止个人证据进入基础课程写入链
- [x] 4.4 展示正文、知识、题目和其他表达的影响范围
- [ ] 4.5 确认后通过课程命令提交并触发精准重建
- [ ] 4.6 在结构化链稳定后评估外部 `.pptx` 回导

## 5. 图解、动画与边界协作验收

- [ ] 5.1 实现 `DiagramSpec` 与确定性图解渲染
- [ ] 5.2 实现 `AnimationSpec`、关键帧、文字步骤和静态降级
- [x] 5.3 将个人学习证据用于选择或新增教学表达，不建立固定学习风格
- [x] 5.4 完成结构化同源演示 A
- [ ] 5.5 验证基础课程更新后个人覆盖层可重新定位、冲突时不被静默覆盖
- [x] 5.6 运行后端、前端、生产构建、OpenSpec 和真实浏览器验收
- [ ] 5.7 清理被新注册表和变化协议接管的竞争状态
- [ ] 5.8 全部完成后归档本 OpenSpec 并再次校验全部规格
