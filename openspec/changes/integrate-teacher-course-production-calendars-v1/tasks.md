## 1. 教师课程工作台入口与课程上下文

1.1 将“我的课程 / 教学总日历 / 新建课程”组织为同一教师课程工作台层；课程按学年/学期分组，课程卡只显示可判断状态和一个下一步。

1.2 新建课程使用可恢复分步页：先幂等创建课程壳，再补排课基础，最后选择 AI 规划、导入大纲或空白开始；复用现有上传和生成请求合同。

1.3 新增教师课程概览，固定作为课程卡落点；单课程六项导航保留 `courseId` 和返回上下文，不以学生 `LearningView` 充当教师概览。

## 2. 真实课程生产编排

2.1 核对并修复课程库、真实生产路由与课程加载/任务观察生命周期，确保刷新和离开页面不会串课程或残留订阅。

2.2 课程生产只编排课次总览、分讲教案与 PPT，现有学生课程内容保持原入口。补齐显式阻断、失败原因、暂停/恢复和下一动作。

2.3 复用现有目录确认、教案工作台、任务中心、发布门和 PPT 工作台；禁止模拟计时器、平行 Store 和第二套 PPT 接口。

2.4 归一生产页 UI：原子/复合/反馈组件继承项目现有实现，页面网格和间距按当前功能重组；总览取消固定阶段侧栏与状态侧栏，默认直接出现课次表，版本/来源进入抽屉，任务进入浮窗。完成中英文文案和 1440/1180/880/680 验证。

## 3. 教学日历后端

3.1 实现 `TeachingCalendarV1` / `ClassSessionV1` 校验、按 owner/course 隔离的原子 JSON 仓库和 `base_revision` 冲突检测。

3.2 实现单课程读取、保存、从当前大纲派生候选和教师总日历日期范围聚合 API，并注册到 FastAPI 主应用。

3.3 增加后端测试，覆盖真实持久化、身份隔离、增删场次、无效时间、课程不存在、修订冲突、派生不覆盖和多课程聚合。

## 4. 单课程教学日历前端

4.1 新增强类型日历 Store，封装读取、保存、派生和总日历查询；错误保持服务端原因，不用假成功提示。

4.2 新增课程教学日历路由与页面，复用课程壳和现有组件；实现表格、月、周视图，场次增删改、二次确认、保存状态和冲突恢复。

4.3 从课程生产页/课程侧栏进入教学日历；大纲派生只形成候选，保留已有排课字段。

## 5. 教师教学总日历前端

5.1 在课程工作台外围新增教师教学总日历入口和路由，提供月、周、列表视图及稳定课程色标。

5.2 点击总日历事件进入对应课程教学日历并定位场次；第一版保持聚合只读。

5.3 增加前端 Store/路由/关键交互测试和中英文词典校验。

## 6. 集成、问题记录与验收

6.1 启动真实前后端，准备一个生成中课程、一个局部失败课程和两个已保存日历的课程作为验收数据。

6.2 使用真实浏览器验证课程生成隔断、刷新恢复、PPT 跳转、日历保存后刷新、总日历聚合和返回上下文；检查控制台与网络失败。

6.3 在当前 run 记录发现的问题、失败原因、已采用调整和延期边界；未验证的能力明确标记，不伪装完成。

6.4 运行相关后端测试、前端测试、类型检查/构建、OpenSpec 校验和变更范围检查。

## 7. 学生/教师产品面隔离与合并准备

7.1 冻结双端路由契约：学生端保留 `/courses`、`/course/:courseId/learn/:nodeId?` 和原课程入口；教师端收敛到 `/teacher/courses` 与 `/teacher/course/:courseId/...`。

7.2 建立教师前端运行时适配入口，将教师页面对 `courseStore`、`generationStore` 及生成/PPT 能力的依赖收敛到一处；不复制底层状态。

7.3 将教师专属 authoring/orchestration 端点从共享 `courses` 路由中拆出，保留共享课程、任务、教案和 PPT 引擎的原契约。

7.4 对当前分支与 `origin/main` 做文件所有权审计：教师专属文件保留，共享策略修改还原或拆出，必要兼容扩展保持最小且有独立契约测试。

7.5 增加双端回归：学生课程库/续学/练习/AI 教师行为不变；教师课程库/概览/生产/预览/日历与发布流程可用；教师预览不产生学习写入。

7.6 生成合并前 diff/contract 报告，明确学生端、教师端、共享引擎、适配层四类文件及后续所有者；未经用户授权不 push。
