# 实施任务

## 1. 合同与数据

- [x] 1.1 扩展 `TeachingRepresentation` 支持 `image`、`candidate / accepted` 生命周期。
- [x] 1.2 实现候选原子发布、同作用域旧候选归档、教师确认和 `RepresentationSet` 更新。
- [x] 1.3 实现讲义修订来源核对与旧表达 `stale` 传播。

## 2. 三类表达编译

- [x] 2.1 实现讲义块 `DiagramSpec` 确定性编译和质量检查。
- [x] 2.2 实现图片提示词、可选提供方调用、不可变资产保存与未配置/失败降级。
- [x] 2.3 保留 `scene_spec_v1` 对象、动作、时间、检查点和静态降级的历史兼容。
- [x] 2.4 实现按教学块角色给出视觉表达建议，不让建议直接写入正式表达。
- [x] 2.5 实现文本模型规划的 `scene_spec_v2`，支持图元、路径、位移、旋转、轨迹和加减速曲线，拒绝纯卡片显隐伪动画。

## 3. 教师接口与资产

- [x] 3.1 新增视觉列表、候选生成和候选处理接口。
- [x] 3.2 扩展教学表达资产读取接口，允许读取经规格与哈希验证的图片表达资产。
- [x] 3.3 补充来源冲突、不可采用空图片、重复处理和课程隔离测试。

## 4. 讲义交互

- [x] 4.1 新增独立讲义视觉 Store，负责读取、生成、确认和图片 Blob URL 生命周期。
- [x] 4.2 新增每教学块默认折叠的视觉表达工作区，显示建议、候选、已采用、过期和恢复动作。
- [x] 4.3 新增结构化动画播放器，支持播放、暂停、上一步、下一步、重播和 reduced-motion。
- [x] 4.4 最小接入 `TeacherScriptDocument.vue`，不覆盖已有讲义编辑改动。
- [x] 4.5 扩展播放器逐帧解释 `scene_spec_v2`，渲染小球沿斜面连续滚动等教学场景。

## 5. 验收与收束

- [x] 5.1 运行后端视觉编译、生命周期、来源过期和图片降级测试。
- [x] 5.2 运行前端视觉工作区、动画控制和现有讲义组件测试。
- [x] 5.3 运行类型检查、前端构建、OpenSpec 严格校验和 `git diff --check`。
- [x] 5.4 更新产品蓝图、系统架构和产品状态中的当前能力与剩余边界。
- [x] 5.5 用真实 `qwen3.8-27b` 生成斜面小球场景，并验证规格、连续播放、暂停与检查点。
