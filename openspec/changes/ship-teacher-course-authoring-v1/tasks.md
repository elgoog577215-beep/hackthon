## 1. 范围冻结与基线确认（8 月 4 日至 8 月 6 日）

- [ ] 1.1 对照本变更、现有正式 specs 与全部活动 OpenSpec，建立功能归属矩阵并消除重复实现项
- [ ] 1.2 将 `upgrade-teaching-plan-workbench` 标记为教案编辑能力的实现依赖，明确其保存、版本、发布和回滚的交付接口
- [ ] 1.3 冻结 V1 范围，确认学生个性化、数字人、通用视频、TTS、任意代码动画、PPT 深度生成和文件管理不进入 9 月 1 日发布
- [ ] 1.4 盘点现有 `CourseDocument`、生成任务、事件流、课程命令、版本、质量检查和发布链的代码入口与测试覆盖
- [ ] 1.5 选定至少三类代表性课程，建立固定输入、资料引用、预期结构和异常样例组成的验收数据集
- [ ] 1.6 建立 12 小节真实模型基准集，固定模型、prompt、参数、资料版本和统计口径
- [ ] 1.7 确认试点教师、试点课程、发布负责人、回滚负责人和 P0 问题裁决人
- [ ] 1.8 定义教师工作台总开关、分能力开关、教师白名单和课程白名单命名及默认关闭策略
- [ ] 1.9 运行现有后端测试、前端 lint、类型检查、构建和核心课程生成 smoke，保存可比较的基线结果
- [ ] 1.10 完成 G0 范围门：功能归属、验收集、负责人、开关和基线结果均可追溯后才进入合同开发

## 2. 领域合同与兼容迁移（8 月 5 日至 8 月 9 日）

- [x] 2.1 定义并实现版本化 `TeacherCourseBriefV1` 模型、枚举、字段上限和组合校验
- [x] 2.2 定义并实现稳定 `ResourceRef` 合同，只保留资源标识、版本、显示信息和解析状态
- [ ] 2.3 定义大纲节点命令、基础修订号、幂等键、停用墓碑和冲突响应合同
- [x] 2.4 扩展 `CourseTeachingPlanV3` 的课程级课堂字段与小节级课堂字段
- [ ] 2.5 定义教案模块的新增、更新、移动、拆分、合并和停用命令合同
- [ ] 2.6 定义 `CourseContentDraft`、正文块操作、草稿状态和应用结果合同
- [ ] 2.7 定义带来源、作用范围、模型版本和状态的 AI 候选合同
- [ ] 2.8 定义影响分析五态、依赖边类型、局部重生成计划和 last-good 应用合同
- [ ] 2.9 定义四种动画模板的注册表、参数 schema、控制能力和降级内容合同
- [ ] 2.10 为新合同增加序列化、反序列化、边界值、非法输入和向后兼容单元测试
- [ ] 2.11 为旧课程和旧教案实现只读兼容与显式升级路径，确保迁移可重复、可审计
- [ ] 2.12 完成 G1 合同门：前后端共享 schema、迁移测试和兼容测试全部通过后才开放写入

## 3. 结构化课程创建与资料引用（8 月 8 日至 8 月 12 日）

- [ ] 3.1 扩展课程生成请求与持久化层，保存简报版本、教师身份、幂等键和资料引用快照
- [x] 3.2 实现简报服务端字段校验、组合约束校验与可定位错误响应
- [x] 3.3 将简报确定性映射到现有课程生成链，避免为相同阶段新增重复模型调用
- [ ] 3.4 接入已有资源查询或解析接口，并为资源服务不可用实现移除、重试和无资料继续策略
- [x] 3.5 实现课程创建表单、课时与结构约束反馈、资料选择和生成前确认界面
- [ ] 3.6 为课程创建表单增加草稿保留和重复提交幂等处理
- [ ] 3.7 实现创建成功后进入同一 `course_id` 教师工作台的连续导航
- [ ] 3.8 增加合法简报、非法简报、资料失效、重复提交和旧客户端请求的 API 集成测试
- [ ] 3.9 增加中英文课程创建流程浏览器测试并验证移动端可完成核心字段填写

## 4. 教学大纲结构编辑（8 月 10 日至 8 月 15 日）

- [ ] 4.1 在课程命令服务实现大纲节点新增、修改、移动、拆分、合并和停用操作
- [ ] 4.2 实现稳定节点标识、同级排序、层级合法性、循环引用和最小结构校验
- [ ] 4.3 实现基于基础修订号的乐观并发控制及包含当前值的冲突响应
- [ ] 4.4 实现命令幂等存储，验证重放请求不会产生重复节点或重复修订
- [ ] 4.5 实现停用节点墓碑及历史引用解析，禁止物理删除破坏版本链
- [ ] 4.6 实现大纲树编辑界面的行内修改、菜单操作、拆分与合并确认
- [ ] 4.7 实现拖拽排序及等价的键盘和菜单移动操作，并保持焦点与状态提示
- [ ] 4.8 在提交前显示预计影响对象，在提交后显示实际修订与受影响范围
- [ ] 4.9 增加每种结构命令、跨章节移动、并发冲突、命令重放和历史读取测试
- [ ] 4.10 增加教师从大纲修改到版本历史查看的端到端浏览器测试

## 5. 课堂教案工作台完善（8 月 12 日至 8 月 18 日）

- [x] 5.1 将新课堂字段接入既有教案草稿、AI 候选、保存、版本和发布模型
- [ ] 5.2 为旧教案补充兼容默认值和升级提示，保留来源修订及迁移审计
- [x] 5.3 实现课程级学期、课时、教学场景、学情、课前准备和评价方案编辑
- [x] 5.4 实现小节级时长、重难点、师生活动、资料、检查、作业和备注编辑
- [ ] 5.5 实现教案模块新增、移动、拆分、合并和停用，并同步课时统计
- [ ] 5.6 实现总课时与小节时长的软提醒、硬冲突规则和发布定位入口
- [ ] 5.7 将大纲节点与教案小节建立稳定映射，处理新增、拆分、合并和停用后的教案状态
- [ ] 5.8 实现教案字段级 AI 候选比较、接受、拒绝和撤销，不允许直接覆盖草稿
- [ ] 5.9 增加课堂合同、课时一致性、版本、发布、恢复和大纲联动集成测试
- [ ] 5.10 增加教师完成一节真实课堂教案的中英文端到端测试
- [ ] 5.11 完成 G2 可编辑门：大纲与教案的关键对象均可增删改排、可保存、可恢复且不存在只读死角

## 6. 课程正文草稿与块级 AI 编辑（8 月 15 日至 8 月 21 日）

- [ ] 6.1 实现按课程与教师隔离的正文草稿存储、读取、状态和过期清理策略
- [ ] 6.2 实现 650 毫秒自动保存、显式保存、离开保护和保存失败恢复
- [ ] 6.3 在课程命令服务实现文本范围修改、块替换、插入、停用和排序命令
- [ ] 6.4 实现草稿应用时的基础修订校验、冲突比较、重放和放弃流程
- [ ] 6.5 实现正文块编辑、插入、停用、排序、预览和撤销界面
- [ ] 6.6 实现选区或单块 AI 改写请求，保存候选来源、范围、模型和 prompt 版本
- [ ] 6.7 实现候选差异比较、接受、拒绝、再次生成和失败重试界面
- [ ] 6.8 保证 AI 超时、非法结构或取消请求不会改变草稿和正式课程修订
- [ ] 6.9 增加草稿隔离、自动保存、并发冲突、命令幂等和 AI 候选生命周期测试
- [ ] 6.10 增加从正文修改到预览新修订的端到端浏览器测试

## 7. 确定性影响分析、局部重生成与恢复（8 月 18 日至 8 月 23 日）

- [ ] 7.1 建立大纲、教案、正文、引用、质量报告、动画和发布对象之间的依赖索引
- [ ] 7.2 为每种领域命令实现确定性影响规则并输出五态影响集合
- [ ] 7.3 增加相同输入重复计算、无关字段修改和跨节点依赖的性质测试
- [ ] 7.4 实现教师确认前的影响预览与命令应用后的实际影响对比
- [ ] 7.5 实现只包含 `needs_regeneration` 对象的局部重生成计划与稳定任务键
- [ ] 7.6 复用现有生成任务和事件流创建局部任务，不新增第二套任务状态机
- [ ] 7.7 实现临时结果结构检查、质量检查和通过后的原子应用
- [ ] 7.8 实现 last-good 修订保留、失败小节隔离和仅重试失败范围
- [ ] 7.9 实现服务重启后 30 秒内扫描并恢复未完成任务的检查点流程
- [ ] 7.10 验证已完成有效单元在重试、恢复和重复请求中不会重复执行或计费
- [ ] 7.11 增加单小节失败、模型超时、事件流中断、服务重启和原子应用失败的故障注入测试
- [ ] 7.12 完成 G3 韧性门：局部修改不会触发全课重做，失败不会破坏 last-good 版本

## 8. 质量门禁与生成质量提升（8 月 20 日至 8 月 26 日）

- [ ] 8.1 盘点现有课程与教案质量规则，删除重复规则并确定唯一规则标识与版本
- [ ] 8.2 实现结构完整性、引用有效性、修订一致性、课时硬冲突和核心内容可呈现硬检查
- [ ] 8.3 实现目标可检查性、活动丰富度、教学法匹配度和表达质量教师建议
- [ ] 8.4 为每条结果返回对象定位、证据、严重级别、规则版本和修复入口
- [ ] 8.5 保证只有确定性硬错误阻止发布，建议分数不得单独阻止发布
- [ ] 8.6 建立覆盖三类代表性课程的专家标注样本与常见失败样本
- [ ] 8.7 使用固定样本评估 prompt 或模型变更的结构通过率、建议命中率和误报率
- [ ] 8.8 为高频失败增加修复 prompt、结构化重试或确定性后处理，并记录采用理由
- [ ] 8.9 增加规则版本、历史结果不变性、定位链接和发布门禁集成测试
- [ ] 8.10 完成 G4 质量门：基准集无未解释硬错误，建议可理解且不会造成误阻塞

## 9. 受控教学动画（8 月 21 日至 8 月 27 日）

- [ ] 9.1 建立四种 V1 动画模板注册表并拒绝未注册类型与任意脚本
- [ ] 9.2 实现 `process_flow_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.3 实现 `timeline_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.4 实现 `state_transition_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.5 实现 `math_algorithm_steps_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.6 实现暂停、重播、进度控制、键盘控制、减少动态效果和无障碍说明
- [ ] 9.7 实现教师选择模板、编辑参数、预览、接受、替换和移除动画的工作流
- [ ] 9.8 将动画失败隔离到单个内容块并自动展示文本、步骤或静态内容
- [ ] 9.9 增加恶意参数、越权 URL、脚本注入、离线加载和低性能设备测试
- [ ] 9.10 完成 G5 媒体门：动画失败导致核心课程内容不可读的测试比例为 0%

## 10. 教师工作台整合、i18n 与可用性（8 月 22 日至 8 月 28 日）

- [ ] 10.1 在同一课程页面整合简报、大纲、教案、正文、检查、预览、版本和发布入口
- [ ] 10.2 设计并实现跨模块未保存、生成中、过期、阻塞和已发布状态的统一提示
- [ ] 10.3 实现教师可从质量问题或影响项直接定位到对应字段、节点或正文块
- [ ] 10.4 实现长任务进度、当前步骤、局部失败、取消和继续编辑能力
- [x] 10.5 在现有中英文 locale 字典补齐全部新增标题、按钮、表单、状态、错误和无障碍文案
- [ ] 10.6 检查英文模式无写死中文、原始翻译 key、截断、遮挡和布局破坏
- [ ] 10.7 完成键盘导航、焦点管理、语义标签、颜色对比和非拖拽等价操作检查
- [ ] 10.8 在桌面、窄屏和移动视口验证创建、编辑、预览与发布核心流程
- [ ] 10.9 运行前端 lint、类型检查、单元测试和生产构建并清除新增错误
- [ ] 10.10 使用浏览器截图与交互检查验证关键页面无重叠、不可达控件和状态跳动
- [x] 10.11 将“课程上下文”接为课程基础信息查看、编辑、差异确认与历史恢复入口，不混入大纲、课次和下游产物状态

## 11. 遥测、性能、安全与运维控制（8 月 24 日至 8 月 29 日）

- [ ] 11.1 为课程创建、生成步骤、草稿保存、命令、重生成、质量、动画和发布定义结构化事件
- [ ] 11.2 实现贯穿前端请求、任务、模型调用、课程修订和发布的关联标识
- [ ] 11.3 实现提示与资料相关遥测的脱敏、摘要和留存策略，禁止记录密钥和完整私有正文
- [ ] 11.4 建立成功率、耗时、重试、恢复、重复单元、硬错误和动画降级仪表板
- [ ] 11.5 实现总开关、分能力开关、白名单、只读降级和紧急停止新写入能力
- [ ] 11.6 验证关闭新能力后已有课程、修订、草稿和发布内容仍可读取且不被删除
- [ ] 11.7 对草稿保存、块级 AI、影响分析、局部重生成和发布接口执行负载测试
- [ ] 11.8 将草稿保存 P95 优化到 1 秒内，将单块 AI 候选 P95 优化到 90 秒内
- [ ] 11.9 执行鉴权越权、租户隔离、资源引用权限、脚本注入和输入上限安全测试
- [ ] 11.10 完成生产候选环境部署、备份、监控告警和回滚演练

## 12. 真实模型基准、教师试点与 9 月 1 日发布（8 月 27 日至 9 月 1 日）

- [ ] 12.1 在生产候选环境使用拟上线模型执行至少 20 次独立的 12 小节完整生成
- [ ] 12.2 核算任务成功率不低于 95%、首次大纲可见 P95 不超过 60 秒、最终审阅状态 P95 不超过 10 分钟
- [ ] 12.3 核验已完成单元重复执行率为 0%，媒体失败导致核心内容不可读比例为 0%
- [ ] 12.4 执行模型超时、限流、非法结构、资料失败、网络中断、服务重启和数据库冲突失败矩阵
- [ ] 12.5 由试点教师完成简报、资料、大纲、教案、正文、局部重生成、动画、检查、发布和恢复全流程
- [ ] 12.6 记录各教师任务完成率、耗时、阻塞点、内容质量反馈和是否需要人工协助
- [ ] 12.7 修复全部 P0 问题，并将剩余问题明确归类为灰度跟进或后续版本
- [ ] 12.8 复跑后端测试、前端 lint、类型检查、生产构建、端到端测试和 OpenSpec 全量校验
- [ ] 12.9 完成发布评审，逐项签署功能、数据、质量、性能、安全、运维和回滚门禁
- [ ] 12.10 8 月 31 日保持默认关闭并完成最终备份、告警和回滚确认
- [ ] 12.11 9 月 1 日按教师或课程白名单灰度启用并监控首批真实任务
- [ ] 12.12 指标稳定且无 P0 故障后逐步扩大范围，任一硬门失败时立即回到只读或关闭状态

## 13. 收束、事实源更新与 OpenSpec 归档（发布稳定后）

- [ ] 13.1 将实际交付状态、能力边界和后续事项回写现有产品蓝图或正式项目状态载体，不创建第二份产品全景
- [ ] 13.2 更新教师端操作、质量规则、开关、告警、回滚和故障处理文档
- [ ] 13.3 记录真实模型基准、教师试点结论、已接受风险和发布决策
- [ ] 13.4 将仍有效的产品结论更新到 `docs/产品蓝图.md`，代码边界更新到 `docs/系统架构.md`，高代价重复错误更新到 `AGENTS.md` 并增加自动化保护
- [ ] 13.5 对照本 spec 的全部场景完成最终验收并关闭所有未解释差异
- [ ] 13.6 运行 `openspec validate ship-teacher-course-authoring-v1 --strict` 与 `openspec validate --all`
- [ ] 13.7 在实现、验证和发布均完成后执行 `openspec archive ship-teacher-course-authoring-v1`
- [ ] 13.8 归档后再次运行 `openspec validate --all` 并确认正式 specs 与真实系统一致
