## 1. 范围冻结与基线确认（8 月 4 日至 8 月 6 日）

- [ ] 1.1 对照本变更、现有正式 specs 与全部活动 OpenSpec，建立功能归属矩阵并消除重复实现项
- [ ] 1.2 将 `upgrade-teaching-plan-workbench` 标记为教案编辑能力的实现依赖，明确其保存、版本、发布和回滚的交付接口
- [ ] 1.3 冻结 V1 范围，确认学生个性化、数字人、通用视频、TTS、任意代码动画、PPT 深度生成和文件管理不进入 9 月 1 日发布
- [ ] 1.4 盘点现有 `CourseDocument`、生成任务、事件流、课程命令、版本、质量检查和发布链的代码入口与测试覆盖
- [ ] 1.5 选定至少三类代表性课程，建立固定输入、资料引用、预期结构和异常样例组成的验收数据集
- [ ] 1.6 建立 12 小节真实模型基准集，固定模型、prompt、参数、资料版本和统计口径
- [ ] 1.7 确认试点教师、试点课程、发布负责人、回滚负责人和 P0 问题裁决人
- [ ] 1.8 定义教师工作台总开关、分能力开关、教师白名单和课程白名单命名及默认关闭策略
- [ ] 1.9 运行现有后端测试、前端 lint、类型检查、构建和核心课程生成 smoke，保存可比较的基线结果
- [ ] 1.10 完成 G0 范围门：功能归属、验收集、负责人、开关和基线结果均可追溯后才进入合同开发

## 2. 领域合同与兼容迁移（8 月 5 日至 8 月 9 日）

- [x] 2.1 定义并实现版本化 `TeacherCourseBriefV1` 模型、枚举、字段上限和组合校验
- [x] 2.2 定义并实现稳定 `ResourceRef` 合同，只保留资源标识、版本、显示信息和解析状态
- [ ] 2.3 定义大纲节点命令、基础修订号、幂等键、停用墓碑和冲突响应合同
- [x] 2.4 扩展 `CourseTeachingPlanV3` 的课程级课堂字段与小节级课堂字段
- [ ] 2.5 定义教案模块的新增、更新、移动、拆分、合并和停用命令合同
- [ ] 2.6 定义 `CourseContentDraft`、正文块操作、草稿状态和应用结果合同
- [ ] 2.7 定义带来源、作用范围、模型版本和状态的 AI 候选合同
- [ ] 2.8 定义影响分析五态、依赖边类型、局部重生成计划和 last-good 应用合同
- [ ] 2.9 定义四种动画模板的注册表、参数 schema、控制能力和降级内容合同
- [ ] 2.10 为新合同增加序列化、反序列化、边界值、非法输入和向后兼容单元测试
- [ ] 2.11 为旧课程和旧教案实现只读兼容与显式升级路径，确保迁移可重复、可审计
- [x] 2.12 定义并实现保留教师原话、允许动态重规划的 `CourseChangeIntentV1 / CourseChangePlanV1`
- [x] 2.13 定义并测试结构操作、六类逐单元迁移结论、保护约束、结构确认点和场景覆盖矩阵
- [ ] 2.14 完成 G1 合同门：前后端共享 schema、迁移测试和兼容测试全部通过后才开放写入

## 3. 结构化课程创建与资料引用（8 月 8 日至 8 月 12 日）

- [ ] 3.1 扩展课程生成请求与持久化层，保存简报版本、教师身份、幂等键和资料引用快照
- [x] 3.2 实现简报服务端字段校验、组合约束校验与可定位错误响应
- [x] 3.3 将简报确定性映射到现有课程生成链，避免为相同阶段新增重复模型调用
- [ ] 3.4 接入已有资源查询或解析接口，并为资源服务不可用实现移除、重试和无资料继续策略
- [x] 3.5 实现课程创建表单、课时与结构约束反馈、资料选择和生成前确认界面
- [ ] 3.6 为课程创建表单增加草稿保留和重复提交幂等处理
- [ ] 3.7 实现创建成功后进入同一 `course_id` 教师工作台的连续导航
- [ ] 3.8 增加合法简报、非法简报、资料失效、重复提交和旧客户端请求的 API 集成测试
- [ ] 3.9 增加中英文课程创建流程浏览器测试并验证移动端可完成核心字段填写
- [x] 3.10 在现有资料解析管线补齐旧 `.doc/.ppt/.xls` 临时转码、扫描 PDF 本地 OCR、PPTX 图表/备注/对象位置证据及显式降级测试，原件和 `ParsedDocument` 真源边界不变
- [x] 3.11 按浙大学期规则自动确定 8/16 个教学周，保留特殊课程自定义周次，并统一建课、课程信息、大纲预览与正式导出

## 4. 教学大纲结构编辑（8 月 10 日至 8 月 15 日）

- [ ] 4.1 在课程命令服务实现大纲节点新增、修改、移动、拆分、合并和停用操作
- [ ] 4.2 实现稳定节点标识、同级排序、层级合法性、循环引用和最小结构校验
- [ ] 4.3 实现基于基础修订号的乐观并发控制及包含当前值的冲突响应
- [ ] 4.4 实现命令幂等存储，验证重放请求不会产生重复节点或重复修订
- [ ] 4.5 实现停用节点墓碑及历史引用解析，禁止物理删除破坏版本链
- [ ] 4.6 实现大纲树编辑界面的行内修改、菜单操作、拆分与合并确认
- [ ] 4.7 实现拖拽排序及等价的键盘和菜单移动操作，并保持焦点与状态提示
- [ ] 4.8 在提交前显示预计影响对象，在提交后显示实际修订与受影响范围
- [ ] 4.9 增加每种结构命令、跨章节移动、并发冲突、命令重放和历史读取测试
- [ ] 4.10 增加教师从大纲修改到版本历史查看的端到端浏览器测试

## 5. 课堂教案工作台完善（8 月 12 日至 8 月 18 日）

- [x] 5.1 将新课堂字段接入既有教案草稿、AI 候选、保存、版本和发布模型
- [ ] 5.2 为旧教案补充兼容默认值和升级提示，保留来源修订及迁移审计
- [x] 5.3 实现课程级学期、课时、教学场景、学情、课前准备和评价方案编辑
- [x] 5.4 实现小节级时长、重难点、师生活动、资料、检查、作业和备注编辑
- [ ] 5.5 实现教案模块新增、移动、拆分、合并和停用，并同步课时统计
- [ ] 5.6 实现总课时与小节时长的软提醒、硬冲突规则和发布定位入口
- [ ] 5.7 将大纲节点与教案小节建立稳定映射，处理新增、拆分、合并和停用后的教案状态
- [ ] 5.8 实现教案字段级 AI 候选比较、接受、拒绝和撤销，不允许直接覆盖草稿
- [ ] 5.9 增加课堂合同、课时一致性、版本、发布、恢复和大纲联动集成测试
- [ ] 5.10 增加教师完成一节真实课堂教案的中英文端到端测试
- [ ] 5.11 完成 G2 可编辑门：大纲与教案的关键对象均可增删改排、可保存、可恢复且不存在只读死角
- [x] 5.12 将确认编排设为分讲教案模块身份、顺序和分钟数的权威输入，生成、AI 候选与手工保存共用对齐函数和本讲总时长硬门
- [x] 5.13 为模型漏节、漏模块和总时长漂移增加回归测试，验证 45 分钟编排不会被生成结果改写
- [x] 5.14 将已有教案设为中栏第一主体，生成依据后置并默认收起；阅读与导出合并师生活动和达成信息，完整字段只在编辑与质检中保留

## 6. 课程正文草稿与块级 AI 编辑（8 月 15 日至 8 月 21 日）

- [ ] 6.1 实现按课程与教师隔离的正文草稿存储、读取、状态和过期清理策略
- [ ] 6.2 实现 650 毫秒自动保存、显式保存、离开保护和保存失败恢复
- [ ] 6.3 在课程命令服务实现文本范围修改、块替换、插入、停用和排序命令
- [ ] 6.4 实现草稿应用时的基础修订校验、冲突比较、重放和放弃流程
- [ ] 6.5 实现正文块编辑、插入、停用、排序、预览和撤销界面
- [ ] 6.6 实现选区或单块 AI 改写请求，保存候选来源、范围、模型和 prompt 版本
- [ ] 6.7 实现候选差异比较、接受、拒绝、再次生成和失败重试界面
- [ ] 6.8 保证 AI 超时、非法结构或取消请求不会改变草稿和正式课程修订
- [ ] 6.9 增加草稿隔离、自动保存、并发冲突、命令幂等和 AI 候选生命周期测试
- [ ] 6.10 增加从正文修改到预览新修订的端到端浏览器测试

## 7. AI 影响检索、结构迁移、约束式重生成与恢复（8 月 18 日至 8 月 23 日）

- [ ] 7.1 在生成与发布时建立覆盖大纲、教案、题库、讲稿和 V6 PPT 单元的 `CourseChangeIndexV1`
- [ ] 7.2 实现关键词、实体、embedding、结构路径和显式关系的多路高召回与 Rank，不把任一索引结果直接当正式影响
- [ ] 7.3 实现 AI 精读候选、发现新线索后递归扩展、覆盖度检查及低置信度扩大扫描
- [ ] 7.4 为结构领域命令实现稳定身份和直接结构影响规则，并输出五态集合与可解释路径
- [ ] 7.5 实现从临时语义发现动态升级到结构或混合策略的重规划，保留老师原话、假设和旧计划历史
- [ ] 7.6 实现 `CurrentCourseGraph -> ProposedCourseGraph -> IdentityMapping`，覆盖新增、移动、排序、拆分、合并、退休和整课重构
- [ ] 7.7 为大纲、教案、题目、讲稿块和 PPT 页面生成 `reuse_exact / reuse_rebind / rewrite_partial / regenerate / retire / blocked` 迁移结论
- [ ] 7.8 校验保护要求、层级无环、课时、先修、目标、知识覆盖和来源修订；冲突时阻断而不是强行迁移
- [ ] 7.9 实现教师确认结构前的迁移预览，确认后才按新结构拓扑并行生成下游候选
- [ ] 7.10 实现教师确认前的预计影响与命令应用后的实际影响对比，明确保持不变和 last-good 对象
- [ ] 7.11 为内容变化创建只处理 AI 确认影响单元的稳定任务键；大范围结构重构允许按真实迁移结论大量重生成
- [ ] 7.12 复用现有生成任务、检查点和事件流，不新增第二套任务状态机或领域写入链
- [ ] 7.13 实现临时结果结构检查、质量检查和通过后的版本组应用
- [ ] 7.14 实现 last-good 保留、失败单元隔离、仅重试失败范围和被取代计划历史读取
- [ ] 7.15 实现服务重启后 30 秒内扫描并恢复未完成任务，已完成有效单元不得重复执行或计费
- [ ] 7.16 增加版本更新、目标变化、移动、拆分、合并、退休、整课重构和模糊表达的固定测试集
- [ ] 7.17 增加模型超时、召回遗漏、索引过期、结构冲突、事件流中断、服务重启和版本组应用失败的故障注入测试
- [ ] 7.18 完成 G3 韧性门：系统不因追求复用阻止必要重生成，也不因实现简单覆盖可复用教师资产；失败不得破坏 last-good
- [x] 7.19 增加不拥有正文的 `TeacherCourseChangeContext` 只读投影，聚合正式课程、教师大纲工作区、教案、讲稿、V6 PPT 页面和题库当前修订，并返回 available / partial / stale / missing 就绪状态
- [x] 7.20 增加独立课程级 `/course-context`、`/course-plans` 与影响范围审阅接口；全课请求不再伪造预览小节 ID，重复 `request_id` 不生成平行计划
- [x] 7.21 实现本地跨资产 Rank 后的 AI 判断、AI 不可用时的显式索引回退，以及预计影响中 `formal_content_changed=false` 的真值边界；完整递归扩展、embedding 和领域候选执行仍由 7.2、7.3、7.9 至 7.15 继续跟踪
- [x] 7.22 固化教师确认的大纲章节/小节规模到同一任务恢复合同，讲稿公式格式只修复可唯一判断的当前块并保留既有检查点
- [x] 7.23 失败任务恢复前从隔离工作区还原完整请求，教师确认骨架不受派生指纹漂移影响；提供方认证/额度锁定改为可到期的有界冷却
- [x] 7.24 讲稿提供方失败时从已确认教案编译显式警告的可编辑基线；PPT 故事/视觉降级只能进入待教师确认文书，禁止绕过文书直接发布
- [x] 7.25 将教师普通内容要求编译为经过当前正式字段命中校验的精确 `REPLACE_COURSE_BLOCK` 候选，逐项排除后通过现有操作组应用并可整次撤销
- [x] 7.26 将完整目标课程树、旧新身份来源和显式退休节点编译为单个 `REBUILD_COURSE_OUTLINE` 操作，在同一修订中完成排序、合并、拆分归属、退休与撤销恢复
- [x] 7.27 在现有四阶段工作台展示候选前后差异、可应用数量、逐项排除和 applied / failed / unchanged 精确回执；未生成正式候选的下游资产不得被误报为已更新
- [x] 7.27a 将明确“A 统一替换为 B”的要求在正式课程字段中确定性编译为同一 `REPLACE_COURSE_BLOCK` 候选；零命中返回范围澄清，不依赖模型失败回退生成假候选
- [ ] 7.28 将教案、题库、讲稿和 PPT 的真实候选执行器接入同一操作组与 last-good 恢复，并只重试失败单元；当前只完成正式课程正文和课程结构写入

## 8. 质量门禁与生成质量提升（8 月 20 日至 8 月 26 日）

- [ ] 8.1 盘点现有课程与教案质量规则，删除重复规则并确定唯一规则标识与版本
- [ ] 8.2 实现结构完整性、引用有效性、修订一致性、课时硬冲突和核心内容可呈现硬检查
- [ ] 8.3 实现目标可检查性、活动丰富度、教学法匹配度和表达质量教师建议
- [ ] 8.4 为每条结果返回对象定位、证据、严重级别、规则版本和修复入口
- [ ] 8.5 保证只有确定性硬错误阻止发布，建议分数不得单独阻止发布
- [ ] 8.6 建立覆盖三类代表性课程的专家标注样本与常见失败样本
- [ ] 8.7 使用固定样本评估 prompt 或模型变更的结构通过率、建议命中率和误报率
- [ ] 8.8 为高频失败增加修复 prompt、结构化重试或确定性后处理，并记录采用理由
- [ ] 8.9 增加规则版本、历史结果不变性、定位链接和发布门禁集成测试
- [ ] 8.10 完成 G4 质量门：基准集无未解释硬错误，建议可理解且不会造成误阻塞
- [x] 8.11 为教学大纲增加确定性整篇审读，识别目标与达成检验模板化、重复标题和标题过载，并返回规则版本、节点定位与修复指令
- [x] 8.12 默认以正式大纲文档呈现结构化真源，保留显式结构视图与教师确认后的定点 AI 候选修复
- [x] 8.13 旧版已为首次大纲生成增加课程定位、教学组织和能力重点预设；该界面已经由 `split-teacher-lesson-authoring-v1` 第 11 节迁移为学习目的、一级学科与课程教学类型，本讲课型继续归当前讲管理
- [x] 8.14 为 PPT 文书增加模型请求预检压缩、显式讲稿块标题、稳定页面组和代表性安全分页方案；完整合同继续执行来源、标题、分页与质量校验
- [x] 8.15 代表性分页改为均衡完整分区，台上投影移除生产/授课指令，标题硬门拒绝“给出、提供、重点突出”等内部语言，导出通过来源区域与文本溢出对象审计
- [x] 8.16 将 AI 故事摘要设为学生画布唯一精简投影，禁止完整讲稿在 artifact/text 续页中二次回填；整讲补齐开场、学习路径和收束硬门，并保证文书冻结主题贯穿最终导出
- [x] 8.17 将题库概念、目标、掌握三个槽位编译为单一主要目标、动作数、推理步数和题干长度预算；单题只消费当前槽位投影，三题共同覆盖讲次
- [x] 8.18 课程兼容准备与逐题模型活动续写同一个题库重建任务心跳，质量失败保持当前有效题库且不启动平行生成器
- [x] 8.19 按讲大纲直接生成并全链保留学生产出、判断标准和本讲范围；取消正常链路的统一空泛补写，增加 UI 设计专业画像和 16 讲回归样例

## 9. 受控教学动画（8 月 21 日至 8 月 27 日）

- [ ] 9.1 建立四种 V1 动画模板注册表并拒绝未注册类型与任意脚本
- [ ] 9.2 实现 `process_flow_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.3 实现 `timeline_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.4 实现 `state_transition_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.5 实现 `math_algorithm_steps_v1` 参数校验、渲染器、静态降级和测试
- [ ] 9.6 实现暂停、重播、进度控制、键盘控制、减少动态效果和无障碍说明
- [ ] 9.7 实现教师选择模板、编辑参数、预览、接受、替换和移除动画的工作流
- [ ] 9.8 将动画失败隔离到单个内容块并自动展示文本、步骤或静态内容
- [ ] 9.9 增加恶意参数、越权 URL、脚本注入、离线加载和低性能设备测试
- [ ] 9.10 完成 G5 媒体门：动画失败导致核心课程内容不可读的测试比例为 0%

## 10. 教师工作台整合、i18n 与可用性（8 月 22 日至 8 月 28 日）

- [ ] 10.1 在同一课程页面整合简报、大纲、教案、正文、检查、预览、版本和发布入口
- [ ] 10.2 设计并实现跨模块未保存、生成中、过期、阻塞和已发布状态的统一提示
- [ ] 10.3 实现教师可从质量问题或影响项直接定位到对应字段、节点或正文块
- [ ] 10.4 实现长任务进度、当前步骤、局部失败、取消和继续编辑能力
- [x] 10.5 在现有中英文 locale 字典补齐全部新增标题、按钮、表单、状态、错误和无障碍文案
- [ ] 10.6 检查英文模式无写死中文、原始翻译 key、截断、遮挡和布局破坏
- [ ] 10.7 完成键盘导航、焦点管理、语义标签、颜色对比和非拖拽等价操作检查
- [ ] 10.8 在桌面、窄屏和移动视口验证创建、编辑、预览与发布核心流程
- [ ] 10.9 运行前端 lint、类型检查、单元测试和生产构建并清除新增错误
- [x] 10.10 使用浏览器截图与交互检查验证本次课程工作台、全课 AI 修改和 PPT 工作台关键状态无重叠、不可达控件和状态跳动
- [x] 10.11 将“课程上下文”接为课程基础信息查看、编辑、差异确认与历史恢复入口，不混入大纲、课次和下游产物状态
- [x] 10.12 将现有“调整课程”独立工作区改为请求、理解、扫描、内容审阅、结构审阅、应用回执六态自适应布局，并用“说出要求—分析影响—审阅修改—应用完成”四阶段流程及贯通上下文条保持全程心智一致；空态不铺多栏、不重复解释系统规则，教师端不混入个人学习地图和学习证据轨迹
- [x] 10.13 内容变化展示确定/可能影响、因果路径、原文差异和保持不变对象；聊天不重复铺陈完整影响列表
- [x] 10.14 结构变化展示新旧课程树、身份映射及复用、重绑、改写、重生成、退休、阻断数量，并提供结构确认点
- [x] 10.15 应用后展示实际回执、失败项、last-good、重试和整次撤销，不把预计影响冒充已完成结果
- [x] 10.16 实现外部 PPTX 不可变原件、页/文字块索引、上游修订向量和审阅报告合同
- [x] 10.17 实现“上传并审阅 / 根据讲稿生成”双入口，手工修订、AI 候选、确认与下载回执
- [ ] 10.18 用实际教师 PPTX 验收文字索引定位、报告误报、修订稿版式保真与上游过期提示
- [x] 10.19 将全课工作台接到课程级规划接口，首屏同时呈现自然语言入口、真实课程资产索引和最近修改；内容审阅支持按资产导航、来源摘录、原因、置信边界和逐项排除，课程源缺失不再误报为版本冲突
- [x] 10.20 将全课联动修改从课程页弹层迁为可恢复的课程级工作区；请求态收敛为单栏输入和紧凑课程准备状态，预计影响、候选和实际回执保持真值分层；结构变化增加“确认结构与迁移”检查点且不提前写入正式课程
- [x] 10.21 在无原版 PPT 的生成页明确呈现“PPT 文书 → PPT”两步；失败显示具体原因、追踪代码和唯一重试动作，文书未确认时第二步始终锁定，并按课时而非放大后的碎片数估算页数
- [x] 10.22 为题库失败讲次提供同范围重试入口；成功后按课程与合同一致性合并回原整课检查点，6/6 完成才结束整课任务
- [x] 10.23 删除课程工作台的整课发布入口、待发布状态与发布接口；学生视角预览改为直接读取教师当前课程投影，各产物继续由自身确认状态守门
- [x] 10.24 将全课调整工作区嵌入课程“审计与更新中心”，与材料上传/替换共用变化来源、影响审阅、历史和原位返回；旧材料审计与全课调整地址仅作兼容跳转，不再保留两套页面。

## 11. 遥测、性能、安全与运维控制（8 月 24 日至 8 月 29 日）

- [ ] 11.1 为课程创建、生成步骤、草稿保存、命令、重生成、质量、动画和发布定义结构化事件
- [ ] 11.2 实现贯穿前端请求、任务、模型调用、课程修订和发布的关联标识
- [ ] 11.3 实现提示与资料相关遥测的脱敏、摘要和留存策略，禁止记录密钥和完整私有正文
- [ ] 11.4 建立成功率、耗时、重试、恢复、重复单元、硬错误和动画降级仪表板
- [ ] 11.5 实现总开关、分能力开关、白名单、只读降级和紧急停止新写入能力
- [ ] 11.6 验证关闭新能力后已有课程、修订、草稿和发布内容仍可读取且不被删除
- [ ] 11.7 对草稿保存、块级 AI、影响分析、局部重生成和发布接口执行负载测试
- [ ] 11.8 将草稿保存 P95 优化到 1 秒内，将单块 AI 候选 P95 优化到 90 秒内
- [ ] 11.9 执行鉴权越权、租户隔离、资源引用权限、脚本注入和输入上限安全测试
- [ ] 11.10 完成生产候选环境部署、备份、监控告警和回滚演练

## 12. 真实模型基准、教师试点与 9 月 1 日发布（8 月 27 日至 9 月 1 日）

- [ ] 12.1 在生产候选环境使用拟上线模型执行至少 20 次独立的 12 小节完整生成
- [ ] 12.2 核算任务成功率不低于 95%、首次大纲可见 P95 不超过 60 秒、最终审阅状态 P95 不超过 10 分钟
- [ ] 12.3 核验已完成单元重复执行率为 0%，媒体失败导致核心内容不可读比例为 0%
- [ ] 12.4 执行模型超时、限流、非法结构、资料失败、网络中断、服务重启和数据库冲突失败矩阵
- [ ] 12.5 由试点教师完成简报、资料、大纲、教案、正文、局部重生成、动画、检查、发布和恢复全流程
- [ ] 12.6 记录各教师任务完成率、耗时、阻塞点、内容质量反馈和是否需要人工协助
- [ ] 12.7 修复全部 P0 问题，并将剩余问题明确归类为灰度跟进或后续版本
- [ ] 12.8 复跑后端测试、前端 lint、类型检查、生产构建、端到端测试和 OpenSpec 全量校验
- [ ] 12.9 完成发布评审，逐项签署功能、数据、质量、性能、安全、运维和回滚门禁
- [ ] 12.10 8 月 31 日保持默认关闭并完成最终备份、告警和回滚确认
- [ ] 12.11 9 月 1 日按教师或课程白名单灰度启用并监控首批真实任务
- [ ] 12.12 指标稳定且无 P0 故障后逐步扩大范围，任一硬门失败时立即回到只读或关闭状态

## 13. 收束、事实源更新与 OpenSpec 归档（发布稳定后）

- [ ] 13.1 将实际交付状态、能力边界和后续事项回写现有产品蓝图或正式项目状态载体，不创建第二份产品全景
- [ ] 13.2 更新教师端操作、质量规则、开关、告警、回滚和故障处理文档
- [ ] 13.3 记录真实模型基准、教师试点结论、已接受风险和发布决策
- [ ] 13.4 将仍有效的产品结论更新到 `docs/产品蓝图.md`，代码边界更新到 `docs/系统架构.md`，高代价重复错误更新到 `AGENTS.md` 并增加自动化保护
- [ ] 13.5 对照本 spec 的全部场景完成最终验收并关闭所有未解释差异
- [ ] 13.6 运行 `openspec validate ship-teacher-course-authoring-v1 --strict` 与 `openspec validate --all`
- [ ] 13.7 在实现、验证和发布均完成后执行 `openspec archive ship-teacher-course-authoring-v1`
- [ ] 13.8 归档后再次运行 `openspec validate --all` 并确认正式 specs 与真实系统一致

## 14. 教师备课开发看板对齐（8 月 30 日）

- [x] 14.1 将会议纪要、课程信息、大纲、教案附件与教师开发看板归入本变更，不建立平行规划文档。
- [x] 14.2 创建、编辑、查看共用课程信息合同，并将排课直接投影到教学日历。
- [x] 14.3 课程首页默认进入“我的课程”，课程库移除待处理状态、动作按钮和版本列，增加最后编辑与表头排序。
- [x] 14.4 课程级“备课完成”改为由确认大纲及全部计划讲的确认教案、讲义、PPT 推导。
- [x] 14.5 工作台主导航收束为大纲、教案、讲义、PPT 四阶段，题库移入其他课程文件。
- [x] 14.6 课程文件分类统一为教学大纲、分讲教案、讲义、PPT、其他课程文件、课程资料和回收站。
- [x] 14.7 右侧资料栏在生成前显示“资料文件 / 参考文件”上传命令和引导；生成开始后隐藏添加与移除入口，切换为真实使用来源、进度及暂停、继续、取消状态；生成后可核对实际来源。
- [x] 14.8 课程信息保留在右侧资料栏顶部，打开共用课程信息面板；不占用左侧阶段导航，不复制数据真源。
- [x] 14.9 教案页实现一次生成全部讲次的父任务与可独立恢复的讲次子任务。
- [x] 14.10 大纲和教案结构、生成约束、编辑和导出共用附件模板合同。
- [x] 14.11 大纲、教案、讲义、PPT 使用一致任务 ID 与暂停、继续、取消、重试、重新生成和安全清稿动作。
- [x] 14.12 保留并简化 AI 审阅为提出要求、查看影响、审阅候选、应用或撤销。
- [x] 14.13 所有课程内页面提供返回“我的课程”与首页入口，并保留返回上下文。
- [x] 14.14 完成 OpenSpec、前后端测试、视觉交互、生产构建、发布与线上验收。

## 15. 按讲课程模型与动态工作台修正（8 月 31 日）

- [x] 15.1 将每条已确认改动映射到领域定义、产品机制、交互设计和技术架构，并同步蓝图、架构、状态与本变更。
- [x] 15.2 新课程只写入 `lecture_v1`，由教师确认讲数；内部章/节仅作一讲一容器兼容，旧课程不迁移。
- [x] 15.3 建课与课程信息共用 45 分钟、有效周次、7×13 可视课表、地点与计划讲数合同。
- [x] 15.4 大纲一次生成全课讲次；确认后批量生成教案，每讲独立确认、失败和恢复。
- [x] 15.5 教案改为知识/能力/育人三类目标、正式外壳和学科化动态教学块，并阻止 AI 生成活动照片。
- [x] 15.6 保持既有三栏布局，左栏为大纲/教案/讲义/PPT，中栏为正式内容，右栏始终显示课程信息并按任务状态切换资料操作。
- [x] 15.7 补齐右栏生成、暂停、失败重试、审阅、确认状态和减少动态效果支持；保留全课 AI 审阅入口。
- [x] 15.8 更新相关后端与前端测试，运行类型检查、生产构建、OpenSpec 严格校验和桌面真实交互验收。
- [x] 15.9 仅提交本轮文件并推送 `main`，经既有生产发布链完成线上健康、关键页面、资源、授权、日志与回滚验收。
- [x] 15.10 将全部教案改为右栏一次整课启动、按讲串行执行；中栏只承载当前讲，移除逐讲生成与逐讲任务控制，只保留整课进度和内部恢复点。
- [x] 15.11 教案后台刷新按课程合并为单一请求；已有成功内容在刷新和超时时保持可见，读取接口不再因版本未变化而写盘或逐任务重复读取。
