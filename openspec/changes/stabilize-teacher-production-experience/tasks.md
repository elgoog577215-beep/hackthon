## 1. 恢复可信发布基线

- [x] 1.1 修复根目录两个旧测试对已删除 `task_manager` 的导入并验证 `backend/.venv/bin/python -m pytest tests` 可以完整收集和运行
- [x] 1.2 将 Ruff 固定到开发依赖并验证 README 中的 lint 命令在全新依赖环境可执行
- [x] 1.3 分类当前 `backend/tests` 失败为远端基线、PPT V6 在途改动或本 change 问题，修复本 change 所需基线并保存可复现命令与结果
- [x] 1.4 将后端两套 pytest、Ruff、前端 Vitest、构建和 OpenSpec strict validation 接入发布前置 job，验证任一失败都会在部署前停止

## 2. 建立唯一课程生产状态投影

- [x] 2.1 定义 `course_production_state_v1` 类型、四态映射和辅助状态合同，并用 schema/contract 测试锁定字段与枚举
- [x] 2.2 实现纯服务端投影编译器，读取现有大纲任务、教师资产任务、PPT 检查点、正式修订和 readiness，验证调用不产生任何仓储写入
- [x] 2.3 以当前正式讲次全集计算各阶段分母，把最新批次限制在 `latest_attempt`，新增“最新 0/1、整课 15/16”回归测试
- [x] 2.4 实现 last-good 优先级，验证“旧版可使用且最新重生成失败”同时返回 `available` 与失败 issue
- [x] 2.5 为 issue 返回稳定 `stage/lesson_unit_id/block_id/task_id/code` 和安全恢复信息，验证相同事实产生稳定 issue identity
- [x] 2.6 在课程列表、单课程读取和教学日历响应中并行返回新投影与旧兼容字段，记录低基数差异并验证活动任务没有被迁移、重启或改写
- [x] 2.7 将教师逐讲 PPT V6 页面内容稿与 PPT attempt 持久化为教师资产 job，让投影、刷新、暂停、取消、继续与失败重试使用同一真实 job ID，验证 last-good 不受失败 attempt 覆盖
- [x] 2.8 保留 `waiting_for_input / waiting_for_review / quality_blocked / unknown` 的真实控制语义，并由服务端投影显式返回允许动作；验证未知态、幽灵检查点和不可恢复质量阻断不会开放写操作

## 3. 收敛前端状态与生成操作

- [x] 3.1 增加新投影的前端类型和唯一读取适配器，采用“新字段优先、缺失时旧字段兜底”，验证刷新和 SSE 更新不会创建新任务
- [x] 3.2 将课程库改为只渲染服务端课程两态、阶段进度和 issue 深链，删除独立 `globalProductionTask/authoringProductionTask/courseProduction` 裁决并通过课程库生命周期测试
- [x] 3.3 将工作台阶段、讲次和操作状态迁到同一投影，保留 Store 的事件传输职责并删除最终展示状态的重复推导，通过流式、暂停、失败和 last-good 测试
- [x] 3.4 将教学日历纳入讲义并显示大纲、教案、讲义、PPT 四项准备度，验证教案可用但讲义缺失时下一步指向讲义
- [x] 3.5 将课程文件状态迁到同一投影，验证相同课程在课程库、工作台、日历和文件空间的阶段计数与问题完全一致
- [x] 3.6 实现“处理问题”深链，只导航到准确阶段/讲次/教学块并展开完整错误，使用网络断言证明没有 POST、PATCH、PUT 或 DELETE
- [x] 3.7 移除教案和讲义的可见“只生成本讲”入口，只保留“生成全部”；失败后原按钮改为“重新生成”，验证批量接口只恢复失败或缺失单元且不覆盖可用讲次
- [x] 3.8 更新中英文状态与动作映射，使资产主状态只显示“未生成 / 生成中 / 可使用 / 生成失败”，解析 locale JSON 并覆盖暂停、取消、过期、待核对和最近失败的辅助文案
- [x] 3.9 按确认方案将失败信息分为课程级数量摘要、正文局部状态与原位恢复、右栏完整原因与恢复说明；验证具体错误不再三处重复，并保持大纲完整度字号和紧凑图标尺寸不变
- [x] 3.10 将工作台及共享适配器改为只消费服务端允许动作，把等待输入、等待审阅、暂停恢复、质量修复和失败检查分别路由到准确命令；验证按钮出现即获得后端授权且未知态默认禁用

## 4. 补齐可用性、恢复和定向重建

- [x] 4.1 将大纲结构硬校验与非阻断审阅建议分别投影，验证零学时/学时不匹配显示“可使用 + 待修正”而不新增确认门
- [x] 4.2 返回可选来源待核对摘要和必需来源阻断原因，验证 AI 推荐待确认不阻断、主来源损坏或过期才阻断
- [x] 4.3 统一教案/讲义批量恢复入口，冻结原输入快照并验证 12 讲中 10 可用、1 暂停、1 失败时只恢复 2 讲
- [x] 4.4 通过现有共享 executor 接通教案与讲义块级定向重建，验证未受影响块与 last-good 修订不变
- [x] 4.5 在不覆盖 PPT V6 在途修改的前提下接通 V6 page ID 级影响和恢复，验证健康页面不重建且失败页面保留 last-good
- [x] 4.6 接通题库最小合法单元的定向重建，验证题组/共享材料等不可拆单位不会产生半份正式资产
- [x] 4.7 补齐跨讲结构操作 DAG、部分接受后的依赖重算、稳定 ID/墓碑/引用迁移、原子应用和撤销测试
- [x] 4.8 删除教案骨架和批次中的前置顺序阻断，验证同讲同节及跨节的反向 `prerequisite` 不再触发生成失败、未知端点仍被阻断

## 5. 加固任务与通用数据持久化

- [x] 5.1 将通用 `save_data` 改为临时文件、flush、fsync、原子替换且成功后才更新深拷贝缓存，验证 replace 失败时磁盘与缓存保留旧值
- [x] 5.2 新增跨进程安全的 `update_data(filename, updater)`，在锁内重新读盘并原子提交，使用两个进程并发写测试证明无丢失更新
- [x] 5.3 将 UsageEvent 去重、追加、裁剪和删除迁到 `update_data`，验证分析采集失败不会改变正式业务响应
- [x] 5.4 盘点其他通用读改写账本并按风险迁入统一入口，每迁移一个仓储都通过并发、失败和兼容读取测试，不做无证据的全仓机械替换
- [x] 5.5 将任务创建、暂停、恢复、取消、确认、失败和完成改为 copy-on-write 严格持久化，验证写盘失败不会对外确认新状态；高频进度继续 best-effort
- [x] 5.6 为任务索引增加可校验 last-good 和损坏/超限隔离恢复，验证双份无效时进入 degraded、课程仍可读且任务写操作被明确拒绝
- [x] 5.7 在读取和恢复任务前取得数据目录 leader lock，并把 consumer 启动移到加载/对账之后；使用双实例测试证明第二实例不执行或改写任务

## 6. 关闭跨资产崩溃窗口

- [x] 6.1 在现有 CourseEvolutionPlan 回执旁增加逐 operation journal，持久化 `pending/applying/applied/failed` 与结果修订并通过 schema 兼容测试
- [x] 6.2 在教案、讲义、PPT 和题库每项提交后立即写入 operation 结果，验证正常 partial 回执与现有撤销行为不变
- [x] 6.3 实现启动/重试时的正式修订对账，验证“第 N 项提交后进程中断”只补记或继续未完成项、不重放已成功操作
- [x] 6.4 在前端保留现有 partial 续办界面并改读 journal 汇编结果，验证 4 成功 1 失败时只重试失败 operation ID

## 7. 健康、发布、备份与观测

- [x] 7.1 保留 `/health` 轻量存活语义，为 `/api/health` 增加版本、leader、任务索引和数据目录探针，验证 degraded 时 liveness 成功而 readiness 失败
- [x] 7.2 保持真实文本模型调用只在发布探测执行，验证 readiness 不发模型请求且发布探测仍证明生产角色使用指定模型
- [x] 7.3 在发布脚本停止服务前检查活动任务，验证存在不可安全中断任务时发布退出且当前版本和任务继续运行
- [x] 7.4 为数据备份增加 manifest、checksum 和隔离临时目录恢复校验，验证演练不会覆盖生产数据
- [x] 7.5 增加任务等待、心跳超时、持久化失败、恢复结果、跨资产 partial 和模型错误分类的低基数指标，验证不记录正文、材料、提示词或身份敏感值
- [x] 7.6 在临时数据目录完成磁盘满、损坏索引、双实例、跨资产中断和 SSE 断开故障演练，并证明最后可用课程与任务恢复合同不受破坏

## 8. 历史课程复制升级

- [x] 8.1 实现历史课程升级只读预检和讲次映射报告，验证预检不写入旧课程或正式课程列表
- [x] 8.2 在隔离工作区编译新讲次式课程并通过当前 CourseDocument/LessonUnit 校验后发布为新 course ID，验证旧课程 checksum 不变且旧教案历史未导入
- [x] 8.3 实现升级失败清理和幂等重试，验证失败半成品不出现在课程列表且旧课程继续可读

## 9. 退场旧分支并完成验收

- [ ] 9.1 观察至少一个完整发布周期的新旧投影差异和单讲兼容调用，形成可复核报告；差异或外部调用未归零时不得删除兼容路径
- [ ] 9.2 在退场门通过后删除前端旧状态推导和后端重复聚合，停止旧字段写入但保留必要版本化读取，并验证全仓无新的平行状态来源
- [x] 9.3 同步 `PRODUCT.md`、产品蓝图、产品状态、系统架构和相关现有 OpenSpec 任务，明确唯一状态权威、批量生成入口、兼容退场与实际完成证据
- [x] 9.4 运行两套后端 pytest、Ruff、前端 Vitest、构建、locale 解析、相关 change 与全量 OpenSpec strict validation、`git diff --check`，逐项记录通过、原有失败和未运行项
- [x] 9.5 使用中文桌面端真实浏览器验证全新、生成中、暂停、取消、部分失败、不可重试、重新生成、刷新恢复、last-good、深链、来源待核对和日历四项状态；确认列宽、右栏、长大纲导航、包体与装饰样式未被改变
- [x] 9.6 在安全发布窗口完成生产备份、模型探测、健康检查和代表性课程只读验收；确认无活动任务被中断后再提交、推送并记录 SHA、分支、远端、部署与回滚证据

## 10. 全局修改与失败恢复反馈修复（2026-09-05）

- [x] 10.1 保留学生视角预览，修复讲次结构请求分流、确定性讲次删除/顺序方案及完整可编辑文本检索，验证嵌套与长正文替换和稳定 ID
- [x] 10.2 修复候选成功项保留、方案修订与迟到结果保护、放弃接口及跨课程/新方案响应身份
- [x] 10.3 形成同讲教案到讲义的候选依赖并在应用时解析来源，验证真实仓库联动、冲突和撤销
- [x] 10.4 候选生成接入已有持久任务、逐项结果保存与刷新恢复，验证断流和进程中断后的可续办性
- [x] 10.5 统一失败分类与恢复范围，区分冻结输入续办和补充输入后新尝试，验证重复失败和 last-good
- [x] 10.6 全局修改直达现有中心，提供明确查找替换与结构操作、完整可读差异、真实覆盖范围及全部历史回执，收拢技术字段
- [x] 10.7 修复全部候选失败、部分应用、撤销与历史状态，课程库只保留课程两态，同步中英文
- [x] 10.8 完成针对性前后端回归、真实模型与中文桌面连续流程、构建和合同校验；回写项目真源并安全提交推送

- [x] 10.9 修复轻量讲次方案同时具有草稿时隐藏继续按钮：waiting_for_input 优先使用 provide_input 及其真实 task ID，保留完整大纲草稿的 regenerate 合同；补组合回归并只读验证用户课程页面

- [x] 10.10 完整大纲沿现有 outline_detail_requested 任务事实维持第三步，打通继续回执、WebSocket、轮询和刷新恢复；验证课程合同子阶段、暂停、失败不退回第二步并安全发布
