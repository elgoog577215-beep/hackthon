# 实施任务

## 0. 集成与基线

- [x] 0.1 在最新 `main` 上合并 `dev/lz-course-gen` 与 `dev/lz-lesson-plan`，保留两条分支的任务可观察性、恢复和教案工作台成果。
- [x] 0.2 运行合并后的课程生成、教案、材料、知识和前端基线测试，记录失败归属。
- [x] 0.3 固定 4 章 8 节与 12 节跨学科输入、资料版本、模型池和质量量表。

## 1. 教师输入与生成过程

- [x] 1.1 将课程填写页整理为主题/目标、教学对象、课时、课程依据四组必要信息。
- [x] 1.2 把难度、讲法、题目策略、章节规模和额外要求收进统一高级设置，保留全部现有合同字段。
- [x] 1.3 将上传资料与联网研究合并为课程依据区，显示隐私边界、解析能力和联网可用状态。
- [x] 1.4 将生成现场收束为三步主流程，课程树与技术阶段进入按需详情。
- [x] 1.5 主状态同时显示当前产物、已保存数量、下一步、是否需要确认和局部恢复动作。
- [ ] 1.6 补齐中文/英文、键盘、读屏、390/789/1024/1440 视口测试。

## 2. 正式教案文档

- [x] 2.1 定义并实现 `FormalLessonPlanDocumentV1` 确定性编译器，不建立 Markdown 真源。
- [x] 2.2 将正式教案设为默认阅读视图，结构化总体/分课时编辑保留为次级视图。
- [x] 2.3 展示基本信息、目标、重难点、策略/资源/评价、逐课时过程、作业/量规和来源。
- [x] 2.4 增加打印/导出语义、缺失字段提示和中英文组件测试。

## 3. 预检、编排与部分结果

- [x] 3.1 实现 `GenerationPreflightV1`，校验主/备提供方、模型、凭据、容量、联网和资料可读性。
- [ ] 3.2 为解析、知识、教案和正文工作单元统一稳定 ID、原子检查点和 retryable 状态。
- [x] 3.3 任务投影直接统计已完成单元；7/8 必须显示 7/8，不依赖最终汇编。
- [ ] 3.4 恢复只调度失败/缺失单元，服务重启和重复请求不重复执行已完成单元。
- [ ] 3.5 实现自适应 1–3 节批次和 provider 级并发回压，补限流/额度/切换故障测试。

## 4. 资料解析与联网补证

- [x] 4.1 多文件解析改为有界并行，缓存键和恢复行为保持兼容。
- [ ] 4.2 为扫描 PDF 增加页级 OCR 降级，并保留表格、公式、图片、讲者备注、工作表与定位。
- [x] 4.3 实现 `ParsedDocumentQualityV2` 与教师可见解析预览/缺失能力。
- [x] 4.4 实现主题级广泛检索和知识缺口定向补证，共用不可变证据包与安全准入。
- [x] 4.5 检索未配置、失败、超时和来源不足时返回稳定状态，不用模型常识伪装联网核验。

## 5. 课程局部知识图谱

- [x] 5.0 将现有学科教学画像、变体、课型与模块注册表编译为版本化 `SubjectGenerationTemplateV1`，并传入任务检查点、蓝图与正文提示。
- [ ] 5.1 扩展节点/关系 schema，覆盖条件、边界、正反例、能力、易错辨别/修复、掌握、来源和置信度。
- [ ] 5.2 并行生成分节知识候选，串行完成全课去重、稳定 ID、所有权和前置 DAG，再并行补全详情并冻结；教案批次只消费冻结知识，不再同时生成 `knowledge_details`。
- [ ] 5.3 增加关系方向、孤立节点、循环、引用端点、易错/掌握/来源覆盖和低置信质量门。
- [ ] 5.4 将课程树、关系图、能力图和易错图统一为同一知识图谱投影。
- [ ] 5.5 验证知识变化与教案、正文、练习、学习证据和表达的双向影响边界。

## 6. 内容质量与真实基准

- [x] 6.1 建立跨学科教师量表与模板泄漏、事实风险、跨节引用、活动可实施性失败集。
- [ ] 6.2 将硬门与教师建议分别版本化，返回精确定位、证据和修复入口。
- [ ] 6.3 运行至少 20 次 12 节真实模型完整课程，记录成功率、P50/P95、重复执行与成本。
- [ ] 6.4 达到目录、正式教案、首节正文、完整课程和 98% 成功率发布门；未达到时定位瓶颈并继续优化。

## 7. 验证、灰度与收束

- [x] 7.1 运行相关后端测试、前端测试、类型检查、生产构建和 `git diff --check`。
- [x] 7.2 运行 `openspec validate upgrade-grounded-course-generation-v2 --strict` 与 `openspec validate --all`。
- [ ] 7.3 使用真实浏览器验证填写、生成、局部失败、续跑、正式教案和知识图谱流程。
- [ ] 7.4 更新产品状态与完成后的架构真源，按能力开关灰度并验证回滚不删除已完成产物。
- [ ] 7.5 全部发布门通过后归档本变更，并再次运行 `openspec validate --all`。
