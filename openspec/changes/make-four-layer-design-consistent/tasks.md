## 1. 查清当前情况

- [ ] 1.1 生成正式词表、旧词和旧字段使用清单，区分历史读取、新写入和仍会影响结果的判断。
- [ ] 1.2 列出课程、教案、讲稿、题库、PPT、任务和课程修改的正式数据、保存模块、修改命令和下游关系。
- [x] 1.3 生成后端模块依赖报告，列出循环调用、调用最多的模块、超大文件和路由直接组织业务的位置。
- [ ] 1.4 列出前端重复 AI 入口、状态说法、编辑操作和超过职责范围的大组件。
- [x] 1.5 检查源码、测试夹具、运行数据、演示产物和临时导出的 Git 归属。

## 2. 统一名称、数据和修改办法

- [ ] 2.1 让正式词表成为新课程、页面选项、接口字段和 Prompt 的共同依据；旧字段只在明确的历史读取适配器中出现。
- [ ] 2.2 停止旧课程字段、旧教师接口和旧任务入口产生新的正式写入，并加入回归测试。
- [ ] 2.3 让生成、手工编辑和 AI 编辑使用现有课程命令、教案修订、讲稿修订、题库修订和 PPT 修订。
- [x] 2.4 补齐内容修改与结构修改的精确影响、部分应用、失败重试、实际回执和整次撤销测试。
- [x] 2.5 统一大纲、教案、讲稿、题库和 PPT 的用户操作与状态名称，不新增第二个 AI 助手或页面流程。

## 3. 整理代码职责

- [ ] 3.1 将路由中跨多个仓库拼接业务状态的代码移入现有应用服务，并保持公开 API 不变。
- [x] 3.2 将课程、任务、学习和课程修改核心模块的循环调用拆开，为依赖方向增加自动检查。
- [ ] 3.3 按现有职责继续拆分 `jobs/manager.py`、`course_generation/service.py` 和超大前端组件，原入口只负责转调和兼容。已分出节点进度、PPT 构建、内容处理、关系检查和规划状态，但两个主文件仍过大，本项不提前标为完成。
- [x] 3.4 将运行数据、缓存、模型输出和演示产物移出源码跟踪；测试所需内容改为明确夹具。
- [x] 3.5 补齐内部包和稳定导入路径，使领域合同测试不依赖特定启动目录。

## 4. 验证真实结果

- [x] 4.1 运行正式词表、旧写入口、依赖方向、数据目录和 OpenSpec 检查。
- [x] 4.2 运行课程生成、教案、讲稿、题库、PPT、课程修改和恢复的相关后端测试。
- [x] 4.3 运行相关前端测试和生产构建。
- [x] 4.4 用多学科固定样本和复杂修改脚本检查生成语义、影响范围、失败恢复与撤销。
- [ ] 4.5 用中文桌面浏览器检查大纲、教案、讲稿、题库、PPT 的手工编辑与 AI 编辑。本轮按用户明确要求不做浏览器和真实模型验收，不将本地测试冒充为该项证据。

## 5. 更新正式说明并交付

- [x] 5.1 将最终使用的概念和关系写回 `docs/产品蓝图.md`，不复制已有定义。
- [x] 5.2 将真实完成情况、仍需真实教师验证的内容写回 `docs/产品状态.md`。
- [x] 5.3 将实际模块职责、调用方向、数据和打包方式写回 `docs/系统架构.md`。
- [x] 5.4 运行严格 OpenSpec 校验、相关测试、构建、`git diff --check` 和提交范围检查。
- [ ] 5.5 只提交本次任务文件，安全推送当前分支并复查网站真实路径。
