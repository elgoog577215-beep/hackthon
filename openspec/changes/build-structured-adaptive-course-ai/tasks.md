# 任务：知识坐标驱动、证据可解释的课程生长

> 完成度门禁：章节编号和任务层级仅用于表达依赖，不得按局部阶段宣布产品完成。验收以“证据 → 假设 → CourseEvolutionPlan → 用户决定 → CourseDocument 新修订 → 撤销 → 效果比较”为单位；其他课程、跨课程参考和历史事实必须始终保持不变。本变更不以结构化同源链是否完成作为自身完成条件。

## 1. 基线与测试夹具

- [ ] 1.1 固化现有 `CourseDocument`、块候选、知识库、学习事实、AI 老师和运行时的契约测试基线
- [x] 1.2 建立包含行级锚点、正式题目、笔记、错题、后续章节依赖和知识映射的标准验收课程
- [x] 1.3 建立单条强证据、模糊证据、多源重复、反证、跨章节模式和拒绝冷却的确定性测试样本
- [ ] 1.4 升级并版本化新 `CourseKnowledgeBase`、`EvidenceItem`、`AdaptationHypothesis`、`CourseEvolutionPlan` 和课程操作组 schema
- [ ] 1.5 验证新课程正式链路不读取旧课程知识映射或跨课程知识设施
- [ ] 1.6 增加功能开关与回滚门禁，确保新链关闭时现有课程学习主链继续可用

## 2. 课程知识库底座

- [ ] 2.1 实现按课程隔离、原子保存和修订保护的 `CourseKnowledgeBase` 仓库
- [ ] 2.2 实现 `ConceptGroup / KnowledgePoint / SkillUnit / Misconception / MasteryCriterion`、来源、颗粒度状态和墓碑模型
- [ ] 2.3 新课程知识化无法确认原子知识时阻止发布，禁止用章节标题伪造知识点
- [x] 2.4 将“先生成知识蓝图、再生成正文与资产”接入唯一 `GenerationJob` 工作区和首次发布质量门
- [x] 2.5 将 `CourseKnowledgeMap` 收束为精确 `KnowledgeBinding`，连接课程知识、正文、课件、目标、题目与教学作用
- [ ] 2.6 实现知识节点新增、补写、拆分、合并、移动、重命名和关系操作的领域命令
- [x] 2.7 从新课程生成、运行时 AI、练习、掌握投影和知识维护中移除 `SubjectKnowledgeLibrary` 读取；正式链路不保留历史课程迁移入口
- [ ] 2.8 覆盖原子性、模板内容、关系无环、关系语义、来源、绑定精度和引用完整性测试
- [ ] 2.9 实现六类关系白名单、规范方向、对称去重和 `relation_group_id + all_of / any_of` 联合关系
- [x] 2.10 停止新写入 `ImprovementPoint`，将稳定目标、具体任务和个人建议分别迁入能力、练习与 AI 动态建议
- [x] 2.11 让正文、课件、练习、AI 老师和诊断按知识关系与教学作用消费同一课程知识 ID

## 3. 证据账本与适应假设

- [x] 3.1 实现按当前课程组织的轻量 `EvidenceItem` 索引仓库
- [x] 3.2 将 AI 对话和问答输入的每次用户提交幂等登记为原始证据引用
- [x] 3.3 接入 `PracticeAttempt`、诊断、补救、`LearningRecord`、正式进度和回答反馈证据引用
- [x] 3.4 实现证据来源、课程位置、知识、目标、时间和修订索引
- [x] 3.5 实现 `AdaptationHypothesis` 仓库及支持证据、反证、范围、置信度、新鲜度和状态
- [x] 3.6 实现原始证据、适应假设与 `LearnerModel` 的硬隔离测试
- [ ] 3.7 实现证据查看、来源回跳和用户范围纠正的领域命令
- [ ] 3.8 实现证据删除、课程删除、导出和当前课程范围测试

## 4. 动态证据决策器

- [x] 4.1 实现直接性、范围、严重度、独立重复、一致性、结果、新鲜度和反证的确定性特征提取
- [x] 4.2 实现单条明确强证据立即生成局部候选的门禁
- [x] 4.3 实现模糊单次证据只观察或即时解释、不生成广域修改的门禁
- [x] 4.4 实现多源证据聚合、同义假设合并和跨位置稳定模式识别
- [x] 4.5 实现问答结束、块完成、正式提交、小节离开和主动请求的弹性稳定点评估
- [x] 4.6 实现反证、过期、范围缩小、假设结束和旧候选失效
- [x] 4.7 实现拒绝冷却与新证据、目标修订或范围变化后的合法再触发
- [ ] 4.8 用真实中文学习表达样本评估局部、依赖范围和广域判断的准确性与可解释性

## 5. 课程生长操作与正式修订

- [ ] 5.1 将现有方案仓库升级为 `CourseEvolutionPlan / CourseEvolutionOperation`
- [ ] 5.2 删除 `adaptive_blocks / PersonalCourseOverlay` 的正式课程写入能力，只保留即时支持和待确认投影
- [ ] 5.3 实现 `PATCH_COURSE_SPAN` 的文本定位、前后差异、课程修订和冲突校验
- [ ] 5.4 实现 `INSERT_COURSE_BLOCK / REPLACE_COURSE_BLOCK / RETIRE_COURSE_BLOCK` 课程操作
- [ ] 5.5 实现课程支架拆分、组合及来源块、目标和知识引用迁移
- [ ] 5.6 实现 `RESEQUENCE_COURSE_PATH` 及前置依赖检查
- [ ] 5.7 实现 `ADD_CHECKPOINT / ADD_REMEDIATION_PATH / ADJUST_COURSE_DIFFICULTY`
- [ ] 5.8 实现部分接受后的依赖重算、禁用、重生和操作图无环校验
- [ ] 5.9 实现课程操作对笔记、问题、书签、学习位置、活动 Attempt 和历史作答的保护
- [ ] 5.10 课程生长只能调用当前 `course_id` 的领域命令；兼容入口必须声明 `write_target`

## 6. 难度与课程规划器

- [ ] 6.1 定义并实现个体 `difficulty_delta` 的知识、推导、例子、支架、任务、跨度、节奏和掌握维度
- [ ] 6.2 让规划器把每个难度 delta 编译为具体课程块、支架或检查操作
- [ ] 6.3 实现当前位置、向后修补和向前预调三类规划
- [ ] 6.4 实现基于知识前置和目标依赖的后续章节影响范围计算
- [ ] 6.5 实现已掌握内容压缩、必要检查点保留和提高迁移挑战的候选规划
- [ ] 6.6 扩展质量门，拒绝只增长度、术语、公式或题量的伪难度修改
- [ ] 6.7 验证同一主题在不同证据和范围下产生可观察、可区分的课程组成变化

## 7. 知识坐标读取与当前课程受控变化

- [ ] 7.1 建立当前课程块、知识节点、能力、易错、掌握标准、题目和个人锚点的只读依赖索引
- [x] 7.2 让知识关系决定课程适配范围与后续节点，不生成跨课程知识写操作
- [ ] 7.3 课程或知识修订变化后，重新定位待确认方案；无法定位时明确冲突
- [ ] 7.4 实现已有课程支架识别，避免同一知识缺口重复生成内容
- [ ] 7.5 实现 `plan_id + evidence_signature + effect_signature` 去重
- [ ] 7.6 实现学习问题转当前课程改进建议的门禁和循环抑制
- [ ] 7.7 知识语义建议必须转换为当前课程 `CourseAuthoringChange` 并通过知识质量门
- [ ] 7.8 覆盖知识拆分、合并、重命名、课程重排和删除引用后的候选重定位测试

## 8. 课程操作组、冲突与恢复

- [x] 8.1 实现完整 `base_revision_vector` 和最终范围预检
- [ ] 8.2 实现 `prepared / applying / committed / compensating / failed` 课程操作组日志
- [ ] 8.3 实现课程操作的确定性应用顺序和单次原子文档提交
- [ ] 8.4 实现重复确认返回同一 `ActionReceipt` 的幂等协议
- [ ] 8.5 实现服务重启后 prepared 和 applying 课程操作组的自动对账与续跑
- [ ] 8.6 实现不能安全续跑时的补偿命令和明确失败回执
- [ ] 8.7 实现同块、不同块共享知识、范围变化和部分接受的冲突策略
- [ ] 8.8 以进程强制终止、磁盘写失败、重复请求和并发窗口验证无静默半应用

## 9. 后端接口与 AI 编排

- [ ] 9.1 实现课程知识库只读切片、证据摘要、适应问题、课程方案、范围预览和历史读取接口
- [ ] 9.2 将方案读取、评估、接受、拒绝、撤销 API 统一为当前课程写入，并删除平行兼容写入口
- [ ] 9.3 扩展 `AIContextPackage`，按局部、依赖范围和广域意图装配最小必要证据与修订
- [ ] 9.4 实现证据解释器、教学决策器、课程生长规划器、内容生成器、影响分析器和质量评估器的结构化协议
- [ ] 9.5 对模型输出执行操作白名单、目标归属、修订、引用、范围和质量校验
- [ ] 9.6 实现流式生成状态、取消、失败重试和同请求并发去重
- [ ] 9.7 将主动课程生长方案接入统一 AI 老师提案、课程命令与回执协议
- [ ] 9.8 验证模型不可用时课程阅读、正式练习、笔记、知识库和已有候选继续可用

## 10. 前端候选层与范围交互

- [x] 10.1 建立唯一 `courseEvolution` Store 并接入方案、范围、回执和历史
- [ ] 10.2 实现 `CourseDocument + PendingAdaptationOverlay + InlineLearningRecordProjection` 的确定性前端投影
- [ ] 10.3 实现行级新增、删除、替换差异和“AI 新增/修改”标记
- [ ] 10.4 实现新增块、支架拆分组合、路径预调和隐藏的课程流方案表达
- [x] 10.5 实现左侧目录未来章节候选数量、定位和统一待处理摘要
- [ ] 10.6 实现知识状态、命中能力/易错和适配范围表达；知识语义修改另行跳转维护建议
- [ ] 10.7 实现选区、段、块、小节、章节、多节点和全课程范围选择器
- [ ] 10.8 实现多章节或全课程范围的影响预览与保持不变摘要
- [x] 10.9 实现理由、证据、难度 delta、影响和质量信息的分层展示
- [ ] 10.10 实现接受、拒绝、重新生成、补充要求、部分接受和批量操作
- [x] 10.11 实现待确认候选跨刷新、退出、断网和重连恢复
- [ ] 10.12 让正文、目录、知识库和 AI 老师共享同一 `CourseEvolutionPlan` 与回执状态
- [ ] 10.13 实现用户笔记与 AI 解答自动锚定入正文，并支持删除、编辑和 AI 重做

## 11. 历史、撤销与效果评价

- [ ] 11.1 实现课程适配理由、证据、范围、前后内容、用户决定、回执和修订的历史视图
- [x] 11.2 实现单操作与整组安全撤销的补偿命令
- [ ] 11.3 实现存在后续依赖时的影响提示并禁止静默整课快照回滚
- [x] 11.4 为每个已接受变更保存目标、范围、问题类型和修改前效果基线
- [x] 11.5 实现修改后同类求助、反馈、错误和独立表现的效果比较
- [x] 11.6 实现无效、副作用或范围错误修改的回退与重新调整候选
- [x] 11.7 验证接受、拒绝或内容变长本身不会被误报为学习改善
- [x] 11.8 区分被动动画接触、概念判断、单题初步支持和多题持续确认，并展示调整前到复验的证据链

## 12. 国际化、无障碍与视觉验收

- [ ] 12.1 为全部个人方案、范围、状态、冲突、历史和错误同步中文与英文 locale
- [ ] 12.2 为图标按钮、差异区域、目录标记和批量操作补齐 tooltip、焦点和读屏名称
- [ ] 12.3 验证键盘可完成定位、展开、范围选择、接受、拒绝和重新生成
- [ ] 12.4 验证 390、789、1024、1440 像素下正文差异、目录、AI 老师和操作区无遮挡溢出
- [ ] 12.5 验证候选高亮与正式正文、临时 AI 回答、笔记和正式任务视觉可区分
- [ ] 12.6 检查英文模式无中文残留、原始翻译 key、截断和布局破坏

## 13. 性能、安全与故障矩阵

- [ ] 13.1 对大课程、多证据、多候选和跨章节影响分析建立性能基准
- [ ] 13.2 实现只读依赖索引增量更新、受影响子图计算、分页和异步后台规划
- [ ] 13.3 验证候选生成和全局影响分析不阻塞课程阅读与正式作答
- [ ] 13.4 覆盖错误课程 ID、伪造候选 ID 和越过当前课程范围的拒绝测试
- [ ] 13.5 覆盖候选与证据的删除、导出、课程软删除和永久删除
- [ ] 13.6 覆盖模型超时、网络断开、服务重启、损坏候选、过期修订和重复回调
- [ ] 13.7 验证课程生长只修改当前 `course_id`，不会修改其他课程、跨课程参考或启智数据

## 14. 纵向验收与旧链退役

- [ ] 14.1 完成单条强证据立即行级修改候选的真实模型和浏览器验收
- [ ] 14.2 完成模糊证据不外推、反证缩小范围和拒绝冷却验收
- [ ] 14.3 完成第二节证据预调第三至第五节并由目录审阅的验收
- [ ] 14.4 完成知识点、能力点和易错点精确驱动课程适配，并验证普通教学变化不写知识库
- [ ] 14.5 完成部分接受、重新生成、课程修订冲突、单项撤销和课程操作组恢复验收
- [x] 14.6 完成候选内容交互不产生正式掌握的证据边界验收
- [x] 14.7 完成当前课程隔离和其他课程保持不变的验收
- [ ] 14.8 删除被新协议接管的旧持久个人覆盖块和前端平行状态
- [ ] 14.9 运行后端新旧测试、前端完整测试、生产构建和 `openspec validate --all`
- [ ] 14.10 更新唯一产品蓝图、项目决策和课程学习全链验收文档
- [ ] 14.11 使用真实学习样本证明至少一条“发现问题 → 修改课程 → 后续改善或回退”闭环
- [ ] 14.12 全部任务与验证完成后归档本 OpenSpec 并再次校验全部规格

## 15. 首次生成的全课一致性

- [x] 15.1 从课程蓝图与 `CourseKnowledgeBase` 编译版本化的全课一致性契约
- [x] 15.2 将节点位置、唯一责任、前置产出、后续边界和规范术语接入正文与修复 prompt
- [x] 15.3 在首次发布质量门检查顺序、依赖、承接和跨节实质重复
- [x] 15.4 仅对可修复的阻断问题生成定点候选，且只在节点质量通过且阻断数下降时接受
- [x] 15.5 兼容生成草稿与正式 `CourseDocument` 投影，并用新课程和故障恢复回归验证
- [x] 15.6 将用户明确章数与小节总数编译为确定性蓝图硬约束，并把验收结果写入蓝图和最终质量报告
- [x] 15.7 删除模型大纲异常时的通用占位课程；该整课纠正策略已被第 17 节的分阶段局部返工方案取代

## 16. 课程知识库结构与关系网络设计基线

- [x] 16.1 确认“一门课程一套知识库”，`CourseKnowledgeBase` 为唯一产品知识库，跨课程设施退出新生成与运行主链
- [x] 16.2 定义“课程路径 → 概念组 → 原子知识点 → 能力 / 易错 / 掌握标准”的实体结构

- [x] 16.3 定义 `prerequisite / derives / equivalent_to / contrasts_with / applies_to / generalizes` 六类正式关系及判定门禁
- [x] 16.4 定义 `KnowledgeBinding`、关系驱动教学表达、生成审查链、质量门与下游消费契约
- [x] 16.5 将稳定结论回写产品蓝图、总需求、项目决策和本 OpenSpec
- [ ] 16.6 按第 2 节任务实现 schema、仓库、生成链、质量门和精确绑定，不以当前设计完成代替代码完成
- [ ] 16.7 用至少三门不同教学模式的真实课程验收原子性、关系有效性、课件/题目绑定和最小知识断点诊断
- [x] 16.8 完成 `course_knowledge_base_v2` 首条纵向实现、标题复制禁令、原子性 / 关系 / 绑定硬质量门与降级空投影
- [x] 16.9 让正文块、目标、题目、五类教学表达和 AI 老师消费同一课程知识 ID，并覆盖确定性契约测试

## 17. 分阶段、可恢复的首次课程生产线

- [x] 17.1 将整课复杂蓝图拆为轻量目录、逐节知识包和逐节关系批次三类结构化产物
- [x] 17.2 在审阅模式中把确认点前移到轻量目录通过检查之后，确认前不生成知识包与正文
- [x] 17.3 为轻量目录实现独立 schema、形状与唯一责任检查，快速模式复用相同冻结门
- [x] 17.4 为每个小节实现知识包生成、局部检查、一次局部纠正和检查点持久化
- [x] 17.5 模型按规范名称提交关系候选，由系统解析端点并分配稳定知识 ID，不让模型创建内部 ID
- [x] 17.6 所有目录和知识生成调用使用心跳、阶段超时、可解释错误与最小恢复单元
- [x] 17.7 任务进度展示真实阶段、当前小节、已完成知识包数和正文块数，不再依赖固定百分比
- [x] 17.8 恢复说明区分目录检查点、知识包检查点和正文草稿，不在 0/0 内容时声称已保留正文
- [ ] 17.9 覆盖一章两节、六章大课、单节知识失败、关系失败、服务重启和模型不可用故障矩阵
- [ ] 17.10 完成中文、英文、桌面与移动端真实浏览器验收，并更新唯一产品蓝图与项目决策

## 18. 六步确认与结构化同源发布链

- [x] 18.1 修订唯一产品蓝图与 OpenSpec，取消面向用户的直接生成模式并明确六步确认契约
- [x] 18.2 为首次生成任务保存需求、目录、知识、教学、内容、发布六步状态及各步产物修订
- [x] 18.3 让第 2 至第 6 步在同一 `GenerationJob` 中依次进入待确认，服务重启不得越过确认门
- [x] 18.4 提供各步骤安全审阅投影，只展示用户需要判断的内容，不暴露 prompt、内部 ID 或格式修复
- [x] 18.5 实现上游修订变化后的确定性下游失效传播，旧产物不得继续进入发布链
- [x] 18.6 实现最终同源版本链报告，任一确认缺失、修订漂移或知识绑定不一致都阻止发布
- [x] 18.7 前端取消“直接生成”，用 1—6 步骤工作台展示当前步骤、产物、确认动作和需要重做状态
- [x] 18.8 同步中英文文案，并覆盖六步确认、恢复、重复确认、越步确认和发布阻断回归
- [x] 18.9 将教学编译严格后移到知识确认之后，并在内容确认前完成正文、练习资产和质量检查
- [ ] 18.10 完成目录回改、知识职责、内容资产和同源发布的桌面及英文真实浏览器验收

## 19. 本节课程生长统一链

- [x] 19.1 让用户主动本节调整与学习证据挑战建议复用同一 `CourseEvolutionPlan`、Store 和确认状态
- [x] 19.2 按本节真实 `CourseBlock.role` 盘点结构，对已有作用执行保留块身份的升级，对缺失作用新增课程块
- [x] 19.3 在每个候选生成后保存方案检查点，候选失败或中断不得修改正式 `CourseDocument`
- [x] 19.4 对整组候选执行本节知识同源、范围、角色、重复和内容质量检查，未通过时禁止确认
- [x] 19.5 扩展课程操作组，在一次文档提交中原子执行 replacements 与 insertions，并保持稳定命令幂等
- [x] 19.6 撤销时恢复升级前的块载荷与知识引用，只退役本次新增块，不覆盖历史作答、笔记和掌握记录
- [x] 19.7 将重复正式通过同时作为缺口反证和挑战就绪正证据，保留旧难度掌握事实并等待更高挑战复验
- [x] 19.8 在 AI 老师中展示“需求 → 结构 → 候选 → 同源 → 确认 → 复验”六步及升级/新增候选差异
- [x] 19.9 接受与撤销后对账受影响教学表达，并在回执中保留重建结果
- [x] 19.10 覆盖原子升级与新增、失败不污染、撤销恢复、挑战建议、Store 与前端工作台回归

## 20. Workflow 内的轻量 AI 场景判断

- [x] 20.1 定义场景判断结构，只允许输出场景摘要、教学 role、难度方向、差异向量、资料要求和判断理由
- [x] 20.2 让 AI 读取当前小节结构、课程知识上下文和本节证据摘要，不暴露跨课程内容或任意写接口
- [x] 20.3 对模型输出执行 role 白名单、难度边界、资料要求和字段归一化，系统继续决定具体 REPLACE / INSERT
- [x] 20.4 AI 不可用、超时或输出非法时使用同一契约的确定性规则兜底，不阻断原课程生长链
- [x] 20.5 将场景判断和资料需求写入 `CourseEvolutionPlan`，候选生成继续消费同一判断
- [x] 20.6 在六步工作台展示“AI 理解 / 规则保底”、场景摘要、判断理由和可信资料提示
- [x] 20.7 覆盖复杂间接表达、非法模型输出、模型失败、前沿资料要求和原有明确指令回归
- [x] 20.8 运行后端、前端、构建、真实页面与 `openspec validate --all`，并更新蓝图和项目决策
