# 任务：结构化同源、证据驱动的个人课程智能体

## 1. 基线、迁移与测试夹具

- [ ] 1.1 固化现有 `CourseDocument`、块候选、知识库、学习事实、AI 老师和运行时的契约测试基线
- [ ] 1.2 建立包含行级锚点、正式题目、笔记、错题、后续章节依赖和知识映射的标准验收课程
- [ ] 1.3 建立单条强证据、模糊证据、多源重复、反证、跨章节模式和拒绝冷却的确定性测试样本
- [ ] 1.4 定义并版本化 `CourseKnowledgeBase`、`EvidenceItem`、`AdaptationHypothesis`、`CourseChangeSet` 和命令组 schema
- [ ] 1.5 编写旧单块候选、旧课程知识映射和历史课程的迁移预览与幂等测试
- [ ] 1.6 增加功能开关与回滚门禁，确保新链关闭时现有课程学习主链继续可用

## 2. 课程知识库底座

- [ ] 2.1 实现按课程隔离、原子保存和修订保护的 `CourseKnowledgeBase` 仓库
- [ ] 2.2 实现课程知识节点、关系、来源、正式引用、颗粒度状态和墓碑模型
- [x] 2.3 从现有 `CourseKnowledgeMap`、学习目标、课程块和学习资产确定性编译历史课程知识库
- [x] 2.4 将新课程知识库生成接入唯一 `GenerationJob` 工作区和首次发布质量门
- [x] 2.5 扩展 `CourseKnowledgeMap` 连接课程局部节点、正式知识、课程块、目标和题目
- [ ] 2.6 实现知识节点新增、补写、拆分、合并、移动、重命名和关系操作的领域命令
- [ ] 2.7 实现正式知识精确映射、部分映射、未映射和禁止自造正式 ID 的门禁
- [ ] 2.8 覆盖知识无环、合法层级、来源、引用完整性、颗粒度和旧新 ID 映射测试

## 3. 证据账本与适应假设

- [ ] 3.1 实现按学习者和课程隔离的轻量 `EvidenceItem` 索引仓库
- [ ] 3.2 将 AI 对话和问答输入的每次用户提交幂等登记为原始证据引用
- [ ] 3.3 接入 `PracticeAttempt`、诊断、补救、`LearningRecord`、正式进度和回答反馈证据引用
- [ ] 3.4 实现证据来源、课程位置、知识、目标、时间和修订索引
- [ ] 3.5 实现 `AdaptationHypothesis` 仓库及支持证据、反证、范围、置信度、新鲜度和状态
- [ ] 3.6 实现原始证据、适应假设与 `LearnerModel` 的硬隔离测试
- [ ] 3.7 实现证据查看、来源回跳和用户范围纠正的领域命令
- [ ] 3.8 实现证据删除、课程删除、导出、身份切换和跨用户跨课程隔离测试

## 4. 动态证据决策器

- [ ] 4.1 实现直接性、范围、严重度、独立重复、一致性、结果、新鲜度和反证的确定性特征提取
- [ ] 4.2 实现单条明确强证据立即生成局部候选的门禁
- [ ] 4.3 实现模糊单次证据只观察或即时解释、不生成广域修改的门禁
- [ ] 4.4 实现多源证据聚合、同义假设合并和跨位置稳定模式识别
- [ ] 4.5 实现问答结束、块完成、正式提交、小节离开和主动请求的弹性稳定点评估
- [ ] 4.6 实现反证、过期、范围缩小、假设结束和旧候选失效
- [ ] 4.7 实现拒绝冷却与新证据、目标修订或范围变化后的合法再触发
- [ ] 4.8 用真实中文学习表达样本评估局部、依赖范围和广域判断的准确性与可解释性

## 5. 通用课程变更操作

- [ ] 5.1 实现 `CourseChangeSet / ChangeOperation` 仓库、状态机、幂等和恢复描述
- [ ] 5.2 将现有块候选迁移为单操作 `REPLACE_BLOCK` 变更集并退役独立应用规则
- [ ] 5.3 实现 `PATCH_SPAN` 的文本定位、前后差异、修订和冲突校验
- [ ] 5.4 实现 `INSERT_BLOCK / PATCH_BLOCK / REPLACE_BLOCK` 课程命令
- [ ] 5.5 实现 `SPLIT_BLOCK / MERGE_BLOCKS` 及旧新 ID、目标、资料和知识引用迁移
- [ ] 5.6 实现 `RESEQUENCE_BLOCKS / REMOVE_BLOCK / RESTORE_BLOCK` 及墓碑和前置依赖检查
- [ ] 5.7 实现 `ADD_CHECKPOINT / ADD_REMEDIATION_PATH / ADJUST_DIFFICULTY_CONTRACT`
- [ ] 5.8 实现部分接受后的依赖重算、禁用、重生和操作图无环校验
- [ ] 5.9 实现课程命令对笔记、问题、书签、学习位置、活动 Attempt 和历史作答的保护
- [ ] 5.10 删除或拒绝所有可绕过统一变更集与课程领域命令的持久课程写入口

## 6. 难度与课程规划器

- [ ] 6.1 定义并实现个体 `difficulty_delta` 的知识、推导、例子、支架、任务、跨度、节奏和掌握维度
- [ ] 6.2 让规划器把每个难度 delta 编译为具体课程、知识或学习资产操作
- [ ] 6.3 实现当前位置、向后修补和向前预调三类规划
- [ ] 6.4 实现基于知识前置和目标依赖的后续章节影响范围计算
- [ ] 6.5 实现已掌握内容压缩、必要检查点保留和提高迁移挑战的候选规划
- [ ] 6.6 扩展质量门，拒绝只增长度、术语、公式或题量的伪难度修改
- [ ] 6.7 验证同一主题在不同证据和范围下产生可观察、可区分的课程组成变化

## 7. 课程与知识双向影响分析

- [ ] 7.1 建立课程块、知识节点、目标、题目、标准、记录和锚点的依赖索引
- [ ] 7.2 实现知识节点变化到课程与学习资产的精确影响分析
- [ ] 7.3 实现课程语义变化到课程知识库补写、细化或重新映射的影响分析
- [ ] 7.4 实现已有覆盖识别，避免知识拆分后重复生成正文
- [ ] 7.5 实现单一 `change_set_id`、因果操作引用和效果签名去重
- [ ] 7.6 实现课程到知识再到课程的循环抑制和修订变化重算
- [ ] 7.7 实现跨域操作依赖图、部分接受影响和过期处理
- [ ] 7.8 覆盖新增知识、拆分知识、合并知识、重排课程和删除引用的端到端测试

## 8. 跨域命令组、冲突与恢复

- [ ] 8.1 实现完整 `base_revision_vector` 和最终范围预检
- [ ] 8.2 实现 `prepared / applying / committed / compensating / failed` 命令组日志
- [ ] 8.3 实现课程、课程知识库、映射和受影响资产的确定性应用顺序
- [ ] 8.4 实现重复确认返回同一 `ActionReceipt` 的幂等协议
- [ ] 8.5 实现服务重启后 prepared 和 applying 命令组的自动对账与续跑
- [ ] 8.6 实现不能安全续跑时的补偿命令和明确失败回执
- [ ] 8.7 实现同块、不同块共享知识、范围变化和部分接受的冲突策略
- [ ] 8.8 以进程强制终止、磁盘写失败、重复请求和并发窗口验证无静默半应用

## 9. 后端接口与 AI 编排

- [ ] 9.1 实现课程知识库、证据摘要、适应问题、变更集、范围预览和历史读取接口
- [ ] 9.2 实现变更集生成、重新生成、接受、拒绝、撤销和恢复接口
- [ ] 9.3 扩展 `AIContextPackage`，按局部、依赖范围和广域意图装配最小必要证据与修订
- [ ] 9.4 实现证据解释器、教学决策器、课程规划器、内容生成器、影响分析器和质量评估器的结构化协议
- [ ] 9.5 对模型输出执行操作白名单、目标归属、修订、引用、范围和质量校验
- [ ] 9.6 实现流式生成状态、取消、失败重试和同请求并发去重
- [ ] 9.7 将主动课程生长候选接入统一 AI 老师提案与回执协议
- [ ] 9.8 验证模型不可用时课程阅读、正式练习、笔记、知识库和已有候选继续可用

## 10. 前端候选层与范围交互

- [ ] 10.1 建立唯一 `courseEvolution` Store 并接入服务端候选、范围、回执和历史
- [ ] 10.2 实现 `CourseDocument + PendingChangeOverlay` 的确定性前端投影
- [ ] 10.3 实现行级新增、删除、替换差异和“AI 新增/修改”标记
- [ ] 10.4 实现新增块、拆分、合并、重排和移除的课程流候选表达
- [ ] 10.5 实现左侧目录未来章节候选数量、定位和统一待处理摘要
- [ ] 10.6 实现知识库节点新增、细化、合并、关系和映射候选表达
- [ ] 10.7 实现选区、段、块、小节、章节、多节点和全课程范围选择器
- [ ] 10.8 实现多章节或全课程范围的影响预览与保持不变摘要
- [ ] 10.9 实现理由、证据、难度 delta、影响和质量信息的分层展示
- [ ] 10.10 实现接受、拒绝、重新生成、补充要求、部分接受和批量操作
- [ ] 10.11 实现待确认候选跨刷新、退出、断网、重连和跨设备恢复
- [ ] 10.12 让正文、目录、知识库和 AI 老师共享同一候选与回执状态

## 11. 历史、撤销与效果评价

- [ ] 11.1 实现变更理由、证据、范围、前后内容、用户决定、回执和修订的历史视图
- [ ] 11.2 实现单操作与整组安全撤销的补偿命令
- [ ] 11.3 实现存在后续依赖时的影响提示和禁止静默快照回滚
- [ ] 11.4 为每个已接受变更保存目标、范围、问题类型和修改前效果基线
- [ ] 11.5 实现修改后同类求助、反馈、错误和独立表现的效果比较
- [ ] 11.6 实现无效、副作用或范围错误修改的回退与重新调整候选
- [ ] 11.7 验证接受、拒绝或内容变长本身不会被误报为学习改善

## 12. 国际化、无障碍与视觉验收

- [ ] 12.1 为全部候选、范围、状态、冲突、历史和错误同步中文与英文 locale
- [ ] 12.2 为图标按钮、差异区域、目录标记和批量操作补齐 tooltip、焦点和读屏名称
- [ ] 12.3 验证键盘可完成定位、展开、范围选择、接受、拒绝和重新生成
- [ ] 12.4 验证 390、789、1024、1440 像素下正文差异、目录、AI 老师和操作区无遮挡溢出
- [ ] 12.5 验证候选高亮与正式正文、临时 AI 回答、笔记和正式任务视觉可区分
- [ ] 12.6 检查英文模式无中文残留、原始翻译 key、截断和布局破坏

## 13. 性能、安全与故障矩阵

- [ ] 13.1 对大课程、多证据、多候选和跨章节影响分析建立性能基准
- [ ] 13.2 实现依赖索引增量更新、受影响子图计算、分页和异步后台规划
- [ ] 13.3 验证候选生成和全局影响分析不阻塞课程阅读与正式作答
- [ ] 13.4 覆盖跨用户、跨课程、伪造 course ID、伪造候选 ID 和缺失身份的拒绝测试
- [ ] 13.5 覆盖候选与证据的删除、导出、课程软删除、永久删除和账号切换
- [ ] 13.6 覆盖模型超时、网络断开、服务重启、损坏候选、过期修订和重复回调
- [ ] 13.7 验证公共正式知识库、其他学生课程和启智数据不会被个人课程命令修改

## 14. 纵向验收与旧链退役

- [ ] 14.1 完成单条强证据立即行级修改候选的真实模型和浏览器验收
- [ ] 14.2 完成模糊证据不外推、反证缩小范围和拒绝冷却验收
- [ ] 14.3 完成第二节证据预调第三至第五节并由目录审阅的验收
- [ ] 14.4 完成知识节点细化驱动课程候选和课程变化反向补知识的双向验收
- [ ] 14.5 完成部分接受、重新生成、修订冲突、单项撤销和跨域恢复验收
- [ ] 14.6 完成候选内容交互不产生正式掌握的证据边界验收
- [ ] 14.7 完成当前课程隔离和其他课程保持不变的验收
- [ ] 14.8 删除被新协议接管的旧块候选应用、持久适配块和前端平行状态
- [ ] 14.9 运行后端新旧测试、前端完整测试、生产构建和 `openspec validate --all`
- [ ] 14.10 更新唯一产品蓝图、项目决策和课程学习全链验收文档
- [ ] 14.11 使用真实学习样本证明至少一条“发现问题 → 修改课程 → 后续改善或回退”闭环
- [ ] 14.12 全部任务与验证完成后归档本 OpenSpec 并再次校验全部规格
