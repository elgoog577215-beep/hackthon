# 教师课程生产能力迁移清单

> 状态：G1 能力盘点第一版<br>
> 核验日期：2026-08-24<br>
> 核验对象：当前 `main` 的前端路由、工作台、后端路由、领域服务、任务与聚焦测试<br>
> 用途：决定旧整课链的能力如何被新工作台吸收；不是删除清单，也不替代 `tasks.md`

## 1. 当前两套实现的真实边界

### 1.1 当前正式教师入口

正式入口是 `/courses` 和 `/course/:courseId/workspace/:mode`：

```text
TeacherTeachingCalendarView
→ TeacherCourseLibraryView / TeacherCourseCreateView
→ CourseWorkspaceView
→ TeacherCourseWorkbench
```

旧 `/teacher/course/:courseId/...` 地址已经在 `frontend/src/router/index.ts` 重定向到同一课程空间、学生视角预览或 V6 PPT 工作台。`TeacherCourseProductionView.vue`、`TeacherCourseOverviewView.vue`、`TeacherCourseFilesView.vue` 和非嵌入式 `TeacherCourseSidebar.vue` 仍保留源码，但不再由正式路由挂载；它们属于 G3 调用审计对象，不是新功能应继续依赖的页面。

### 1.2 新工作台仍在复用旧链的部分

新工作台生成大纲时，经 `CourseWorkspaceView.startOutlineGeneration` 调用 `courseStore.generateCourse`，再进入 `/api/course-generation/generate`。请求携带 `target_course_id` 和 `teacher_authoring_mode=lesson_assets_v1`，后端把它投影为教师大纲任务。也就是说，新工作台是新的教师编排壳，但大纲规划、任务和部分数据仍合理复用旧整课链能力。

因此不能按文件名把 `course_generation` 整体删除。必须把“教师入口”“共享生成能力”“学生整课运行时”分开判断。

## 2. 能力迁移矩阵

| 能力 | 当前代码真源与证据 | 当前判断 | 新主线归宿 | 下一动作 |
| --- | --- | --- | --- | --- |
| 新建课程与课程基本信息 | `routers/courses.py:create_teacher_course`、`TeacherCourseCreateView.vue` | 已符合先建空课程空间；无需旧整课生成才能建课 | 课程基本信息真源 | 保留；只补教学类型、学科类型/细分的可选识别与修改 |
| 教师大纲规划 | `course_outline_planning.py`、`CoursePromptComposer.build_outline_*`、`CourseService._generate_hierarchical_course_outline` | 成熟：骨架、分批、小节数约束、校正、降级、组装、恢复均已有测试 | 教师大纲任务 | 原样复用领域能力；确认大纲后停止教师任务，不进入旧整课后续阶段 |
| 课程分类与教学策略 | `course_type_contracts.py`、`course_pedagogy.py`、`test_course_pedagogy_archetypes.py` | 四类教学类型与内部学科/课型策略有价值，但内部 40 类 archetype 不宜直接暴露 | 课程级默认偏好和本讲推荐器 | 保留内部注册表；增加冻结语言的适配层，禁止复制新注册表 |
| 本讲课型与教学块锁定 | `lesson_arrangement.py`、`TeacherLessonAuthoringRepository.arrangement`、`TeacherCourseWorkbench.generateLessonPlan` | **已进主链并完成界面收口**：内部教学策略先投影为七类课型与有序教学块，教师点击生成时自动锁定，前端不提供逐块编辑器 | 本讲编排修订 | 保留内部结构真源与 V3 输入合同；生成后的说明、调整和重生成统一经 AI 候选机制，不建平行结构 |
| 分讲教案生成 | `CourseService.prepare_teacher_lesson_plan` 复用 `_prepare_course_teaching_plan`；`course_teaching_plan_v3.py`；`standard_lesson_plan_v1` | 成熟能力已正确复用，不应重写；当前可按讲限制作用域并允许合格降级 | 教案正式资产 | 保留 V3 骨架、批次、校正、知识关系与质量门；改输入为已确认教学块 |
| 教案编辑与 AI 候选 | `TeacherLessonAuthoringService`、`optimize_teacher_lesson_plan`、`TeacherLessonAiWorkspace.vue` | 已有工作修订、确认指针、字段差异、采用/放弃和知识保护 | 教案修订链 | 保留；补原教案吸收与编排字段的同一候选机制 |
| 原教案吸收 | 当前有 `extract_uploaded_pptx_evidence`、课程资料 evidence 和主来源关系；未发现 DOCX/PDF 教案结构忠实吸收合同 | **缺口**：现有资料可作为证据，但不能保证保留原教案分块、顺序和原文 | 原文件 → 教案工作修订的单向吸收器 | 建立 DOCX/PDF/结构化文档解析适配；原件不写回，冲突只形成候选 |
| 资料作用域与来源关系 | `teacher_course_space.relationships`、`TeacherCourseWorkbench.saveRelationships`、`lesson-plan:<lesson_id>` | 当前已按讲保存主来源和参考资料；同一原件可复用且不复制 | 当前讲来源包 | 保留；讲稿和 PPT 复用同一作用域，不另建资料真源 |
| 教师讲稿结构真源 | `teacher_script.py` 已有 `teacher_script_v2`、稳定 block、模块顺序、知识范围和 `teacher_script_quality_v1` | 基础结构正确，已不是学生式自由 Markdown；无需推倒 | 讲稿修订链 | 保留并升级；统一术语由“module”逐步适配为教师可见“教学块” |
| 旧正文生成能力 | `CoursePromptComposer.build_content_prompt`、`course_generation_workflow` 的 module plan、`generate_node_content_stream` 及相关学科/连贯性测试 | 有成熟的学科教学结构、上下文预算、并发、修复和降级；但其目标是学生自学正文 | 讲稿块生成策略库 | 迁移可复用模板和约束，不复用学生正文资产、发布或整节自由重写语义 |
| 当前讲稿生成 | `CourseService.generate_teacher_script_section`、`TeacherLessonAuthoringService.run_script_job`、`routers/teacher_lesson_authoring.py:generate_lesson_script` | 已按确认教案逐块生成；首批吸收旧正文链的学科讲解策略、知识边界、课程连贯和内容完整性约束，并显式排除学生自学口吻 | 教师讲稿生成器 | 继续迁移不同学科和代码/公式/表格模板，使用真实模型与旧正文样本对照，不得重新选择课型或块顺序 |
| 讲稿任务与恢复 | `TeacherLessonAuthoringRepository` 保存讲稿 job 的输入指纹、块状态和 `result_sections`；每块完成即持久化并由 SSE 投影，失败保留完成块，恢复只调用缺失块，全部通过后才保存正式修订 | **耐久切片已完成**；后端和前端聚焦测试覆盖保留、续做和原位展示 | 统一耐久任务 + 逐块工作片段 | 继续统一取消、跨讲切换、重复点击和服务重启矩阵，再把同一合同扩展到 V6 |
| V6 PPT 生成 | `teacher_lesson_v6_source`、`SlideDeckV6Orchestrator`、`slide_deck_v6.py`、V6 测试族 | V6 规划、布局、来源保真、预览和导出成熟；教师入口已记录 plan/script revision | 已确认讲稿 → V6 | 保留 V6；收紧为讲稿唯一内容语义输入，教案只作追溯和校验 |
| 旧轻量教师 PPT | `generate_teacher_lesson_ppt`、`save_ppt_revision`、`/ppt/export.pptx` 与 V6 并存 | 与 V6 有重复风险；目前仍可能承担历史导出 | 历史只读/导出兼容 | G2 后禁止新建，G3 对账，G4 删除生成和 AI 候选接口 |
| 统一任务控制 | `TaskManager` 对旧整课有持久任务、心跳、检查点、恢复；`TeacherLessonAuthoringRepository` 又维护本讲 jobs；V6 还有候选/进度仓库 | **结构缺口**：能力可复用，但存在多个状态所有者 | 一个任务控制合同，资产仓库只存修订和任务投影 | 设计统一 job adapter；先覆盖讲稿，再合并教案/V6 投影，禁止一次性大迁移 |
| 题库、日历、配套文档 | 各自已有工作台入口和正式文件投影 | 不属于教案到 PPT 的阻塞链 | 非阻塞旁支 | 保持独立，只共享课程/讲次与来源关系 |
| 学生课程运行时 | `CourseDocument`、`LearningView`、学生 generation preview、练习和学习事实 | 与旧整课生成存在共享能力，不能随教师入口退场一起删除 | 本轮兼容边界 | 每次删除前做学生回归；共享服务保留，教师专属入口按门退场 |

## 3. 提示词与模板的迁移判断

### 3.1 直接保留

- 大纲：`build_outline_skeleton_v2_prompt`、`build_outline_batch_v2_prompt` 及校正提示词。
- 教案：`build_teaching_plan_skeleton_v3_prompt`、`build_teaching_plan_batch_v3_prompt`、知识细节和关系字段修复提示词。
- 教案 AI 优化：当前紧凑字段合同、最小充分修改和小节身份保护。
- V6：故事规划、来源保真、布局容量和视觉修复合同。

### 3.2 拆开吸收

旧 `build_content_prompt` 不整体搬入讲稿。需要逐项抽取并建立测试：

1. 学科类型和学科细分的讲解行为；
2. 背景引入、概念讲解、过程推导、案例、示范、练习、互动、检查、总结等逻辑块约束；
3. 前后小节连贯性、知识范围和资料依据；
4. 输入预算、超时、一次结构修复和可信降级；
5. 代码、公式、表格等学科产物的完整性。

这些能力进入“教学块 → 讲稿块”的策略选择器；学生自学口吻、学习者个性化、发布 CourseDocument、整节自由重写和练习发布不迁入教师讲稿。

### 3.3 逐步退场

- 旧教师整课页面和未挂载侧栏：先确认无动态导入和生产访问，再删除。
- 旧轻量教师 PPT 生成/候选：V6 全部覆盖历史需求后改为只读导出，再删除写入口。
- 旧教师整课任务的正文、练习和发布阶段：新工作台不再创建后进入只读兼容；学生仍使用的共享代码不删除。

## 4. 下一实施切片

按风险和依赖，下一阶段不是先删旧代码，而是连续完成三个纵向切片：

1. **本讲编排界面已收口**：逐块编辑器已撤销，系统在生成时自动锁定建议编排；教师通过 AI 助手查看影响并采用调整候选。
2. **讲稿耐久切片已完成**：讲稿使用持久任务，逐块持久化和流式投影，失败保留已完成块并只重试失败块。
3. **旧正文能力吸收切片进行中**：通用学科策略、知识边界与连贯约束已接入；继续给不同学科教学块建立模板映射和真实模型对照，确认质量不低于旧正文后再关闭教师旧正文写入。

完成这三项后，再收紧 V6 来源并启动旧轻量 PPT 与旧教师页面的 G3 只读审计。

## 5. 尚未核验的退场条件

以下内容没有证据前不得标记完成：

- 真实课程数据中旧教案、旧正文和旧 PPT 的数量、来源与最后访问时间；
- 生产日志中 `/api/course-generation/generate` 的教师/学生调用分布；
- 旧教师页面是否仍被外部书签、演示脚本或自动化访问；
- DOCX/PDF 原教案解析对浙大理论课、实践课样例的结构保真率；
- 旧正文模板迁入讲稿后跨学科真实模型质量；
- 统一任务后教案、讲稿、V6 的断线、重启、重复点击和幂等矩阵。
