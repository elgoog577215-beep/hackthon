# 灵知系统架构

> 文档状态：当前技术架构、代码与运行边界真源<br>
> 本文回答：产品定义怎样落成领域协议、前端组件、后端能力、Agent 编排、运行链和基础设施。<br>
> 本文不回答：产品为什么这样定义、当前成熟度、某次变更怎样施工或历史上为什么这样选择。

相关真源：

- [产品蓝图](./产品蓝图.md)：教学体系、产品逻辑、交互与界面的最高优先级真源。
- [产品状态](./产品状态.md)：当前成熟度、在建项、缺口和下一攻坚位置。
- [项目规则](../AGENTS.md)：跨任务长期有效的工程、执行和验收护栏。
- [`openspec/`](../openspec/)：具体字段、接口、状态机和高影响变更合同。

维护规则：

1. 本文只记录当前有效的技术结构和会约束多个模块的架构原则，不提前记录未落地设想。
2. 产品对象与关系先在产品蓝图确认，再由本文说明怎样实现；技术实现不得反向替代产品定义。
3. 模块、接口和协议发生稳定变化后更新本文；实施过程和测试数字分别进入 OpenSpec、产品状态或验收证据。
4. 新实现替代旧实现时直接修正当前结构；兼容原因和退役历史交给 Git 与项目决策记录。

## 1. 技术架构设计

### 1.1 架构目标

技术架构承接产品蓝图中的教学体系、产品逻辑、交互与界面，目标不是堆积更多页面和生成接口，而是把稳定产品关系实现为可复用、可组合、可验证的能力。

```text
产品对象与规则
-> 结构化领域协议
-> 前端通用交互能力 + 后端原子领域能力
-> 工作流和 Agent 按任务组合
-> 确定性状态、权限、质量与恢复
```

核心原则：

1. 前端把稳定的对象、关系、状态和动作沉淀为共享组件与工作区能力，页面只负责组合。
2. 后端把类型判断、结构规划、教学块组合、内容生成、质量检查、局部修复和版本确认拆成边界清楚的能力。
3. 教学类型、学科类型、小节类型、教学块和教学产物之间的稳定关系进入注册表、规则或领域协议，不长期隐藏在 Prompt、页面分支或巨型服务中。
4. Agent 通过工具和结构化协议组合能力；权限、正式真源、状态迁移、质量门、恢复和发布继续由确定性系统控制。
5. 新增教学类型、学科分型、教学表达或生成提供方时，优先扩展注册表和组合规则，不复制整条前后端链路。
6. 跨模块链路保留来源、任务、版本、输入摘要、输出状态、失败原因和恢复入口。

### 1.2 总体分层

```text
Vue 页面与工作区
-> 共享交互组件 / Pinia 领域投影
-> FastAPI HTTP / WebSocket 接入层
-> 应用服务与工作流编排
-> 领域对象、规则、命令与质量门
-> 仓库、任务工作区与修订日志
-> AI / 检索 / 文件 / 独立 Runner 等提供方
```

各层职责：

| 技术层 | 负责 | 不负责 |
| --- | --- | --- |
| 页面与工作区 | 组织当前任务、上下文和界面状态 | 保存正式业务事实、复制领域规则 |
| 共享组件与 Store | 复用交互语义、投影服务端状态 | 成为第二真源、独立发明状态机 |
| 接入层 | 身份、鉴权、参数、HTTP 错误和响应转换 | 复制领域状态机和生成逻辑 |
| 应用与编排层 | 组合已有领域能力、管理任务和检查点 | 绕过领域不变量直接写仓库 |
| 领域层 | 对象、规则、命令、质量合同和状态迁移 | 依赖具体页面布局 |
| 仓库与任务层 | 持久化、修订、幂等和恢复 | 解释产品语义 |
| 外部提供方 | 模型、检索、文件解析、渲染和隔离执行 | 决定正式状态和用户权限 |

### 1.3 仓库结构

| 目录 | 职责 |
| --- | --- |
| `frontend/` | Vue 3、Vite、Pinia、教师工作台、学习现场、PPT 工作台和中英文界面 |
| `backend/` | FastAPI、领域服务、生成编排、学习运行时、AI 能力、持久化和任务恢复 |
| `runner/` | 独立代码执行服务；主后端通过客户端调用，不直接执行用户代码 |
| `tests/`、`backend/tests/` | 兼容回归与当前后端领域测试 |
| `scripts/` | 迁移、验收、部署、录屏和诊断工具，不拥有运行时真源 |
| `openspec/` | 高影响变更的提案、设计、稳定规格、任务与归档 |
| `docs/` | 产品、状态、架构、事实、验收、研究和历史归档 |

开发态由 `dev.sh` 或 `dev.bat` 启动前后端；生产态由 FastAPI 托管前端构建产物，入口 HTML 与带哈希资源使用不同缓存策略。

## 2. 领域协议与正式真源

### 2.1 正式真源

| 领域 | 正式真源 | 不能作为真源 |
| --- | --- | --- |
| 正式课程 | `CourseDocument + ordered CourseBlock[]` | 生成草稿、旧 `nodes`、Markdown 导入稿、前端缓存 |
| 课程生成 | `GenerationJob + job_id 隔离工作区 + 发布回执` | 课程 ID、本地百分比、单条 WebSocket 消息 |
| 课程教学设计 | 版本化 `CourseTeachingPlan` 与分讲确认修订 | Markdown 投影、页面表单草稿 |
| 教师分讲资产 | `TeacherLessonAuthoringRepository` 中按 `LessonUnit` 保存的编排、教案、讲稿、PPT 关系与修订 | 上传原件、旧整课快照 |
| 课程知识 | 当前课程 `CourseKnowledgeBase` 与稳定课程内 ID | 模型临时标签、目录标题、前端关系图 |
| 学科知识 | 版本化 `SubjectKnowledgeLibrary` | 单门课程自造的正式学科 ID |
| 正式题库与试卷 | 当前题库修订；试卷的有序题目修订引用和固定题库修订 | 前端勾选、复制题干答案 |
| 学习事实 | `LearningEvent`、`LearningRecord`、`PracticeAttempt`、快照与诊断 | AI 会话、页面统计、模型主观判断 |
| 学习者模型 | 从同批正式事实确定性重算的 `LearnerModel` | 可独立写入的画像副本 |
| 课程调整 | `CourseEvolutionPlan`、领域命令、修订和幂等回执 | 组件本地候选、旧 `CourseChangeSet` |
| 教学表达 | 来源绑定的 `TeachingRepresentation / SlideDeckSpec` | 浏览器临时重组、导出器二次摘要 |
| 配套文档 | 版本化模板、结构化输入和当前不可变修订 | DOCX/Markdown 导出、文件列表投影 |
| 产品使用记录 | 非权威、追加式 `UsageEvent` | 学习事实、服务日志、第三方画像 |

任何兼容读取都从正式真源生成投影。新写入不得扩大旧对象或建立平行协议。

### 2.2 修订、状态与命令

- 正式新增、替换、退休、恢复、移动、拆分和合并通过领域命令完成。
- 工作稿可变，正式修订不可变；自动保存只保存工作现场。
- 任务状态、质量结果、确认状态、发布许可和课程可见性正交保存，不能压成一个字段。
- 影响分析只标记真正依赖当前修订的下游对象；失败保留最后可用版本。
- 幂等键、基线修订和稳定对象 ID 共同阻止重复应用、静默覆盖和错位恢复。
- 前端 Store、WebSocket 和文件清单只是投影，必须能从 HTTP 正式状态重新对账。

### 2.3 身份与权限

身份域由路由元数据或领域调用显式声明为 `teacher` / `learner`，HTTP 客户端只执行声明，不根据 URL 猜测。

当前 `X-User-Id` 是本地稳定身份边界，不等价于正式登录鉴权：

- 未发布教师课程、课程任务、资料和工作稿按 `owner_id` 隔离。
- 发布后的课程可以供学习者读取，但学习事实仍按学习者身份分区。
- 教师预览使用教师身份，不读取或写入学生数据。
- 正式多租户部署前必须接入真实账号或会话 authority。

## 3. 前端架构

### 3.1 路由与工作区

| 页面或组件 | 技术职责 |
| --- | --- |
| `TeacherTeachingCalendarView.vue` | 教师首页壳，组合课程范围、教学日历和课次状态 |
| `CourseWorkspaceView.vue` | 课程空间路由壳，组合默认工作台、文件视图、正式课程预览和专用工作区入口 |
| `TeacherCourseWorkbench.vue` | 教师课程生产主界面，组织阶段导航、当前内容和资料栏 |
| `TeacherCourseSpaceView.vue` | 文件视图及兼容工作台投影，组织目录、列表和详情 |
| `CourseReferenceTray.vue` | 当前正式目标的主来源、参考资料和联网来源关系 |
| `WebResearchDialog.vue` | 阶段或课次范围的检索、正文深读、来源审阅和资料采纳 |
| `QuestionBankReviewPanel.vue` | 唯一题库工作台外壳，承载导入、生成、审核、筛选与组卷 |
| `QuestionBankImportWorkspace.vue` | 多文件导入、识别、校对和逐份写入任务 |
| `CompanionDocumentStudio.vue` | 配套文档模板、结构化表单、修订预览和导出 |
| `LearningView.vue` | 正式课程运行态，组合目录、正文、练习、记录、诊断与 AI |
| `PptWorkspaceView.vue` | 同源 PPT 文书审阅、页面编译、预览和导出 |
| `SideAIPanel.vue` | 学习现场与局部编辑中的会话、候选和命令回执 |

教学日历首页使用 `/courses`，课程空间使用 `/course/:courseId/workspace/:mode`，正式课程与 PPT 工作区分别使用 `/course/:courseId/learn/:nodeId?` 和 `/course/:courseId/ppt`。旧教师地址只保留兼容重定向，不挂载独立产品壳。

### 3.2 共享交互能力

前端复用单位不是“长得像的一张卡片”，而是稳定交互语义：

| 共享能力 | 统一处理 |
| --- | --- |
| 工作区骨架 | 阶段或目录导航、当前内容、上下文资料和响应式收拢 |
| 正式文档面 | 阅读、解锁编辑、工作稿、AI 候选、差异、确认和恢复 |
| 结构导航 | 章、讲、小节、文件夹与稳定对象定位 |
| 资料关系 | 主来源、参考资料、联网来源、反向使用关系与删除保护 |
| 生成任务 | 提交、流式片段、真实状态、暂停、重试、恢复与完成回执 |
| 审阅与影响 | 原文/候选差异、影响对象、保持不变项、阻断与部分接受 |
| 版本状态 | 工作、确认、过期、降级、最后可用版本和来源修订 |
| 错误反馈 | 统一分类、原位原因、一个主要恢复动作和可追踪错误码 |

共享组件只拥有展示和短时交互状态。课程、资料、任务、候选和正式修订继续由各自领域 Store 与后端真源拥有。

### 3.3 状态投影

Pinia Store 按领域投影组织：

- `course.ts`：当前课程和正式课程文档。
- `generation.ts`：生成任务、检查点、WebSocket 和恢复。
- `learningSession.ts`、`learningProgress.ts`：当前学习会话和进度。
- `learnerModel.ts`：后端正式学习者模型的只读投影。
- `aiTeacher.ts`：教师或学生视角的会话、候选、动作和回执。
- `courseEvolution.ts`、`changeProposals.ts`：课程调整计划和审阅状态。
- `teachingPlanWorkbench.ts`、`teachingRepresentations.ts`：教案与教学表达。

多个界面消费同一状态时复用一个 Store。组件局部状态只管理展开、选中和临时输入，不能成为持久业务状态。

### 3.4 渲染、语言与设备

- Markdown、KaTeX、Mermaid、代码高亮和安全链接通过共享渲染链处理。
- 课程正文、AI 结果和导出预览不得在不同页面分别修补同一种语法。
- 用户可见固定文案进入中英文词典；课程内容保持原始语言。
- 桌面与移动端共用对象和状态协议，但布局、覆盖层、触控和密度分别验收。
- 身份、课程、讲次、内容锚点和返回位置通过稳定路由或状态参数传递，不从可见标题反推。

## 4. 后端能力架构

### 4.1 分层责任

`backend/main.py` 组装 FastAPI，`backend/routers/` 负责接入。路由只处理鉴权、参数、HTTP 错误和响应转换；应用服务协调领域能力；领域服务拥有不变量；仓库拥有持久化；编排器不复制领域规则。

后端 API 按领域能力、命令或稳定投影设计，不按页面卡片设计专用状态接口。

### 4.2 领域能力地图

| 能力 | 主要模块 |
| --- | --- |
| 课程文档与命令 | `course_document.py`、`course_repository.py`、`course_commands.py`、`course_revisions.py` |
| 课程生成与大纲 | `course_type_contracts.py`、`course_outline_planning.py`、`course_service.py`、`course_generation_workflow.py`、`generation_workspace.py` |
| 教学策略与编排 | `course_pedagogy.py`、`course_pedagogy_archetypes.py`、`lesson_arrangement.py` |
| 教案 | `course_teaching_plan_v3.py`、`teaching_plan_workbench.py`、`teaching_plan_impact.py`、`downstream_rebuild.py` |
| 分讲讲稿 | `teacher_script.py`、`teacher_lesson_authoring.py`、`routers/teacher_lesson_authoring.py` |
| 课程知识 | `course_knowledge_base.py`、`course_knowledge_map.py`、`subject_knowledge.py` |
| 课程知识演进 | `course_knowledge_revisions.py`、`course_knowledge_commands.py`、`course_knowledge_impact.py`、`course_knowledge_relocation.py` |
| 题库、试卷与练习 | `assessment_*`、`question_bank.py`、`exam_papers.py`、`practice_*`、`diagnostic_*` |
| 学习事实与模型 | `learning_events.py`、`learning_records.py`、`learner_model.py`、`learning_runtime.py` |
| 学习事实治理 | `learning_governance.py`、`learning_source_links.py`、`learning_scope_corrections.py` |
| AI 老师与课程调整 | `ai_teacher_*`、`change_proposals.py`、`course_evolution.py`、`course_change_planning.py` |
| 教学表达与 PPT | `representation_*`、`teaching_representations.py`、`ppt_template_packs.py`、`slide_deck_v6_*.py` |
| 资料、文件与排课 | `material_storage.py`、`teacher_course_space.py`、`course_web_research.py`、`teaching_calendar.py` |
| 配套文档 | `course_companion_documents.py`、`routers/companion_documents.py` |
| 任务与恢复 | `task_manager.py`、生成工作区、检查点与任务路由 |
| AI 与检索基础设施 | `ai_base.py`、`ai_capacity.py`、`web_retrieval.py`、各能力专用 AI 服务 |
| 产品使用分析 | `product_usage.py`、`usage_events.py` |

### 4.3 教学策略引擎

教学策略不由一个 Prompt 独占，而由多级规则与注册表共同编译：

```text
course_type_contracts
  -> 整课组织合同
course_pedagogy + subject variant
  -> 学科行为、质量护栏和专业模块集合
lesson archetype + LessonUnit 目标
  -> 当前讲次组织策略
lesson_arrangement
  -> 教师可见本讲课型与有序教学块
teaching plan / script / assessment / representation compiler
  -> 各类正式产物
```

架构约束：

- 明确教师选择与自动推断分开保存；推断结果带证据和置信度。
- 高层偏好通过结构化合同影响已确定类型下的组合，不把通用偏好文本混入学科分类信号。
- 课型和教学块注册表保存稳定 ID、角色、适用范围、输出合同和质量要求。
- 生成服务消费编译后的合同，不在每个下游 Prompt 中重新解释一遍类型关系。
- 新学科或新课型通过扩展注册表、映射和评测集进入，不复制教案、讲稿和 PPT 整链。

### 4.4 资料与证据

原始 `mat-*` 只保存一次，课程文件空间通过稳定引用把它归入当前课程。正式目标保存一个主来源、多份参考资料和可选联网来源；移除引用不删除原件，仍被引用的原件不能直接删除。

教师显式启用联网研究时，统一检索网关生成不可变检索包或课程范围调研会话。公开 HTML/PDF 正文经过 URL 安全、正文质量、超时、体积和注入过滤；单个来源失败只回退该来源摘要。教师勾选后才转成课程资料，继续进入 parse -> evidence -> grounding，不直接成为正式内容。

## 5. Agent 与工作流架构

### 5.1 Agent 的位置

Agent 位于领域能力之上，负责理解任务、选择工具、编排步骤和汇总候选，不拥有正式数据，也不绕过既有工作流。

```text
用户目标 + 当前上下文
-> Agent 任务判断
-> 选择一个或多个结构化工具
-> 领域能力执行并返回候选/报告
-> Agent 解释结果与下一动作
-> 用户确认
-> 正式命令
```

一次模型调用、一个超长 Prompt 或一个巨型服务都不等于 Agent 架构。Agent 式设计至少要求：

- 工具有清楚的输入、输出、权限、状态和失败合同。
- 工作流可以组合和恢复，而不是只能从头运行。
- 模型只处理需要语义判断的部分，确定性规则负责状态和安全。
- 候选、解释和正式写入分开，用户确认不可省略。
- 每次执行可以追踪使用的对象、资料、修订、模型能力和质量结果。

### 5.2 工作流编排

稳定工作流负责必须发生的依赖和质量门；Agent 负责在允许范围内选择能力和顺序：

| 内容 | 稳定工作流负责 | Agent 可决定 |
| --- | --- | --- |
| 大纲 | 课程基线、结构确认、批次、质量和修订 | 资料如何摘要、候选结构如何解释 |
| 教案 | 已确认大纲、课型、教学块、覆盖和确认 | 如何补充案例、活动和教学表达 |
| 讲稿 | 已确认教案、块顺序、知识和来源 | 每块的具体讲述、例子与过渡候选 |
| 题库 | 出题意图、公开/私有隔离、验证和审核 | 题目候选、变式和诊断解释 |
| PPT | 已确认讲稿、文书门禁、来源保真和渲染 | 页面叙事、构图和素材建议 |
| 课程调整 | 证据、范围、影响、命令和复验 | 假设、反证、候选修改和说明 |

Prompt 模板是能力内部实现，不拥有产品类型、正式状态和跨能力依赖。

### 5.3 AI 提供方与容量

AI 调用统一经过 `AIBase` 和共享容量控制。不同能力可以使用专用 Prompt、模型和解析器，但必须复用：

- 预算、超时、并发、重试和熔断。
- 敏感信息与最小上下文边界。
- 结构化输出解析和质量门。
- 明确降级、最后可用结果和任务回执。

模型失败只影响当前最小工作单元，不能清空整门课程或伪造成功。

## 6. 主要运行链

### 6.1 教师课程生产链

```text
TeacherCourseBrief + 教学类型/学科画像 + 资料引用
-> 同一 course_id 下的空课程空间
-> teacher_outline_generation 生成章节骨架
-> 教师确认逐章讲次规模
-> 恢复同一任务生成完整大纲
-> 本讲课型与教学块编排
-> 分讲教案工作稿、质量检查与确认
-> 分讲讲稿按块生成、编辑与确认
-> PPT 文书编译、审阅与确认
-> 从已确认文书确定性编译 Web/PPTX
```

题库、试卷、教学日历和配套文档读取同一课程基线，但不是上述链路的阻断步骤。目录、教案、讲稿和 PPT 按有界工作单元保存检查点；失败只重试当前缺失单元。

`lesson_arrangement.py` 是教学策略到教师可见本讲编排的适配层。它复用学科画像、细课型和模块注册表，保存工作与确认修订并绑定大纲范围修订；大纲变化后旧编排进入 `stale`。教案生成只消费当前已确认编排。

`TeacherLessonAuthoringRepository` 保存分讲教案、讲稿、编排、来源关系和任务投影。讲稿以有序块为真源，连续 Markdown 是确定性投影；局部生成失败保留已完成块，完成态不自动确认。

### 6.2 学习运行链

```text
CourseDocument + 当前学习身份
-> LearningRuntime
-> 阅读、记录、正式练习和诊断
-> 正式领域对象 + LearningEvent
-> LearnerModel 确定性重算
-> 当前进度、连续性和可解释动作
```

`LearningRuntime` 是无独立存储的协调投影。一级章节只负责导航，只有具备正文和正式目标的可学习节点写入进度；普通位置同步不能跨课程版本迁移快照。

### 6.3 AI 老师与课程生长链

```text
当前课程锚点 + LearningRuntime + 最小必要证据
-> AIContextPackage
-> 解释、举例、简化、提问或白名单动作
-> 候选与影响预览
-> 用户接受、拒绝或调整
-> 领域命令 + 幂等回执
-> 新修订与独立效果证据
```

AI 会话不拥有学习事实。联网状态属于会话设置，默认关闭并按会话隔离；只有普通回答可以检索，正式写入动作不出站。

### 6.4 教学表达与 PPT 链

```text
已确认讲稿修订 + 已确认教案追溯 + PPT 阶段资料
-> ppt_manuscript_v1 工作稿
-> 文书质量门与教师确认
-> compile_slide_deck_v6_from_manuscript
-> TeachingRepresentation / SlideDeckV6
-> Web 预览与 PPTX 导出
```

PPT 内容生产与渲染分两步：

1. 文书编译逐页教学任务、主要结论、可见文案、页型、版式、构图、讲稿来源和证据关系。
2. 教师确认文书后，渲染器只从该修订确定性编译页面，不再调用内容模型改写知识。

完整教师讲述进入 speaker notes，学生画布只保留当前教学动作需要的定义、条件、推导、例题、步骤、结论和任务。浏览器预览与 PPTX 消费同一页面规格。

老师上传的 PPTX 进入独立审阅链：保存不可变原件，建立页与文字块索引，对照当前课程产物生成报告；手工和 AI 修改形成可恢复修订，教师确认后才登记为当前 PPT。上传原件不进入讲稿生成 PPT 的两步链。

V5 只保留历史读取和导出兼容；新写入使用 V6。表达失败保留最后可用版本，不能阻断课程阅读。

### 6.5 课程知识演进链

```text
知识白名单命令 + 当前知识库修订
-> 知识候选、质量门和双向影响
-> 用户确认
-> 与课程修订原子提交 + 幂等回执
-> 下游教案、讲稿、练习和 PPT 重建计划
```

知识按稳定实体寻址。改一个知识点只失效引用它的下游；稳定知识 ID 和来源只能通过显式旧新映射迁移。候选遇到基线前移时先重定位，目标或字段已冲突则阻断，不静默覆盖。

### 6.6 教师全课变更链

`course_change_planning.py` 保存老师原话、解释、约束、保护项和执行策略，不拥有正式课程。结构计划使用新增、更新、移动、排序、拆分、合并、退休和重建等原语，并为每类受影响产物给出复用、重绑、局部改写、重建、退休或阻断决定。

前端只渲染合同返回的资产、差异、影响和回执；缺少真实预览时显示待载入，不补造内容。计划应用继续复用现有课程调整、候选审阅和正式命令。

### 6.7 任务、事件与恢复链

任务由 `task/job ID` 唯一控制，`course_id` 只表示归属，`owner_id` 负责查询和控制隔离。HTTP 提供完整状态，WebSocket 提供低延迟增量；前端缓存保存最后现场，联网后按任务 ID 与服务端对账。

部署重启根据持久任务、检查点和隔离工作区恢复，只继续未完成单元。恢复、手动执行、自动执行和兼容入口必须共用领域函数、质量门和修订检查。

### 6.8 产品使用记录链

```text
稳定 Vue route / 写操作终态 / 分类客户端错误
-> 前端有界队列
-> /api/usage-events/batch
-> 事件与属性白名单
-> UsageEvent
-> 个人汇总、导出、删除与受保护聚合
```

`UsageEvent` 只回答产品怎样被使用，不回答学习者是否掌握或课程是否发布。请求正文、答案、Prompt、错误消息、堆栈、IP、User-Agent、Cookie 和设备指纹不得进入账本。

## 7. 外部、存储与安全边界

- 联网调用统一经过 `web_retrieval.py` 的 `SearchProvider`。默认本机 SearXNG 只映射回环地址；Exa 仅作显式兼容，不自动兜底。
- 联网只覆盖教师显式启用的课程资料调研、题库和普通 AI 回答。评分、作答诊断、学习者模型、课程结构演化以及 PPT 渲染不出站。
- 文件上传进入资料管线，浏览器生成 multipart boundary，后端最终校验类型、大小、所有权和用途。
- 用户代码只通过 `runner/` 隔离执行，主后端不直接 `exec` 用户代码。
- 本地 JSON、任务工作区和导出文件只是当前存储介质，不表示对象共享生命周期。
- 源码、用户数据、缓存、演示产物和发布包分开管理；Prompt 正文、密钥和模型推理不进入日志或 Git。
- `usage_events.json` 是运行数据并被 Git 忽略；全局分析只返回聚合，原始使用记录只能由所属身份导出或删除。

## 8. 兼容、降级与扩展

- 旧课程可以只读兼容，新的正式写入必须进入当前领域命令和当前 schema。
- 旧字段、旧 Store、旧路由和旧任务只有明确读取边界、迁移门和退出条件，不成为新能力依赖。
- 模型失败返回可恢复失败、局部明确降级或最后可用结果，不能虚构正式业务内容。
- 新增状态前先确认现有拥有者；新增接口前先确认能否扩展稳定领域投影。
- 新增 AI 能力必须说明输入依据、输出类型、持久化方式、用户控制、失败降级、恢复方式和旧入口退出计划。
- 新增前端界面先判断能否复用现有工作区、正式文档面、资料关系、任务和审阅能力；不能复用时说明新的稳定交互语义。
- 新增后端能力先判断能否由现有领域工具与注册表组合；不能组合时才增加新原子能力。
- 具体字段、状态机、接口和验收场景以当前正式 OpenSpec 为准。
