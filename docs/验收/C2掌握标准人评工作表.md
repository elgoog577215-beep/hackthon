# C2 掌握标准可判定性——教研人评工作表

> 由 `dev/lz-knowledge` 生成，取自 `~/lingzhi` 全部 5 门有知识库的真实课程，
> 共 146 条掌握标准，按固定步长均匀抽 10 条（非挑选）。

## 怎么判

清单 C2 的验收标准是：**每条都能指出"用什么题、什么作答表现算达标"**。
请对每条只回答两个问题，不必写理由：

1. 看完 `可观察表现` + `验证方式`，你能不能出一道题去测它？（能 / 不能）
2. 学生答成什么样算达标，是否明确？（明确 / 模糊）

**注意两个字段的现状**：`独立度` 与 `迁移层级` 在修复前是写死的
（146/146 都是 `independent`、142/146 都是 `variation`），下面这批样本
取自**修复前**已生成的课程，所以这两列基本没有区分度是预期内的——
请评的是前两列的文字质量，这两列留作重生成后的对照基线。

---

## 1. 双向转换流畅

- **课程**：矩阵与线性变换
- **可观察表现**：能直接从箭头位置写出正确坐标，并且从坐标画出箭头端点，无顺序错误
- **验证方式**：给出两个几何情形（起点不在原点）和两个坐标情形，全部正确
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 2. 能解释矩阵的列与基向量的对应关系

- **课程**：矩阵与线性变换
- **可观察表现**：给定矩阵A，能书面写出T(e₁)和T(e₂)的坐标，并能利用它们写出T(x,y)的表达式
- **验证方式**：给矩阵，要求写出每个基向量的变换坐标，并表达任意向量的变换
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 3. 能应用秩-零化度定理求解问题

- **课程**：矩阵与线性变换
- **可观察表现**：给定矩阵的秩，能计算零化度；或给定零化度，计算秩。
- **验证方式**：给定A为5×3矩阵，秩为2，求零化度并解释意义。
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 4. 独立判断浓度对速率的影响并解释

- **课程**：化学反应速率与化学平衡
- **可观察表现**：能独立面对一个具体反应（如N2+3H2⇌2NH3），说出增大N2浓度时速率如何变化，并给出理论解释（活化分子数增加→有效碰撞增加）。
- **验证方式**：提供一道选择题或简答题，要求判断速率变化并用有效碰撞理论解释。
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 5. 独立应用勒夏特列原理

- **课程**：化学反应速率与化学平衡
- **可观察表现**：能独立完成3道不同条件改变的平衡移动方向判断题，并正确解释原理适用性，正确率100%
- **验证方式**：通过选择题和简答题验证
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 6. 正确理解平衡时浓度特征

- **课程**：化学反应速率与化学平衡
- **可观察表现**：能独立完成判断题组：正确判断“达到平衡时各组分浓度相等”为错误，并能用具体例子解释。
- **验证方式**：提供多道涉及平衡浓度表述的题目（包括使用计算数据），要求判断并提供理由，全对即掌握。
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 7. 能正确计算向量空间的维度

- **课程**：矩阵与线性变换
- **可观察表现**：对于常见的 R^n 及其子空间（如过原点的平面、直线），能正确说出维度
- **验证方式**：给出多个子空间描述（如 {(x,y,z) : x+y+z=0} 在 R^3 中），要求计算维度
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 8. 正确计算2×2行列式

- **课程**：矩阵与线性变换
- **可观察表现**：独立计算至少5个不同的2×2矩阵的行列式，全部正确
- **验证方式**：随机给出5个2×2矩阵，要求写出行列式值
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 9. 能独立正确求出给定特征值对应的特征向量集合

- **课程**：矩阵与线性变换
- **可观察表现**：对于给定的矩阵A和特征值λ，能建立并化简齐次方程组，正确书写基础解系，并表达特征向量的一般形式（参数表示），注明参数非零
- **验证方式**：提供2×2和3×3矩阵的不同特征值，包括单特征值和重特征值，学生能正确求解特征向量
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊

## 10. 验证并应用代数性质

- **课程**：矩阵与线性变换
- **可观察表现**：独立验证3组矩阵的结合律和分配律，并构造1个交换律反例；验证过程完整，结果正确，并能口头解释每个性质的意义
- **验证方式**：检查验证计算的正确性，反例是否有效，以及解释的清晰度
- **独立度 / 迁移层级**：`independent` / `variation`
- **能否出题**： ☐ 能　☐ 不能
- **达标线是否明确**： ☐ 明确　☐ 模糊
